---
phase: 03-session-metrics
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/metrics/metrics.ts
  - src/metrics/metrics.test.ts
  - src/ui/ResultsView.tsx
  - src/index.css
autonomous: true
requirements: [METR-03, METR-04]
user_setup: []

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "On completion, the user sees the five slowest keystrokes grouped by the logical character committed (CommittedChar.data code point, D-03) — not by physical KeyboardEvent.code — ranked by descending median gap, gated by >=3 post-outlier-filter samples per character (D-04, METR-03)."
    - "Outlier gaps of exactly 1000ms or exactly 25ms are discarded (the filter is strictly `gap > 25 && gap < 1000`, per REQUIREMENTS.md's '>1000ms and <25ms discarded' wording) — only gaps strictly between 25ms and 1000ms feed the median."
    - "D-04's minimum-sample gate counts POST-outlier-filter samples, not raw pre-filter occurrences — a character with 5 raw occurrences but only 2 samples surviving the filter is excluded from the list (Pitfall 4)."
    - "The slowest-keys list never renders more than 5 rows, even when more than 5 characters clear the threshold — computeSessionMetrics's slowest5 array is always sliced to at most 5 entries after sorting."
    - "When 1-4 characters clear the threshold, the list renders exactly that many rows (never padded to 5) with no extra caption explaining the shorter count."
    - "When zero characters clear the threshold, the slowest-keys section is replaced entirely by the fixed 'Not enough repeated characters...' fallback message, while the WPM and accuracy stats still render normally above it — a partial metrics result is a valid, expected shape, never a whole-panel error."
    - "A slowest-key entry whose logical character is a space or newline renders its key-chip using glyphFor from src/trainer/state.ts (middle dot for space, down-arrow for newline) — reused, not reimplemented — so a slow space/newline stays visually identifiable."
    - "A multi-codepoint (IME composition) CommittedChar record attributes its inter-record latency gap only to the last codepoint in that record — earlier codepoints in the same record are excluded from latency sampling entirely, never fabricating a zero or split gap (Pitfall 7)."
    - "The results panel always renders all three metric groups together and in this fixed order: WPM, then accuracy, then the slowest-keys section (or its fallback) — matching METR-01 -> METR-02 -> METR-03's own ordering; none is ever shown independently of the others."
    - { statement: "The results panel appears with a fade-in capped at the existing 150ms motion budget, wrapped in @media (prefers-reduced-motion: no-preference), and causes no layout shift to the trainer surface or any content above it — it only expands the page downward.", verification: backstop }
    - { statement: "A slowest-key chip's rendered glyph never wraps, truncates, or overflows its chip — the chip's content is always bounded to 1-2 visible characters (a single logical code point, or the 2-character whitespace glyph substitute).", verification: backstop }
    - "METR-03's exact boundary: a character with exactly 2 post-filter samples is excluded from the slowest-5 list; a character with exactly 3 post-filter samples is included and its median is computed normally over those 3 values (D-04's own minimum, tested at both sides of the boundary)."
    - "METR-03's outlier-filter precision contract: filtered = samples.filter(gap => gap > 25 && gap < 1000) — both bounds are strict/exclusive, so a gap of exactly 25ms or exactly 1000ms never survives into the median calculation."
    - "Extending metrics.ts with slowest-key tracking introduces no I/O, no shared mutable module state, and no async behavior — computeSessionMetrics remains a pure, synchronous, side-effect-free function after this plan (METR-04's 're-runnable, no I/O' requirement continues to hold)."
  artifacts:
    - path: "src/metrics/metrics.ts"
      provides: "slowestFive/median helpers + SlowestKeyEntry type + slowest5 field on MetricsResult (D-03/D-04, METR-03)"
      contains: "slowest5"
    - path: "src/metrics/metrics.test.ts"
      provides: "golden-case tests locking D-03/D-04 and Pitfalls 4/5/7"
      contains: "slowest5"
    - path: "src/ui/ResultsView.tsx"
      provides: "slowest-keys list or 'not enough data' fallback, glyphFor reuse for whitespace chips"
      contains: "results-slowest"
    - path: "src/index.css"
      provides: "results panel card/grid/key-chip/fade-in CSS per UI-SPEC's exact spacing tokens"
      contains: "results-panel"
  key_links:
    - from: "src/ui/ResultsView.tsx"
      to: "src/trainer/state.ts"
      via: "glyphFor(char) reused for a slowest-key chip whose char is space or newline"
      pattern: "glyphFor\\("
    - from: "src/metrics/metrics.ts"
      to: "src/ui/ResultsView.tsx"
      via: "slowest5 array rendered as a bounded list or the 'not enough data' fallback"
      pattern: "slowest5"
  prohibitions:
    - "Never render WPM/accuracy with performance-threshold color-coding (no good/bad green/red banding on these numbers) — explicit locked UI-SPEC rule against gamification framing."
    - "Never add a celebratory/gamified framing (confetti, streak counters, exclamation-point copy, pass/fail bands) to the results panel — explicitly out of scope per REQUIREMENTS.md's Out of Scope table."
    - "Never persist computed metrics anywhere (no IndexedDB, no localStorage, no network call) — PERS-01/02 is explicitly v2; metrics are computed live and discarded on restart (D-06)."
---

<objective>
Complete Phase 3's metrics engine with the five-slowest-keystrokes computation (D-03/D-04,
METR-03), extend `ResultsView` to display the ranked list or its "not enough data" fallback,
and apply the full visual/motion polish from `03-UI-SPEC.md` (card layout, key-chip styling,
reduced-motion-safe fade-in).

Purpose: this is the expansion wave that builds out from 03-01's proven tracer (completion ->
WPM + accuracy -> auto-revealed panel) to cover the remaining locked requirement (METR-03) and
finish the phase's visual contract.

Output: `src/metrics/metrics.ts` extended with slowest-key aggregation, `ResultsView.tsx`
extended with the slowest-keys section, `src/index.css` extended with the results panel's
final styling.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-session-metrics/03-CONTEXT.md
@.planning/phases/03-session-metrics/03-RESEARCH.md
@.planning/phases/03-session-metrics/03-UI-SPEC.md
@.planning/phases/03-session-metrics/03-PATTERNS.md
@.planning/phases/03-session-metrics/03-01-SUMMARY.md

<interfaces>
<!-- Current on-disk shape after 03-01 (read this session) — extend, don't re-derive. -->

src/metrics/metrics.ts (03-01, CURRENT shape before this plan):
  export const METRICS_SCHEMA_VERSION = 1
  export interface MetricsResult { schemaVersion: number; wpm: number; accuracy: number }
  export function computeWpm(correctChars: number, elapsedMs: number): number
  export function computeAccuracy(correctAttempts: number, totalAttempts: number): number
  export function computeSessionMetrics(target: string, charLog: readonly CommittedChar[], markers: readonly CaptureMarker[], now: number): MetricsResult
  // private: replayAttempts(target, charLog) -> { correctAttempts, incorrectAttempts }

src/ui/ResultsView.tsx (03-01, CURRENT shape before this plan):
  export interface ResultsViewProps { metrics: MetricsResult }
  export function ResultsView({ metrics }: ResultsViewProps)
  // <section className="results-panel" role="status" aria-live="polite"><h2>Results</h2>
  //   <div className="results-stats">
  //     <div className="results-stat">{Math.round(metrics.wpm)} wpm</div>
  //     <div className="results-stat">{Math.round(metrics.accuracy * 100)}%</div>
  //   </div></section>

src/trainer/state.ts (Phase 2, unchanged by this plan):
  export function glyphFor(char: string): string  // ' ' -> '·', '\n' -> '↵', else unchanged

src/index.css (unchanged sections relevant here):
  --space-xs: 4px; --space-sm: 8px; --space-md: 16px; --space-lg: 24px; --motion-duration: 150ms
  --color-surface, --color-border, --color-text, --color-text-muted (light/dark pairs already defined)
  @media (min-width: 640px) { ... } — the existing responsive breakpoint to reuse
  @media (prefers-reduced-motion: no-preference) { .banner { transition: opacity var(--motion-duration) ease; } ... }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Slowest-5 keystrokes — median/trim aggregation + list/fallback display (D-03/D-04, METR-03)</name>
  <precondition>src/metrics/metrics.ts exports computeSessionMetrics returning { schemaVersion, wpm, accuracy } and src/ui/ResultsView.tsx renders those two stats (03-01, complete)</precondition>
  <files>src/metrics/metrics.ts, src/metrics/metrics.test.ts, src/ui/ResultsView.tsx</files>
  <read_first>
    - src/metrics/metrics.ts (03-01's replayAttempts/computeSessionMetrics/MetricsResult — the exact function bodies to extend, not replace)
    - src/trainer/state.ts (glyphFor's exact whitespace-glyph mapping to reuse, not reimplement)
    - src/ui/ResultsView.tsx (03-01's two-stat markup to extend, not replace)
  </read_first>
  <behavior>
    - computeSessionMetrics's slowest5: given >=3 post-outlier-filter latency samples for a character, returns its median gap in slowest5, sorted descending, capped at 5 entries.
    - A character with exactly 2 post-filter samples is excluded entirely (D-04's minimum is 3, gated on post-filter count per Pitfall 4).
    - Outlier bounds are strict/exclusive: a gap of exactly 25ms or exactly 1000ms never survives the filter; a gap of 26ms or 999ms does.
    - A multi-codepoint (IME) commit's inter-record gap attaches only to the LAST code point of that record (Pitfall 7) — earlier code points in the same record contribute zero latency samples.
    - `{` and `[` are tracked as distinct entries even if they share a physical key (D-03 — grouping is by CommittedChar.data's code point, never KeyboardEvent.code).
  </behavior>
  <action>
    Extend `src/metrics/metrics.ts`: add `export interface SlowestKeyEntry { char: string; medianMs: number }`
    and add `slowest5: SlowestKeyEntry[]` to `MetricsResult`. Rewrite `replayAttempts`'s
    insert-branch loop to also track latency: change the private return shape to include
    `latencySamplesByChar: Map<string, number[]>`; track `let prevTMs: number | null = null`,
    updated to `rec.tMs` at the end of every loop iteration (both delete-branch and
    insert-branch); convert the insert-branch's `for...of` over `Array.from(rec.data ?? '')`
    into a `.forEach((ch, i) => ...)` so the codepoint index `i` is available; when
    `i === codepoints.length - 1 && prevTMs !== null`, push `rec.tMs - prevTMs` onto
    `latencySamplesByChar.get(ch) ?? []` (grouping key is the logical character `ch`, per D-03).

    Add module constants `const MIN_GAP_MS = 25`, `const MAX_GAP_MS = 1000`,
    `const MIN_SAMPLES = 3` (D-04, post-filter per Pitfall 4).

    Add a private `median(samples: readonly number[]): number` that guards
    `samples.length === 0` to return `0`; else sorts a copy ascending, computes
    `mid = Math.floor(sorted.length / 2)`, and for an even-length array reads both
    `sorted[mid - 1]` and `sorted[mid]`, returning their average only if neither read is
    `undefined` (falling back to `0` in that unreachable case); for an odd-length array reads
    `sorted[mid]` and returns it if not `undefined`, else `0`. This keeps every indexed read
    `noUncheckedIndexedAccess`-safe without a non-null assertion.

    Add a private `slowestFive(latencySamplesByChar: Map<string, number[]>): SlowestKeyEntry[]`
    that, for each `[char, samples]` entry, computes
    `filtered = samples.filter((gap) => gap > MIN_GAP_MS && gap < MAX_GAP_MS)`, skips the
    character if `filtered.length < MIN_SAMPLES`, else pushes `{ char, medianMs: median(filtered) }`;
    sorts the result array by `medianMs` descending; returns `.slice(0, 5)`.

    Update `computeSessionMetrics` to call `slowestFive(latencySamplesByChar)` and include it
    in the returned `MetricsResult` as `slowest5`.

    Extend `src/ui/ResultsView.tsx`: import `glyphFor` from `'../trainer/state'`. Add a
    `<div className="results-slowest">` section after the existing `results-stats` div,
    containing a `<span className="text-label text-muted">Slowest keys</span>` and then
    either (when `metrics.slowest5.length === 0`) a `<p className="text-muted">Not enough
    repeated characters in this exercise to measure yet. Try a longer or more varied
    exercise.</p>` fallback, or an `<ol className="results-slowest-list">` mapping
    `metrics.slowest5` to `<li key={entry.char} className="results-slowest-row">` containing
    a `<span className="key-chip">` showing
    `entry.char === ' ' || entry.char === '\n' ? glyphFor(entry.char) : entry.char` and a
    `<span>{Math.round(entry.medianMs)} ms</span>`.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/metrics/metrics.test.ts && pnpm exec tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "slowestFive" src/metrics/metrics.ts` is greater than or equal to 2 (definition + call site)
    - `grep -c "MIN_SAMPLES = 3" src/metrics/metrics.ts` equals 1
    - `grep -c "glyphFor" src/ui/ResultsView.tsx` is greater than or equal to 1
    - `grep -v '^\s*//' src/ui/ResultsView.tsx | grep -c 'dangerouslySetInnerHTML'` equals 0 (never inject raw markup — plain JSX text children only)
    - `pnpm exec vitest run src/metrics/metrics.test.ts` passes with golden cases covering: exactly-2-vs-exactly-3 post-filter samples, gap boundary values (25/26/999/1000ms), a more-than-5-eligible-characters case asserting the result is capped at 5, an IME multi-codepoint case asserting only the last codepoint contributes a latency sample, and a `{`-vs-`[` distinct-grouping case
  </acceptance_criteria>
  <done>computeSessionMetrics returns a correctly-gated, correctly-ranked, at-most-5-entry slowest5 array; ResultsView renders it or the fallback message; pnpm exec tsc reports zero errors.</done>
</task>

<task type="auto">
  <name>Task 2: Results panel visual polish — card layout, key-chip styling, fade-in motion</name>
  <files>src/index.css, src/ui/ResultsView.tsx</files>
  <read_first>
    - src/index.css (existing token set: --space-*, --color-*, --motion-duration; the .banner/.preview/.trainer-caret styling conventions to mirror; the existing @media (min-width: 640px) and @media (prefers-reduced-motion: no-preference) blocks)
    - .planning/phases/03-session-metrics/03-UI-SPEC.md (Spacing Scale and Interaction Contract sections — exact panel/key-chip/motion values)
  </read_first>
  <action>
    Append a new section to `src/index.css` (after the existing whitespace-glyph rules, with a
    header comment attributing it to Phase 3's UI-SPEC) defining:
    `.results-panel { background: var(--color-surface); border: 1px solid var(--color-border); border-radius: 6px; padding: var(--space-lg); display: grid; gap: var(--space-lg); }`;
    `.results-stats { display: grid; gap: var(--space-md); grid-template-columns: 1fr; }` plus
    a `@media (min-width: 640px) { .results-stats { grid-template-columns: 1fr 1fr; column-gap: var(--space-lg); row-gap: var(--space-md); } }`
    block, reusing the existing 640px breakpoint already used elsewhere in this file;
    `.results-stat { display: grid; gap: var(--space-xs); }`;
    `.results-slowest { display: grid; gap: var(--space-xs); }`;
    `.results-slowest-list { display: grid; gap: var(--space-xs); list-style: none; margin: 0; padding: 0; }`;
    `.results-slowest-row { display: flex; align-items: center; gap: var(--space-sm); }`;
    `.key-chip { display: inline-block; padding: var(--space-xs) var(--space-sm); border: 1px solid var(--color-border); border-radius: 4px; background: var(--color-surface); }`.
    Add the fade-in inside the existing `@media (prefers-reduced-motion: no-preference)` block
    alongside the current `.banner`/`button`/`textarea` transition rules:
    `.results-panel { animation: results-fade-in var(--motion-duration) ease; }` plus, outside
    that media block (keyframes are declared once, unconditionally, and only ever invoked from
    inside the guarded block), a `@keyframes results-fade-in { from { opacity: 0; } to { opacity: 1; } }`
    declaration.

    Confirm (read-only, no change) that `App.tsx`'s existing wrapper `div` around
    `CaptureSurface`/`ResultsView`/the Restart button already uses
    `style={{ display: 'grid', gap: 'var(--space-md)' }}`, so `ResultsView` — as a sibling grid
    item within that wrapper — automatically gets the UI-SPEC's required `--space-md` gap on
    both sides once it renders; no additional CSS or App.tsx change is needed for this gap.

    Confirm `ResultsView.tsx`'s root `<section>` still carries `className="results-panel"` and
    `role="status" aria-live="polite"` from the prior tasks; if it does not, rename it to
    `results-panel` here so the new CSS actually applies.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/ui/CaptureSurface.test.tsx && pnpm exec tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "results-panel" src/index.css` is greater than or equal to 1
    - `grep -c "results-fade-in" src/index.css` equals 2 (the animation declaration + the @keyframes rule)
    - `grep -c "prefers-reduced-motion" src/index.css` is greater than or equal to 2 (the pre-existing block plus this new rule's guarded placement)
    - `pnpm exec vitest run src/ui/CaptureSurface.test.tsx` still passes unchanged (a CSS-only change does not alter DOM structure or test assertions)
  </acceptance_criteria>
  <done>The results panel renders with the UI-SPEC's exact card/grid/key-chip styling and a reduced-motion-safe fade-in; all existing tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Slowest-key logical character -> DOM | The slowest-key chip renders a character drawn from `CommittedChar.data` (Phase 1's untrusted corpus content) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering / Information Disclosure | src/ui/ResultsView.tsx (slowest-key chip character) | high | mitigate | Render the key-chip character strictly as a plain JSX text child (or via `glyphFor`'s plain string return), never injected as raw markup — same convention as 03-01's stat values. Verified by a filtered negative grep in Task 1's acceptance criteria. |
| T-03-04 | Denial of Service | src/metrics/metrics.ts `median`/`slowestFive` | low | mitigate | `median` guards `samples.length === 0` and every indexed read against `undefined` (noUncheckedIndexedAccess-safe), returning `0` rather than throwing or producing `NaN`; `slowestFive` never calls `median` on fewer than `MIN_SAMPLES` (3) filtered entries. |

No `T-03-SC` row — this plan installs no npm/pip/cargo packages (03-RESEARCH.md's own Package Legitimacy Audit: not applicable).
</threat_model>

<verification>
- `pnpm exec vitest run` passes for the whole suite (no regressions in Phase 1/2/03-01 tests).
- `pnpm exec tsc --noEmit -p tsconfig.json` reports zero errors.
- Manually confirmable: an exercise with a repeated character (e.g. typed 3+ times) shows a ranked slowest-keys list with `ms` values; an exercise with no qualifying character shows the "Not enough repeated characters..." fallback instead, while WPM/accuracy still render above it.
</verification>

<success_criteria>
- The results panel shows all three metric groups (WPM, accuracy, slowest-5-or-fallback) together, in fixed order, styled per the UI-SPEC's exact spacing/color/motion contract.
- `src/metrics/metrics.ts` fully satisfies METR-01..04: pure, versioned, golden-tested, documented, re-runnable, with zero I/O.
</success_criteria>

<output>
Create `.planning/phases/03-session-metrics/03-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces (consolidated — spans 03-01 and 03-02)

- `src/metrics/metrics.ts`: `METRICS_SCHEMA_VERSION`, `MetricsResult` (interface), `SlowestKeyEntry`
  (interface, added here), `computeWpm`, `computeAccuracy`, `computeSessionMetrics` (all exported,
  established in 03-01); private `replayAttempts` (established in 03-01, extended here to also
  track per-character latency samples), private `median`/`slowestFive` (added here).
- `src/ui/ResultsView.tsx`: `ResultsViewProps` (interface), `ResultsView` (component) — WPM +
  accuracy stats established in 03-01; the slowest-keys list / "not enough data" fallback added
  here.
- `src/ui/CaptureSurface.tsx`: `onComplete?: (completedAt: number) => void` prop and internal
  `firedCompletedAtRef` — established in 03-01, unchanged by this plan.
- `src/ui/App.tsx`: `metrics` state, `handleComplete` function, the `ResultsView` mount site, and
  `setMetrics(null)` resets in `handleLoad`/`handleRestart` — established in 03-01, unchanged by
  this plan.
- `src/index.css`: `.results-panel`, `.results-stats`, `.results-stat`, `.results-slowest`,
  `.results-slowest-list`, `.results-slowest-row`, `.key-chip` classes + `results-fade-in`
  keyframes — added here.

---
phase: 08-parse-dependency-units
plan: 05
type: execute
wave: 3
depends_on: ["08-04"]
files_modified:
  - src/ui/RepoBrowser.tsx
  - src/ui/RepoBrowser.test.tsx
autonomous: true
gap_closure: true
requirements: [FILE-01, PLAN-03]

must_haves:
  truths:
    - "Last-wins token on file clicks: a slower first click cannot overwrite a faster second click's status or onPlanned (FILE-01 concurrency, PLAN-03 concurrency)"
    - "Import busy uses a separate import-generation counter so a file click cannot leave Import disabled (FILE-01 concurrency)"
  prohibitions:
    - statement: "MUST NOT let a blocked or commit click later receive onPlanned from an in-flight loadable"
      status: unverified
      verification: flagged
    - statement: "MUST NOT leave the Import button disabled because a file click stole the busy generation"
      status: unverified
      verification: flagged
  artifacts:
    - path: "src/ui/RepoBrowser.tsx"
      provides: "clickGenRef last-wins for every tree-file click; importGenRef for Import busy; blocked/commit bump generation before setStatus"
      exports: ["RepoBrowser"]
    - path: "src/ui/RepoBrowser.test.tsx"
      provides: "loadable-then-blocked, loadable-then-commit, and Import-vs-click isolation last-wins cases"
  key_links:
    - from: "src/ui/RepoBrowser.tsx onFileClick"
      to: "clickGenRef"
      via: "++clickGenRef.current before any setStatus, including COPY.blocked for commit and non-loadable"
      pattern: "clickGenRef"
    - from: "src/ui/RepoBrowser.tsx onImport finally"
      to: "importGenRef"
      via: "setBusy(false) only when importGenRef.current still equals the Import token"
      pattern: "importGenRef"
    - from: "src/ui/RepoBrowser.tsx onFileClick"
      to: "onPlanned"
      via: "call onPlanned only when clickGenRef.current still equals the click token"
      pattern: "onPlanned"
---

<objective>
Close the Phase 08 last-wins gap: every tree-file click, including blocked
and commit entries, bumps click generation before setStatus, and Import
busy uses a separate counter so a click cannot leave Import disabled.

Purpose: 08-04 last-wins only held for two loadable clicks. Blocked and
commit return before ++tokenRef, so a slower in-flight fetchGithubBlob
still matches, overwrites COPY.blocked, and fires onPlanned. Import and
clicks share one counter, so a click during Import can skip setBusy(false).

Output: RepoBrowser.tsx clickGenRef + importGenRef; RepoBrowser.test.tsx
covers loadable-then-blocked, loadable-then-commit, and Import isolation.

Root cause: incomplete last-wins (execution followed 08-04 sharing tokenRef
and bumping it only on the loadable path). Not a parse/wasm/ingest miss.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-parse-dependency-units/08-VERIFICATION.md
@.planning/phases/08-parse-dependency-units/08-04-PLAN.md
@.planning/phases/08-parse-dependency-units/08-04-SUMMARY.md
@.planning/phases/08-parse-dependency-units/08-PATTERNS.md
@src/ui/RepoBrowser.tsx
@src/ui/RepoBrowser.test.tsx
</context>

## Artifacts this phase produces

This plan owns (creates or changes):

- `clickGenRef` — `useRef(0)` in `RepoBrowser`; click-generation counter for `onFileClick` last-wins of status and `onPlanned`
- `importGenRef` — `useRef(0)` in `RepoBrowser`; Import-generation counter for `setBusy(true|false)` only
- `onFileClick` — increments `clickGenRef` for every `FileNode` click (loadable, blocked, commit) before any `setStatus`; never increments `importGenRef`
- `onImport` — increments `importGenRef` for busy last-wins; also increments `clickGenRef` at start so an in-flight loadable cannot land after Import began; `finally` calls `setBusy(false)` only when `importGenRef.current` still equals that Import token
- `src/ui/RepoBrowser.tsx` — replace shared `tokenRef` with the two refs above
- `src/ui/RepoBrowser.test.tsx` — keep existing loadable-vs-loadable last-wins; add:
  - `last-wins overlapping clicks: slow first loadable cannot overwrite a later blocked README click`
  - `last-wins overlapping clicks: slow first loadable cannot overwrite a later commit mod.ts click`
  - `file click during Import still re-enables Import when the tree fetch resolves`

Do not add files. Do not change `src/parse/wasm.ts`, `src/parse/plan.ts`, `src/github/client.ts`, `src/ingestion/github.ts`, or `src/ui/App.tsx`.

<assumption_delta_decision>
noun: syntactic unit (FilePlan.units)
decision: no-change
rationale: whole-file fallback already exists as a labeled variant; this plan only fixes click-generation last-wins.
</assumption_delta_decision>

<flagged_assumptions>
Specless edge-probe rows not in this plan's task scope. FILE-01/PLAN-03 concurrency are the covered truths above. Remaining rows stay flagged (already verified in 08-VERIFICATION.md, or unresolved unclassified — do not invent work):

- FILE-01 boundary (min/max/threshold ±1): already verified — JSON size 100001 vs 100000, node.size greater than MAX_BYTES skips fetchGithubBlob (VERIFICATION truths 12, 31).
- FILE-01 precision (rounding/tie-break): already verified — integer byteLength vs MAX_BYTES; no rounding contract to add.
- FILE-02 unclassified: unresolved — no extra sourceRef taxonomy; locked shape remains owner/repo:path; History github vs Pasted snippet stays a human backstop in 08-VERIFICATION.md.
- PLAN-01 adjacency (equal/touching ranges): already verified — units come from planUnits; RepoBrowser does not re-split (VERIFICATION truth 36).
- PLAN-01 empty (empty/single/null): already verified — empty github bytes and fallbackPlan one file unit (VERIFICATION truths 5, 16).
- PLAN-01 ordering (equal-start stability): already verified — Kahn then leftover start order; kind-start-seq ids (VERIFICATION truths 4, 26).
- PLAN-01 concurrency (planUnits interrupted): already verified — plan.ts is pure and synchronous with zero WASM imports (VERIFICATION truth 27). Not this UI click race.
- PLAN-02 unclassified: unresolved — do not invent Phase 9 trainer typing; this phase still only holds FilePlan.
</flagged_assumptions>

<canon_referral>
- XSS via blob text is canon — already T-08-03 in 08-04; this plan does not add a DOM sink.
- Click starting the trainer is canon — already T-08-08 in 08-04; do not wire handleLoad.
</canon_referral>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing last-wins tests for blocked, commit, and Import isolation</name>
  <files>src/ui/RepoBrowser.test.tsx</files>
  <read_first>
    - .planning/phases/08-parse-dependency-units/08-VERIFICATION.md (gap #30, missing items)
    - src/ui/RepoBrowser.tsx (onFileClick lines 119-186; onImport finally setBusy)
    - src/ui/RepoBrowser.test.tsx (existing last-wins overlapping clicks: slow first sha cannot overwrite the second; disables Import while busy; blocks commit entries even when the path looks loadable)
  </read_first>
  <behavior>
    - Slow App.tsx fetchGithubBlob still pending, then README.md click: status is COPY.blocked; onPlanned is not called after the blocked click; resolving the hung blob does not change status and does not call onPlanned
    - Slow App.tsx fetchGithubBlob still pending, then mod.ts click: status is COPY.blocked; resolving the hung blob does not call onPlanned
    - After a successful Import showing the tree, a second Import hangs on fetchRepoTree; clicking README.md while the submit button shows Importing… then resolving the hung tree leaves submit.disabled false and submit text Import
  </behavior>
  <action>
    Extend src/ui/RepoBrowser.test.tsx only. Do not edit RepoBrowser.tsx in this task.

    Reuse the hanging-promise pattern from last-wins overlapping clicks: slow first sha cannot overwrite the second: fetchGithubBlob.mockReturnValueOnce(first) where first is an unresolved Promise, importValue o/r, click App.tsx, then click the second file, then resolveFirst(blobResult('def')).

    Case blocked: second file is README.md. Assert statusRegion text equals COPY.blocked, fetchGithubBlob called once (the loadable click), onPlanned not called after README.md, and those assertions still hold after resolveFirst.

    Case commit: second file is mod.ts (NESTED_ENTRIES vendor/mod.ts commit). Same assertions as blocked. fetchGithubBlob still not called for the commit click.

    Case Import isolation: importValue o/r first so the tree is visible. Then fetchRepoTree.mockReturnValue a new hanging Promise. Submit the form again (Importing…). Click README.md while busy. Resolve the hanging tree with treeResult(). Assert button[type=submit] disabled is false and textContent is Import.

    Keep the existing loadable-vs-loadable last-wins test. Do not Language.load. Do not add wasm or github client tests.
  </action>
  <verify>
    <automated>grep -c "slow first loadable cannot overwrite a later blocked README click" src/ui/RepoBrowser.test.tsx; grep -c "slow first loadable cannot overwrite a later commit mod.ts click" src/ui/RepoBrowser.test.tsx; grep -c "file click during Import still re-enables Import when the tree fetch resolves" src/ui/RepoBrowser.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/ui/RepoBrowser.test.tsx contains the exact title last-wins overlapping clicks: slow first loadable cannot overwrite a later blocked README click
    - src/ui/RepoBrowser.test.tsx contains the exact title last-wins overlapping clicks: slow first loadable cannot overwrite a later commit mod.ts click
    - src/ui/RepoBrowser.test.tsx contains the exact title file click during Import still re-enables Import when the tree fetch resolves
    - src/ui/RepoBrowser.tsx is unmodified in this task
    - The three new tests fail against current onFileClick (blocked/commit return before generation bump; shared tokenRef skips setBusy(false)) until Task 2
  </acceptance_criteria>
  <done>Gap races are encoded as failing UI tests; production click path unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Split clickGenRef and importGenRef; bump generation before setStatus</name>
  <files>src/ui/RepoBrowser.tsx, src/ui/RepoBrowser.test.tsx</files>
  <read_first>
    - src/ui/RepoBrowser.tsx
    - src/ui/RepoBrowser.test.tsx
    - .planning/phases/08-parse-dependency-units/08-VERIFICATION.md (gap missing items)
  </read_first>
  <behavior>
    - Every onFileClick, including node.entryType commit and !isLoadablePath, does const token = ++clickGenRef.current before the first setStatus
    - Blocked/commit path still sets COPY.blocked, does not call fetchGithubBlob, and returns without touching importGenRef
    - Loadable path still gates MAX_BYTES, fetchGithubBlob, fromGithubBlob, ensureParser/parseSource/planUnits, and onPlanned only when clickGenRef.current === token
    - onImport uses const importToken = ++importGenRef.current for setBusy; finally setBusy(false) only if importGenRef.current === importToken
    - onImport also does ++clickGenRef.current at start so a slower in-flight loadable cannot overwrite Import status or fire stale onPlanned
    - Existing loadable-vs-loadable last-wins, blocked README, commit mod.ts, and Import overlapping tests stay green
  </behavior>
  <action>
    In src/ui/RepoBrowser.tsx replace the single tokenRef useRef with clickGenRef and importGenRef, both starting at 0.

    onFileClick: first statement is const token = ++clickGenRef.current. Then the commit / !isLoadablePath branch may setStatus COPY.blocked and return. Then setStatus COPY.loading. Keep the MAX_BYTES, importedRef, fetchGithubBlob, fromGithubBlob, ensureParser, parseSource, planUnits, fallbackPlan, onPlanned, and typed catch mapping. Every later guard that today reads tokenRef.current !== token must read clickGenRef.current !== token. onFileClick must not increment importGenRef.

    onImport: const importToken = ++importGenRef.current and ++clickGenRef.current before setBusy(true) and setStatus(null). Tree/caption/status last-wins and catch returns compare importGenRef.current to importToken (not clickGenRef). finally: if importGenRef.current === importToken then setBusy(false). A file click must not be able to skip that setBusy(false).

    Do not change COPY strings. Do not add innerHTML. Do not call handleLoad. Do not prefetch blobs on Import. Do not edit wasm, plan, github client, or App.

    Task 1 tests must now pass. Keep last-wins overlapping clicks: slow first sha cannot overwrite the second.
  </action>
  <verify>
    <automated>pnpm exec vitest run --project ui src/ui/RepoBrowser.test.tsx && pnpm exec tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - pnpm exec vitest run --project ui src/ui/RepoBrowser.test.tsx exits 0 including the three Task 1 titles and the existing slow first sha last-wins case
    - pnpm exec tsc --noEmit -p tsconfig.json exits 0
    - grep clickGenRef src/ui/RepoBrowser.tsx matches at least once
    - grep importGenRef src/ui/RepoBrowser.tsx matches at least once
    - onFileClick increments clickGenRef before any setStatus for loadable, blocked, and commit
    - onImport finally setBusy(false) is gated on importGenRef, not clickGenRef
  </acceptance_criteria>
  <done>Blocked/commit clicks last-win against in-flight loadables; a file click cannot stick Import in disabled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tree-file click → App onPlanned | Untrusted GitHub blob becomes FilePlan in App state; a stale loadable must not land after the user clicked blocked/commit |
| Import busy flag → submit button | Click generation must not cross this boundary or Import stays disabled |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-05-01 | Tampering | onFileClick → onPlanned | high | mitigate | Increment clickGenRef before setStatus on every file click including blocked/commit; skip onPlanned when clickGenRef.current !== token so a slower fetchGithubBlob cannot replace COPY.blocked with a FilePlan |
| T-08-05-02 | Tampering | onImport vs in-flight click | medium | mitigate | onImport increments clickGenRef at start so a stale loadable cannot overwrite Import status or App FilePlan after a new Import began |
| T-08-05-03 | Denial of service | onImport finally setBusy | medium | mitigate | Gate setBusy(false) on importGenRef only; onFileClick never increments importGenRef |
| T-08-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages |

ASVS L1: high threats on the click→App boundary are mitigated. Medium Import-disable is mitigated because it is the failed verification truth, not accepted residual risk.
</threat_model>

<verification>
- pnpm exec vitest run --project ui src/ui/RepoBrowser.test.tsx
- pnpm exec tsc --noEmit -p tsconfig.json
- grep clickGenRef src/ui/RepoBrowser.tsx
- grep importGenRef src/ui/RepoBrowser.tsx
- Do not re-run wasm Language.load; do not regenerate COVERAGE.md
</verification>

<success_criteria>
- A slower first loadable click cannot overwrite a faster blocked or commit click's COPY.blocked status or fire onPlanned
- A file click during Import cannot leave the Import submit button disabled after fetchRepoTree resolves
- FILE-01 blob ingest and PLAN-03 fallback copy are unchanged; this plan only fixes click-generation last-wins
</success_criteria>

<output>
Create `.planning/phases/08-parse-dependency-units/08-05-SUMMARY.md` when done
</output>

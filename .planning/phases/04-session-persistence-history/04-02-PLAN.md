---
phase: 04-session-persistence-history
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/ui/HistoryView.tsx
  - src/ui/HistoryView.test.tsx
  - src/ui/history-metrics.ts
  - src/ui/history-metrics.test.ts
  - src/ui/relative-time.ts
  - src/ui/relative-time.test.ts
  - src/ui/App.tsx
  - src/ui/App.test.tsx
  - src/index.css
autonomous: true
requirements: [PERS-02]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # --- Goal-backward (PERS-02) ---
    - "The user can open a History view (from the header toggle) that lists past sessions newest-first, each row showing at minimum date, WPM and accuracy (PERS-02)"
    - "Each history row also shows a source label, a language tag, the exercise length in characters, and a slowest-key chip — derived from exercise + the cached metricsSnapshot (D-11)"
    - "The date on each row is a relative string ('2h ago', '3d ago', 'just now') from startedAt, with the full absolute timestamp available on hover via the title attribute (D-12)"
    - "History rows render the cached metricsSnapshot directly; resolveMetrics recomputes from the raw log only when metricsSnapshot.schemaVersion !== METRICS_SCHEMA_VERSION (dormant in Phase 4 — always returns the cache) (D-05)"
    - "resolveMetrics passes s.completedAtTMs (event.timeStamp domain) as the recompute 'now' argument — never s.startedAt (wall clock) (RESEARCH Pitfall 2)"
    # --- Resolved edges: PERS-02 ---
    - "History rows render in startedAt-descending order; when two rows share an identical startedAt millisecond they are ordered by ++id descending, and the ordering is stable (PERS-02 / adjacency + ordering edges)"
    - "useLiveQuery undefined renders the loading line ('Loading history…'); [] renders the empty-state copy; the two are never conflated, and <h2>History</h2> renders above the empty state (PERS-02 / empty edge, RESEARCH Pitfall 5)"
    - "Opening the History view twice renders the same list in the same order — the live query is a pure read with no write side effect (PERS-02 / idempotency edge)"
    - "A live-query refresh triggered by a new write diffs the new row into the list without a loading flash and without reordering the existing rows (PERS-02 / concurrency edge)"
    - "The exercise-length field is a Unicode code-point count via Array.from(exercise.text).length, not UTF-16 string .length (D-11, project Unicode rule, RESEARCH Open Question 5)"
    # --- Resolved UI-SPEC considerations owned by this plan ---
    - "The active toggle option carries the active visual treatment (--color-surface fill, weight 600, --color-text); the inactive option is transparent-background, weight 400, --color-text-muted — no accent color, no new token (UI-SPEC H1, Color)"
    - "Switching to History and back preserves in-progress trainer state: typed text, caret index, per-character coloring, the textarea value, and the module-level capture buffer are all intact; no confirm dialog, no progress lost (D-08, UI-SPEC H8)"
    - "Because the trainer textarea is display:none while History is showing it cannot receive keystrokes, so a completion (and its persist write) cannot occur while History is visible; there is no 'row appears while I'm watching' animation to design (D-10, UI-SPEC H4)"
    - "Finishing an exercise does nothing to the History view: the app stays on the results screen, there is no auto-navigation, and the new row is simply present (no flash, highlight, or scroll-into-view) the next time History is opened; if History is already open shortly after completion the live query surfaces the new row once the fire-and-forget write resolves, with no manual reload (D-09, D-10, UI-SPEC)"
    - "History rows are inert: no onClick, no href, no tabindex, not focusable, no pointer cursor, no hover elevation or affordance implying interactivity; the only hover behaviour is the native title tooltip on the relative-date span (D-13, UI-SPEC H6)"
    - "A history row whose metricsSnapshot.slowest5 is empty (short exercise) renders every other field normally and simply omits the slowest-key chip — a partial metrics snapshot is a valid, expected row shape, never a row-level error (UI-SPEC H6)"
    - "A slowest-key chip whose character is a space or newline renders '·' / '↵' via glyphFor (reused from src/trainer/state.ts, exactly as ResultsView) — never a blank, invisible, or literal-whitespace chip (UI-SPEC H6)"
    - "The source label is never blank: the exercise.sourceRef filename for uploads that have one, 'Uploaded file' for uploads without one, 'Pasted snippet' for pasted corpus (D-11, UI-SPEC H6)"
    # --- Resolved UI-SPEC considerations: backstop (verifier abstains -> human_needed if unconfirmed) ---
    - statement: "A long source-label filename or an unexpectedly long exercise.language string wraps or ellipsizes within .history-row and never causes horizontal overflow or a horizontal page scrollbar"
      verification: backstop
    - statement: "With hundreds of stored sessions (D-18 keep-forever), the History <ol> renders as a plain non-virtualized list that scrolls within normal page flow without layout breakage; render performance is acceptable at single-user daily-use scale"
      verification: backstop
    - statement: "If a stored session's metrics must be recomputed on read (schemaVersion mismatch — dormant until Phase 5) and the recompute hits a zero-elapsed-time edge, the row's WPM renders the engine-guarded 0 wpm, never NaN wpm or Infinity wpm"
      verification: backstop
    - statement: "The save-failure notice appears/disappears using only the existing .banner 150ms opacity transition (already wrapped in @media (prefers-reduced-motion: no-preference)); its removal contracts the page upward with no layout shift to the results panel or trainer surface above it"
      verification: backstop
    - statement: "Toggling Trainer -> History -> Trainer after typing a few characters preserves the exact caret index and per-character coloring in the trainer surface (the display:none hide-not-unmount contract, D-08)"
      verification: backstop
    # --- Flagged planner assumption (UI-SPEC 'unresolved' row) ---
    - "PLANNER ASSUMPTION (D-08 / RESEARCH Open Question 1 / Assumption A1): the display:none hide-not-unmount approach is believed sufficient to preserve in-progress capture state, but happy-dom cannot verify uncontrolled-textarea selection/IME behaviour after a visibility toggle. Task 3 adds a Chromium-first real-browser <human-check>; if hiding proves insufficient, the fallback is lifting the capture buffer into a React ref that survives a CaptureSurface remount and allowing the trainer to unmount."
  prohibitions:
    - "MUST NOT make history rows interactive in Phase 4 — no click target, no expansion, no chevron, no hover-reveal button, no 'view details' affordance, and no delete / clear-history control; per-session drill-down stays deferred to ANLY-07 (D-13)"
    - "MUST NOT add a retention cap, prune policy, virtualization, pagination, or storage limit to the History list — all sessions and their full raw logs are kept forever for v1.1; RESEARCH is explicit that the planner should not add a cap 'to be safe' (D-18)"
    - "MUST NOT apply performance-threshold color-coding (good/green, bad/red), personal-best highlighting, date-group separators, a row count header, sort/filter/search controls, or any gamified / count-up framing to history rows or the new-row appearance (locked UI-SPEC rule, REQUIREMENTS Out of Scope; D-10 forbids flash/highlight)"
    - "MUST NOT let resolveMetrics or the row render round any number before ranking or before the display layer, and MUST NOT pass Session.startedAt (wall clock) into computeSessionMetrics as 'now' (RESEARCH Pitfall 2, metrics.ts clock-domain warning)"
  artifacts:
    - path: "src/ui/relative-time.ts"
      provides: "relativeTime(startedAt, now?) — pure Intl.RelativeTimeFormat helper, no new dependency"
      exports: ["relativeTime"]
    - path: "src/ui/history-metrics.ts"
      provides: "resolveMetrics(s: StoredSession): MetricsResult — recompute-if-stale guard, pure, ui/ not persistence/"
      exports: ["resolveMetrics"]
    - path: "src/ui/HistoryView.tsx"
      provides: "MODIFIED — HistoryRow sub-component with all D-11/D-12 fields; three-way loading/empty/rows branch"
      exports: ["HistoryView"]
    - path: "src/index.css"
      provides: "MODIFIED — .history-list / .history-row / .history-row-primary / .history-row-meta / .history-empty / .history-loading rules; nav active-state; chip reuse of .key-chip"
      contains: ".history-row"
    - path: "src/ui/App.tsx"
      provides: "MODIFIED — nav active-state class hooks (aria-current already set in 04-01); D-08 hide-not-unmount contract finalised"
  key_links:
    - from: "src/ui/HistoryView.tsx"
      to: "src/ui/history-metrics.ts"
      via: "const m = resolveMetrics(session) — per row"
      pattern: "resolveMetrics\\("
    - from: "src/ui/history-metrics.ts"
      to: "src/metrics/metrics.ts"
      via: "computeSessionMetrics(s.exercise.text, s.charLog, s.markers, s.completedAtTMs) on schemaVersion mismatch"
      pattern: "computeSessionMetrics\\("
    - from: "src/ui/HistoryView.tsx"
      to: "src/trainer/state.ts"
      via: "glyphFor(entry.char) for the slowest-key chip — reused, not reimplemented"
      pattern: "glyphFor\\("
    - from: "src/ui/HistoryView.tsx"
      to: "src/ui/relative-time.ts"
      via: "relativeTime(session.startedAt) with title={new Date(session.startedAt).toLocaleString()}"
      pattern: "relativeTime\\("
---

<objective>
Expand the minimal History list from 04-01 into the full PERS-02 browsing experience: every
row shows date (relative + absolute-on-hover), WPM, accuracy, source label, language tag,
exercise length in code points, and a slowest-key chip; rows are inert, newest-first, with
explicit loading and empty states. Add the `resolveMetrics` recompute-if-stale guard (D-05,
dormant until Phase 5) and a dependency-free `relativeTime` helper. Style the list per
04-UI-SPEC (bordered secondary-surface cards, no accent, no threshold coloring) and harden +
verify the D-08 hide-not-unmount view-switch contract.

Purpose: satisfies PERS-02 (view a list of past sessions — date, WPM, accuracy — newest
first) at full fidelity, and closes RESEARCH Open Question 1 (D-08 capture-state preservation)
with a real-browser check plus a documented fallback.

Output: `src/ui/relative-time.ts`, `src/ui/history-metrics.ts`, a rewritten
`src/ui/HistoryView.tsx` (with `HistoryRow`), `.history-*` + nav CSS in `src/index.css`,
tests, and a D-08 regression test + human-check.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-session-persistence-history/04-CONTEXT.md
@.planning/phases/04-session-persistence-history/04-RESEARCH.md
@.planning/phases/04-session-persistence-history/04-PATTERNS.md
@.planning/phases/04-session-persistence-history/04-UI-SPEC.md
@.planning/phases/04-session-persistence-history/04-01-PLAN.md
@src/ui/ResultsView.tsx
@src/trainer/state.ts
@src/metrics/metrics.ts
@src/ingestion/types.ts
@src/index.css
</context>

<artifacts_this_phase_produces>
Symbols introduced by THIS plan (rest are in 04-01):

- `relativeTime(startedAt: number, now?: number): string` (`src/ui/relative-time.ts`)
- `resolveMetrics(s: StoredSession): MetricsResult` (`src/ui/history-metrics.ts`)
- `HistoryRow` sub-component (`src/ui/HistoryView.tsx`)
- `.history-list`, `.history-row`, `.history-row-primary`, `.history-row-meta`, `.history-empty`, `.history-loading`, nav active-state rules (`src/index.css`)
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: One fully-populated history row — end-to-end from a stored session through every display helper</name>
  <precondition>src/persistence/repository.ts exports saveSession and listNewestFirst, and src/persistence/types.ts exports StoredSession with fields exercise, charLog, markers, completedAtTMs, metricsSnapshot and STORED_SESSION_SCHEMA_VERSION (delivered by plan 04-01).</precondition>
  <files>src/ui/relative-time.ts, src/ui/relative-time.test.ts, src/ui/history-metrics.ts, src/ui/history-metrics.test.ts, src/ui/HistoryView.tsx, src/ui/HistoryView.test.tsx</files>
  <read_first>
    - src/ui/HistoryView.tsx (the file being modified — the minimal version from 04-01)
    - src/ui/ResultsView.tsx (near-exact structural twin: role="status", glyphFor chip, Math.round at render, empty-vs-populated branch)
    - src/trainer/state.ts (glyphFor — ' ' -> '·', '\n' -> '↵'; do not reimplement)
    - src/metrics/metrics.ts (MetricsResult, SlowestKeyEntry, METRICS_SCHEMA_VERSION, computeSessionMetrics signature, clock-domain warning)
    - src/persistence/types.ts (StoredSession, completedAtTMs semantics — from 04-01)
    - src/ingestion/types.ts (Exercise — sourceType 'paste' | 'upload', sourceRef?)
    - src/ui/CaptureSurface.test.tsx (createRoot + act ui-project harness)
    - .planning/phases/04-session-persistence-history/04-PATTERNS.md (sections: src/ui/HistoryView.tsx + HistoryRow, src/ui/history-metrics.ts, Test files)
    - .planning/phases/04-session-persistence-history/04-RESEARCH.md (sections: Data Model / Field decisions / Recompute-on-stale-schema, Code Examples: HistoryRow field derivation + Relative time)
    - .planning/phases/04-session-persistence-history/04-UI-SPEC.md (Copywriting Contract supporting-copy table; UI Considerations H5/H6)
  </read_first>
  <behavior>
    - relativeTime(now - 2*3600_000) === '2h ago'; relativeTime(now - 3*86_400_000) === '3d ago'; relativeTime(now) is a "just now" / "now"-class string; a future timestamp still returns a sane string.
    - resolveMetrics returns the exact metricsSnapshot object reference when metricsSnapshot.schemaVersion === METRICS_SCHEMA_VERSION.
    - A HistoryRow rendered from a StoredSession shows: relative date (with a title attr holding the absolute timestamp), rounded WPM + ' wpm', rounded accuracy percent + '%', a source label, the language string, "<n> chars" where n = code-point count, and a slowest-key chip iff slowest5[0] is defined.
    - A StoredSession whose slowest5 is [] renders every other field and omits the chip.
    - A slowest char of ' ' renders '·' via glyphFor.
    - Rows sort newest-first; equal startedAt sorts by id desc.
  </behavior>
  <action>
    Create `src/ui/relative-time.ts` — a pure module: `export function relativeTime(startedAt:
    number, now = Date.now()): string`. Use a single module-level
    `new Intl.RelativeTimeFormat(undefined, { numeric: 'auto' })` and a descending unit ladder
    (year 31_536_000_000, month 2_592_000_000, day 86_400_000, hour 3_600_000, minute 60_000,
    second 1000 ms); pick the largest unit whose absolute magnitude the difference reaches and
    format `Math.round(diff / unitMs)` with that unit; below one second return a "just now"-class
    string. No new dependency (discretion — `Intl.RelativeTimeFormat` is a browser built-in).

    Create `src/ui/history-metrics.ts` — a pure module: `export function resolveMetrics(s:
    StoredSession): MetricsResult`. Import `computeSessionMetrics` and `METRICS_SCHEMA_VERSION` from
    `../metrics/metrics`, `type StoredSession` from `../persistence/types`. Return
    `s.metricsSnapshot` directly when `s.metricsSnapshot.schemaVersion === METRICS_SCHEMA_VERSION`;
    otherwise return `computeSessionMetrics(s.exercise.text, s.charLog, s.markers, s.completedAtTMs)`
    — the fourth argument is `s.completedAtTMs` (event.timeStamp domain), never `s.startedAt` (wall
    clock — RESEARCH Pitfall 2, metrics.ts clock-domain warning). This module lives in `ui/`, never
    imports `../persistence/db` or `dexie`, and never rounds. Add a comment: in Phase 4
    `METRICS_SCHEMA_VERSION === 1` and nothing bumps it, so this always returns the cache — the
    recompute branch is dormant until Phase 5.

    Rewrite the row in `src/ui/HistoryView.tsx` as a `HistoryRow({ session }: { session:
    StoredSession })` sub-component. `const m = resolveMetrics(session)`. Render two lines inside an
    inert `<li className="history-row">` (no onClick / href / tabIndex / interactive role):
    - primary line (`.history-row-primary`): `<span className="..." title={new
      Date(session.startedAt).toLocaleString()}>{relativeTime(session.startedAt)}</span>` — the
      title tooltip is the only hover affordance; then `{Math.round(m.wpm)}` + ` wpm` (unit in a
      muted label span, matching `ResultsView`'s `.results-stat-label`); then
      `{Math.round(m.accuracy * 100)}` + `%`.
    - meta line (`.history-row-meta`): the source label — `session.exercise.sourceType === 'upload'
      ? (session.exercise.sourceRef ?? 'Uploaded file') : 'Pasted snippet'` (never blank); the
      `session.exercise.language` string inside a `.key-chip`-style chip; `{Array.from(
      session.exercise.text).length}` + ` chars` (code-point count — UI-SPEC decided characters,
      not lines); and, only when `m.slowest5[0]` is defined (guard for `noUncheckedIndexedAccess`),
      a slowest-key chip rendering `entry.char === ' ' || entry.char === '\n' ? glyphFor(entry.char)
      : entry.char` (import `glyphFor` from `../trainer/state`, exactly as `ResultsView`). Omit the
      chip entirely — no placeholder — when `slowest5` is empty.
    Keep the three-way `useLiveQuery` branch from 04-01 (`undefined` -> loading line; `[]` -> the
    empty-state copy; else the list) and keep `<h2>History</h2>` rendered above the empty state.
    Wrap the rows in `<ol className="history-list">`. Round only at render.

    Write `src/ui/relative-time.test.ts` (unit project): a golden `interface Case` + `it.each` table
    over the unit ladder (seconds/minutes/hours/days/months/years, past and one future case).
    Write `src/ui/history-metrics.test.ts` (unit project): matching `schemaVersion` returns the
    cached snapshot by reference; (documented) a mismatched `schemaVersion` would route to
    `computeSessionMetrics` with `completedAtTMs` as `now` — assert the call arguments via a spy or
    a fixture whose recompute is deterministic.
    Write `src/ui/HistoryView.test.tsx` (ui project, fake-indexeddb via the `ui` setupFile from
    04-01; `createRoot` + `act`): seed StoredSession fixtures via `saveSession`, render
    `<HistoryView />`, and assert — (a) every row shows all seven fields; (b) newest-first order;
    (c) two equal-`startedAt` fixtures order by id descending; (d) a fixture with empty `slowest5`
    omits the chip and still renders every other field; (e) a fixture whose slowest char is `' '`
    renders `·`; (f) before the query resolves the loading line shows (not the empty state) and an
    empty DB shows the empty-state copy with `<h2>History</h2>` present.
  </action>
  <verify>
    <automated>pnpm test && pnpm typecheck && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/relative-time.ts` and `src/ui/history-metrics.ts` exist; neither imports `dexie` or `../persistence/db` (`grep -L` confirms); neither calls `Math.round` / `Math.floor` on a metric value (rounding is display-layer only).
    - In `src/ui/history-metrics.ts` the `computeSessionMetrics(...)` call's fourth argument is `s.completedAtTMs`.
    - `src/ui/HistoryView.tsx` imports `glyphFor` from `../trainer/state` and `resolveMetrics` from `./history-metrics`; it does not import `dexie` or `../persistence/db`.
    - `HistoryRow` renders the exercise length via `Array.from(session.exercise.text).length`.
    - `src/ui/HistoryView.test.tsx` asserts (a)-(f) above and passes under the `ui` project.
    - `src/ui/relative-time.test.ts` and `src/ui/history-metrics.test.ts` pass under the `unit` project.
    - `pnpm test && pnpm typecheck && pnpm lint` exit 0.
  </acceptance_criteria>
  <done>
    A history row rendered from a real `StoredSession` shows all seven D-11/D-12 fields, derived
    end-to-end through `resolveMetrics`, `relativeTime`, and `glyphFor`; the list is newest-first
    with a deterministic tiebreak; empty and loading states are distinct; all gates green; committed.
  </done>
</task>

<task type="auto">
  <name>Task 2: History list + view-toggle styling (04-UI-SPEC)</name>
  <files>src/index.css, src/ui/App.tsx, src/ui/App.test.tsx</files>
  <read_first>
    - src/index.css (the file being modified — existing `.banner`, `.preview`, `.results-panel`, `.key-chip`, `.text-muted`, `.text-label`, `--space-*` / `--color-*` tokens, motion `@media` block, `button` / `:focus-visible` rules)
    - src/ui/App.tsx (header + nav from 04-01; class hooks needed on the toggle buttons)
    - src/ui/ResultsView.tsx + its CSS (`.results-stat` / `.results-stat-label` — the visual register history numbers should echo but at Body not Display weight)
    - .planning/phases/04-session-persistence-history/04-UI-SPEC.md (Spacing Scale: Layout - History view / header view toggle / spacing exceptions; Typography; Color; "Why the active view toggle is NOT accent-colored")
    - .planning/phases/04-session-persistence-history/04-PATTERNS.md (section: role="status" prop-driven panels; Display-layer rounding)
  </read_first>
  <behavior>
    - Exactly one nav toggle button carries aria-current="page" at any time; it flips when setView runs.
    - .history-row renders as a bordered secondary-surface card; .history-list is a gapped grid <ol> with no list markers.
    - .history-empty / .history-loading are muted paragraphs with no card/border.
    - History WPM / accuracy values use var(--color-text) regardless of magnitude (no threshold coloring).
  </behavior>
  <action>
    Add to `src/index.css` (reuse existing tokens; introduce no new custom property):
    - `.history-list` (the `<ol>`): `display: grid; gap: var(--space-sm); list-style: none; margin:
      0; padding: 0`.
    - `.history-row` (the `<li>`): `background: var(--color-surface); border: 1px solid
      var(--color-border); border-radius: 6px; padding: var(--space-md); display: grid; gap:
      var(--space-xs)` — same card treatment as `.preview` / `.results-panel`, reused not
      re-invented.
    - `.history-row-primary`: `display: flex; flex-wrap: wrap; column-gap: var(--space-md);
      row-gap: var(--space-xs); align-items: baseline`. Relative-date span and the WPM / accuracy
      values are weight 600, `var(--color-text)`; the unit labels are the Label role, muted
      (mirror `.results-stat-label`).
    - `.history-row-meta`: `display: flex; flex-wrap: wrap; align-items: center; column-gap:
      var(--space-md); row-gap: var(--space-xs)`. Source label + length are Body role, muted; the
      language chip and slowest-key chip reuse `.key-chip`'s padding + 1px border, with chip
      background `var(--color-bg)` so a bordered chip stays legible on the card surface.
    - `.history-empty`, `.history-loading`: a single muted `<p>`, `margin: 0; padding-block:
      var(--space-md)`; no card, no border.
    - Header: make the existing `<header>` a flex row — `display: flex; align-items: baseline;
      justify-content: space-between; gap: var(--space-md); flex-wrap: wrap`. The toggle `<nav>` is
      `display: inline-flex; gap: var(--space-xs)`.
    - View-toggle active state: a `<button>` with `aria-current="page"` gets `background:
      var(--color-surface); font-weight: 600; color: var(--color-text)` (target it via the
      `[aria-current="page"]` attribute selector, or an `.is-active` class set in `App.tsx`);
      the inactive toggle button is `background: transparent; font-weight: 400; color:
      var(--color-text-muted)`. Do NOT use the accent color or a new token for the active state
      (UI-SPEC: the accent reserved list is exhaustive; weight + surface fill carry the selected
      state).
    No performance-threshold color-coding anywhere: history WPM / accuracy render in
    `var(--color-text)` regardless of magnitude; no per-row highlight, no new-row flash.

    In `src/ui/App.tsx`: only add the class hook(s) the CSS selector needs on the nav buttons (if
    using `[aria-current="page"]` no JSX change is required beyond what 04-01 set). Do not change
    the toggle behaviour.

    Extend `src/ui/App.test.tsx` (ui project): assert that after render exactly one nav toggle
    button has `aria-current="page"`, that it is `History` after `setView('history')` and `Trainer`
    after `setView('trainer')`, and that the other button never simultaneously carries it.
  </action>
  <verify>
    <automated>pnpm test && pnpm typecheck && pnpm lint && pnpm build</automated>
    <human-check>Run `pnpm dev` and open the app in Chromium (COOP/COEP dev server). Complete two short exercises, open History: rows render as bordered cards, newest-first, each showing relative date + WPM + accuracy + source + language chip + "N chars" + slowest-key chip. The active toggle button is surface-filled and bold (NOT green/accent). No WPM or accuracy value is colored green or red. The empty state (fresh profile / cleared IndexedDB) reads "No sessions yet — finish a typing exercise and it'll show up here." under an always-present "History" heading.</human-check>
  </verify>
  <acceptance_criteria>
    - `src/index.css` contains `.history-list`, `.history-row`, `.history-row-primary`, `.history-row-meta`, `.history-empty`, `.history-loading` selectors and a view-toggle active-state rule keyed on `[aria-current="page"]` or `.is-active`.
    - No new `--*` custom property is declared by this task; history-row WPM / accuracy values resolve to `var(--color-text)` (no `--color-accent` / `--color-destructive` / green / red on a metric value).
    - `src/ui/App.test.tsx` asserts the single-active-toggle invariant across `setView` calls.
    - `pnpm test && pnpm typecheck && pnpm lint && pnpm build` exit 0.
  </acceptance_criteria>
  <done>
    The History list and header toggle match 04-UI-SPEC: bordered secondary-surface row cards,
    muted empty/loading paragraphs, an active toggle distinguished by surface fill + weight (no
    accent), zero threshold coloring; automated gates green and the human-check is queued for the
    end-of-phase batch; committed.
  </done>
</task>

<task type="auto">
  <name>Task 3: D-08 hide-not-unmount contract — hardening + verification</name>
  <files>src/ui/App.tsx, src/ui/App.test.tsx</files>
  <read_first>
    - src/ui/App.tsx (the file being modified — the loaded-exercise wrapper, the `view` state and display toggle from 04-01, `key={loadToken}` on CaptureSurface, handleLoad / handleRestart resetCapture calls)
    - src/ui/CaptureSurface.tsx (firedCompletedAtRef one-shot; getCharLog() re-read per render; the trainer re-derives everything from capture module state)
    - src/capture/capture.ts (module-level buffer / charLog / markers; resetCapture() is the ONLY clear, called only from handleLoad / handleRestart)
    - src/capture/use-capture.ts (useLayoutEffect attach/detach — stays bound while the component is mounted)
    - src/ui/CaptureSurface.test.tsx (RestartHarness pattern; trusted beforeinput helpers; nextFrame(); createRoot + act)
    - .planning/phases/04-session-persistence-history/04-RESEARCH.md (Pattern 4: "Hide, don't unmount, the trainer subtree"; Open Question 1; Assumption A1; Anti-Patterns)
    - .planning/phases/04-session-persistence-history/04-UI-SPEC.md (UI Considerations H8 + the "unresolved" row; Interaction Contract "Hide, don't unmount")
  </read_first>
  <behavior>
    - Typing a few chars, switching to History, switching back: getCharLog() length is unchanged, the rendered .trainer-rendered-layer per-char [data-status] classes are identical, and the .trainer-textarea element is the same DOM node instance (no remount).
    - A view switch never bumps loadToken and never reaches resetCapture().
    - HistoryView is a conditional mount (fine — no state to preserve); only the trainer must not unmount.
  </behavior>
  <action>
    In `src/ui/App.tsx`, finalise the D-08 contract (most of it is already wired in 04-01 — this
    task confirms and locks it): the loaded-exercise wrapper toggles its inline `display` between
    `grid` and `none` from a `view === 'trainer' ? 'grid' : 'none'` expression; `CaptureSurface`
    renders unconditionally inside that always-mounted wrapper whenever `exercise !== null`; the
    wrapper is never wrapped in a `view`-conditional expression and never uses the HTML `hidden`
    attribute; `key={loadToken}` on `CaptureSurface` is untouched; no `setView` path calls
    `resetCapture()` or bumps `loadToken`. `{view === 'history' && <HistoryView />}` stays a
    conditional mount (HistoryView holds no state worth preserving).

    Add a D-08 regression block to `src/ui/App.test.tsx` (ui project), modelled on
    `CaptureSurface.test.tsx`'s `RestartHarness`: render `<App/>`, load a short exercise, drive a
    few trusted `beforeinput` commits (including one wrong char then a delete then the right char),
    wait `nextFrame()`, and capture: `getCharLog().length`, the array of
    `.trainer-rendered-layer [data-status]` values, and a reference to the `.trainer-textarea`
    element. Then `act(() => setView('history'))` (via a test hook or by clicking the History nav
    button) and assert the wrapper `<div>` computed `display` is `none` and `HistoryView` markup is
    present. Then switch back to Trainer and assert: (a) `getCharLog().length` is unchanged;
    (b) the `.trainer-rendered-layer [data-status]` array is identical; (c) the `.trainer-textarea`
    element is the same node reference (identity check — proves no remount); (d) the caret marker
    (`.trainer-caret`) is at the same character index. Add a source-level assertion that the
    wrapper's `display` comes from a `view`-keyed ternary and that `CaptureSurface` is not inside a
    `view`-conditional render.

    Document, in the plan SUMMARY and a code comment near the toggle, the residual assumption:
    happy-dom has no layout engine and cannot verify uncontrolled-`<textarea>` selection / IME
    state across a real `display:none` toggle — the `<human-check>` below is the real
    verification, and the fallback (if it fails) is lifting the capture buffer into a React ref
    that survives a `CaptureSurface` remount and letting the trainer unmount.
  </action>
  <verify>
    <automated>pnpm test && pnpm typecheck && pnpm lint</automated>
    <human-check>In Chromium via `pnpm dev` (cross-origin-isolated dev server): load an exercise, type ~10 characters including at least one correction (wrong char -> backspace -> right char), click "History", then click "Trainer". Assert by eye: the caret sits at the exact same character position; per-character correct/incorrect coloring is identical; clicking the trainer surface still reclaims focus; and an IME composition begun before the switch (test with a CJK/pinyin IME if available) still resolves correctly after returning. If ANY of these fail, do not ship the display:none approach — switch to the ref-based capture-buffer preservation fallback (RESEARCH Open Question 1) and re-verify.</human-check>
  </verify>
  <acceptance_criteria>
    - `src/ui/App.tsx`: the loaded-exercise wrapper's `display` is set from a `view === 'trainer' ? 'grid' : 'none'` expression; `CaptureSurface` renders unconditionally inside that wrapper whenever `exercise !== null` (not inside any `view`-conditional expression); no `hidden` attribute on the wrapper; `key={loadToken}` unchanged; no `resetCapture()` or `loadToken` bump reachable from `setView`.
    - `src/ui/App.test.tsx` contains a test that types -> toggles to History -> toggles back and asserts `getCharLog()` length, the rendered per-char `[data-status]` array, and the `.trainer-textarea` element identity are all unchanged, plus the caret index is preserved.
    - The residual D-08 assumption and the ref-based fallback are recorded in the plan SUMMARY and in a code comment near the toggle.
    - `pnpm test && pnpm typecheck && pnpm lint` exit 0.
  </acceptance_criteria>
  <done>
    Switching to History mid-exercise and back is proven (in happy-dom) to keep the same
    `CaptureSurface` instance, the same capture buffer, and the same per-character state; the
    real-browser caret/IME check is queued for the end-of-phase human-verify batch with a
    documented fallback; all automated gates green; committed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IndexedDB origin store -> `HistoryView` / `HistoryRow` render | persisted `exercise.text` / `.language` / `.sourceRef` and cached `metricsSnapshot` re-enter the render path via `useLiveQuery` |
| `resolveMetrics` recompute path (dormant) | a stored raw log fed back into the pure `computeSessionMetrics` fold on a future `schemaVersion` mismatch |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering / Elevation (stored XSS) | `HistoryRow` rendering `exercise.text` / `.language` / `.sourceRef` | low | mitigate | All three rendered only as React text nodes (auto-escaped); no `dangerouslySetInnerHTML`, no HTML parsing of any stored field; the language string is placed inside a chip as a text node |
| T-04-04 | Tampering | a malformed / partial stored record breaking the row render | low | mitigate | Explicit `StoredSession` type; `m.slowest5[0]` read under `noUncheckedIndexedAccess` guard; empty `slowest5` is an expected row shape (chip omitted), never an error; `resolveMetrics` returns the engine-guarded `0 wpm` on a degenerate recompute (backstop truth) |
| T-04-06 | Denial of Service (self-inflicted) | non-virtualized `<ol>` growing unbounded (D-18 keep-forever) | low | accept | Plain list scrolling in normal page flow is acceptable at single-user daily-use scale (RESEARCH Data Model); virtualization / pagination deliberately NOT added (prohibition) — revisit only if felt |
| T-04-02 | Info Disclosure | recompute error / edge surfacing `NaN` / `Infinity` in a row | low | mitigate | `computeWpm` guards `elapsedMs <= 0 -> 0`; rounding at the display layer only; backstop truth covers the zero-elapsed recompute edge |

No `high`-severity findings — nothing blocks the phase (block threshold: `high`). This plan adds
no dependency, no network path, and no new external origin.
</threat_model>

<verification>
- `pnpm typecheck` — 0 errors (strict, `noUncheckedIndexedAccess`).
- `pnpm test` — all vitest projects green, including the new `HistoryView` / `relative-time` / `history-metrics` / `App` D-08 tests.
- `pnpm lint` — 0 warnings.
- `pnpm build` — production build succeeds.
- `src/ui/history-metrics.ts` and `src/ui/relative-time.ts` import neither `dexie` nor `../persistence/db`.
- End-of-phase human-verify batch: (Task 2) History list visual check in Chromium; (Task 3) D-08 caret/IME preservation across a real view toggle in Chromium.
</verification>

<success_criteria>
- The user opens History from the header toggle and sees past sessions newest-first, each row showing date (relative + absolute on hover), WPM, accuracy, source label, language, length in chars, and a slowest-key chip (PERS-02, D-11, D-12).
- Loading (`undefined`) and empty (`[]`) states are distinct; rows are inert; ordering is stable with an `++id` tiebreak.
- `resolveMetrics` renders the cached snapshot in Phase 4 and is wired to recompute from `completedAtTMs` on a future `schemaVersion` bump (D-05).
- The list and toggle match 04-UI-SPEC: bordered cards, no accent on the active toggle, no threshold coloring.
- Switching to History mid-exercise and back preserves in-progress trainer state (D-08), verified in happy-dom and queued for a Chromium human-check.
- All automated gates green; changes committed.
</success_criteria>

<output>
Create `.planning/phases/04-session-persistence-history/04-02-SUMMARY.md` when done.
</output>

---
phase: 09-scaffolded-trainer
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/ui/FileScaffold.tsx
  - src/ui/FileScaffold.test.tsx
  - src/ui/App.tsx
  - src/ui/App.test.tsx
autonomous: true
requirements: [SCAF-01, SCAF-02, SCAF-03, SCAF-04, SCAF-05]

must_haves:
  truths:
    - "Heading + body from Copywriting; CaptureSurface and FileScaffold are not rendered until a load or onPlanned start"
    - "Fixed sentence wraps in --column-max; no ellipsis"
    - "Source-order segments (unit + gap) paint the full Exercise.text; current segment is the CaptureSurface card"
    - "Mix of done + current + future in source order; curriculum index, not visual stack order, assigns roles"
    - "Chrome wrapper max-height: 70vh; overflow: auto; names/lines wrap; unbreakable token may horizontal-scroll"
    - "Fallback 1 / 1 still FileScaffold (one current card, no future). Many units: same segment layout, landmark n / m"
    - "Static pre and overlay use overflow-wrap: anywhere + pre-wrap; no truncation of source"
    - "N / M min-height: 1.4em is a floor; kind/name wraps below; landmark is outside the chrome scroller"
    - "Long name wraps with overflow-wrap: anywhere; no ellipsis"
    - "CaptureSurface on the code-point slice only; pending/correct/incorrect + caret unchanged from Phase 2 (D-02)"
    - "Done and future regions render canonical source slices from Exercise.text, never the typed buffer (D-03)"
    - "FileScaffold owns the three regions and the N/M landmark; CaptureSurface props stay text, onRestartRequested, onComplete (D-05)"
    - "First paint of a unit is all-pending overlay (Phase 2 initial state), not a distinct empty-copy surface"
    - "Last unit persist: all segments done static, CaptureSurface unmounted, existing ResultsView shown, Restart hidden"
    - "Clicking a TS/JS FilePlan starts typing immediately on curriculum unit 0 with no Start button (D-07, SCAF-01)"
    - "GitHub onPlanned uses startScaffold; the paste/upload load callback is never given plan.exercise (D-08, SCAF-05)"
    - "Fallback plan.fallback true still mounts FileScaffold immediately; PLAN-03 notice stays in RepoBrowser status (D-09)"
    - "A new GitHub click or paste/upload load replaces an in-progress scaffold: discard snapshots, resetCapture, remount (D-10 last-wins). Paste|GitHub tab switch does not reset (Phase 7 D-04). (SCAF-01 concurrency, SCAF-02 concurrency, SCAF-03 concurrency, SCAF-04 concurrency, SCAF-05 concurrency)"
    - "Trainer subtree hide-not-unmount is unchanged; the tree stays browseable while typing (D-11)"
    - "Completing a non-last unit snapshots capture logs, resetCapture, bumps loadToken, remounts the next slice with no interstitial (D-12, D-15, SCAF-02)"
    - "Landmark is 1-based N / M with spaces around the slash plus kind/name when present (D-13). COPY verbatim from 09-UI-SPEC.md"
    - "Last unit complete → existing ResultsView + one saveSession; Session.exercise.text is the full file; metrics use joinUnitSlices typedTarget not source-order file (D-14, SCAF-03 encoding)"
    - "Escape and Restart share one handler; scaffold in progress restarts the current unit only — do not zero unitIndex, do not drop completed snapshots, do not refresh file-level startedAt (D-16, D-17, SCAF-04)"
    - "After last-unit persist Restart is hidden; no whole-file restart control; do not arm a second saveSession (D-18, Pitfall 11 option a)"
    - "Paste/upload handleLoad clears curriculum and mounts CaptureSurface on exercise.text with no FileScaffold (D-20, SCAF-05). Empty paste is already rejected at ingest (SCAF-05 empty)"
    - "History github rows show sourceRef owner/repo:path and never Pasted snippet after a persisted github session (D-21)"
    - "Paste empty already rejected at ingest; fallback 1-unit still FileScaffold (SCAF-03 empty, SCAF-05 empty)"
    - statement: "scrollIntoView({ block: 'nearest', inline: 'nearest' }) on the current card at start and every advance; behavior: 'instant' when prefers-reduced-motion: reduce, else 'smooth'. Spy the call in tests; do not assert scrollTop (happy-dom has no layout)."
      verification: backstop
    - statement: "Done/future/gap reuse glyphFor + .ws-glyph at Body 15/1.5 with the same padding as .trainer-stack so columns do not visibly drift from the overlay. Layout-engine check is human/Chromium; unit tests assert glyph characters, not pixel alignment."
      verification: backstop
  prohibitions:
    - statement: "MUST NOT pass the planned exercise into the paste/upload load callback for GitHub"
      status: unverified
      verification: flagged
    - statement: "MUST NOT put the full file into CaptureSurface text when units.length is greater than 1"
      status: unverified
      verification: flagged
    - statement: "MUST NOT modify capture.ts"
      status: unverified
      verification: flagged
    - statement: "MUST NOT hide future units or use a cloze/overlay of a slice inside a full-file textarea"
      status: unverified
      verification: flagged
    - statement: "MUST NOT interpret blob text as HTML (no markup sink, no highlighter)"
      status: unverified
      verification: flagged
    - statement: "MUST NOT call persist onComplete until the last unit"
      status: unverified
      verification: flagged
    - statement: "MUST NOT add a whole-file restart control this phase"
      status: unverified
      verification: flagged
    - statement: "MUST NOT grow CaptureSurface props beyond text, onRestartRequested, onComplete"
      status: unverified
      verification: flagged
    - statement: "MUST NOT rebuild the HistoryView github sourceRef ternary"
      status: unverified
      verification: flagged
    - statement: "MUST NOT use accent color for the current-unit card fill or stroke"
      status: unverified
      verification: flagged
    - statement: "MUST NOT duplicate the PLAN-03 fallback notice on FileScaffold"
      status: unverified
      verification: flagged
    - statement: "MUST NOT put Phase 9, WASM, tree-sitter, parser, AST, or curriculum in user-visible copy"
      status: unverified
      verification: flagged
  artifacts:
    - path: "src/ui/FileScaffold.tsx"
      provides: "Landmark N/M + source-order chrome; mounts CaptureSurface on the current slice"
      exports: ["FileScaffold"]
    - path: "src/ui/App.tsx"
      provides: "startScaffold, curriculum, unit advance, unit restart, last-wins vs handleLoad, last-unit persist"
      exports: ["App"]
  key_links:
    - from: "src/ui/App.tsx"
      to: "src/ui/RepoBrowser.tsx"
      via: "onPlanned={startScaffold} — never the paste/upload load callback"
      pattern: "startScaffold"
    - from: "src/ui/FileScaffold.tsx"
      to: "src/scaffold/cover.ts"
      via: "coverFile(text, units, complete ? units.length : unitIndex)"
      pattern: "coverFile"
    - from: "src/ui/FileScaffold.tsx"
      to: "src/ui/CaptureSurface.tsx"
      via: "text={sliceUnit(...)} key={loadToken} — slice only"
      pattern: "sliceUnit"
    - from: "src/ui/App.tsx"
      to: "src/scaffold/flatten.ts"
      via: "last unit: snapshot then flattenSnapshots then assembleSessionFromLogs"
      pattern: "flattenSnapshots"
    - from: "src/ui/App.tsx"
      to: "src/metrics/metrics.ts"
      via: "computeSessionMetrics(joinUnitSlices(...), charLog, markers, completedAt)"
      pattern: "joinUnitSlices"
---

<objective>
FileScaffold chrome plus App curriculum: click-to-type unit 0, source-order full file, instant advance, unit-only restart, one persisted session, paste/upload still whole-file. Implements D-01 through D-18, D-20, D-21 (D-19 consumed from 09-01).

Purpose: this is the phase goal. 09-01 is unused until this plan.

Output: FileScaffold + App startScaffold / intercept complete / unit restart / last-wins / persist.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-scaffolded-trainer/09-CONTEXT.md
@.planning/phases/09-scaffolded-trainer/09-RESEARCH.md
@.planning/phases/09-scaffolded-trainer/09-PATTERNS.md
@.planning/phases/09-scaffolded-trainer/09-UI-SPEC.md
@.planning/phases/07-github-url-repo-tree/07-CONTEXT.md
@.planning/phases/08-parse-dependency-units/08-UAT.md
@.planning/phases/09-scaffolded-trainer/09-01-SUMMARY.md
@src/ui/App.tsx
@src/ui/App.test.tsx
@src/ui/CaptureSurface.tsx
@src/ui/CaptureSurface.test.tsx
@src/ui/RepoBrowser.tsx
@src/ui/HistoryView.tsx
@src/session.ts
@src/scaffold/slice.ts
@src/scaffold/cover.ts
@src/scaffold/flatten.ts
@src/metrics/metrics.ts
@src/metrics/resolve-metrics.ts
</context>

## Artifacts this phase produces

This plan owns:

- `FileScaffold` props: `text` (full file), `units` (curriculum order), `unitIndex`, `loadToken`, `complete` (boolean after last-unit persist), `onRestartRequested`, `onComplete`. COPY and KIND_LABEL verbatim from 09-UI-SPEC.md Copywriting Contract. Do not teach CaptureSurface about GitHub, units, or FilePlan (D-05).
- Landmark line 1: `{n} / {m}` with spaces around the slash, 1-based `n = unitIndex + 1` (when complete, keep last n/m or show m/m — UI-SPEC: after persist FileScaffold remains; landmark may stay at last unit). Kind/name subtitle per the UI-SPEC table; kind `other` is never a visible word. `role="status"` `aria-live="polite"` `aria-atomic="true"`; `aria-label` = landmark + subtitle joined by comma+space. Landmark **outside** the scroll container. `min-height: 1.4em` on the N/M line.
- File chrome wrapper: `aria-label="File"`, `max-height: 70vh; overflow: auto`, segment stack `gap: 0`. Current card: `data-scaffold-current`, `--color-surface` fill, 1px `--color-border`, radius 6px, padding `var(--space-md)`. Static segments: `data-scaffold-role="done" | "future" | "gap"`, `user-select: text`, not a textarea. Future: `text-muted`. Done/gap: `--color-text`. `white-space: pre-wrap; overflow-wrap: anywhere; tab-size: 4`.
- Static regions: map code points through `glyphFor` + `.ws-glyph` (space ·, newline ↵). React text/spans only (D-03, D-06). Clicks on static text do not type (D-04).
- `App.startScaffold(plan)` (D-07, D-08): exercise = plan.exercise (full file), curriculum = plan.units, unitIndex = 0, snapshots = [], startedAt = Date.now() (file-level), resetCapture, loadToken++, metrics null, saveFailed false. Fallback still FileScaffold (D-09). Keep `filePlan` for `data-file-plan` if still used.
- `handleLoad` last-wins vs scaffold (D-10, SCAF-05): clear curriculum, snapshots, unitIndex, filePlan; then today's whole-file CaptureSurface on `exercise.text`. Tab Paste|GitHub does not call startScaffold or handleLoad (Phase 7 D-04).
- Unit complete wrapper (D-12, D-15): if unitIndex &lt; length-1: push snapshot of getEvents/getCharLog/getMarkers, resetCapture, unitIndex++, loadToken++. Else last unit: snapshot, flattenSnapshots, assembleSessionFromLogs, computeSessionMetrics(joinUnitSlices(...), ...), setMetrics, fire-and-forget saveSession. Never persist until last unit. After persist: complete=true, Restart hidden, do not arm a second saveSession (D-18, Pitfall 11 option a).
- Shared Escape/Restart handler (D-16, D-17): if curriculum active and not complete: resetCapture, loadToken++, keep unitIndex/snapshots/startedAt. Paste path keeps today's whole-session restart including new startedAt. COPY: `Restart unit` while scaffold in progress (including 1-unit fallback); `Restart exercise` on paste/upload; hidden after scaffold persist.
- Empty-state COPY verbatim (GitHub door named). No FileScaffold until load or onPlanned.
- Invert App.test idle onPlanned (Pitfall 10). HistoryView.tsx unchanged (D-21).

<interfaces>
From 09-01:
sliceUnit(text, start, end): string
joinUnitSlices(text, units): string
coverFile(text, units, unitIndex): ScaffoldSegment[]
flattenSnapshots(snaps): { events, charLog, markers }
assembleSessionFromLogs(exercise, logs, startedAt): Session

From src/ui/CaptureSurface.tsx:
props stay { text, onRestartRequested?, onComplete? }. Escape (trusted keydown) calls onRestartRequested. onComplete fires once per remount via completedAt.

From src/ui/App.tsx today:
handleLoad / handleRestart / handleComplete / loadToken / hide-not-unmount trainer subtree / RepoBrowser onPlanned={setFilePlan} which this plan replaces with startScaffold.
CorpusInput onLoad remains handleLoad.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FileScaffold source-order chrome</name>
  <files>src/ui/FileScaffold.tsx, src/ui/FileScaffold.test.tsx</files>
  <read_first>
    - .planning/phases/09-scaffolded-trainer/09-UI-SPEC.md (COPY, Layout, Interaction Contract, Color, Typography)
    - .planning/phases/09-scaffolded-trainer/09-PATTERNS.md (FileScaffold.tsx, FileScaffold.test.tsx)
    - src/ui/CaptureSurface.tsx (props, glyphFor usage, prefersReducedMotion — do not edit)
    - src/ui/CaptureSurface.test.tsx (happy-dom bootstrap, ws-glyph, trusted events)
    - src/ui/RepoBrowser.tsx (COPY const, role=status)
    - src/scaffold/cover.ts
    - src/scaffold/slice.ts
    - src/trainer/state.ts (glyphFor)
    - src/index.css (.preview, .trainer-stack, .ws-glyph, .text-muted)
  </read_first>
  <behavior>
    - Current region contains #capture-surface; future/done/gap contain zero textareas (D-02, D-04, SCAF-01)
    - Leaves-first curriculum still paints top-to-bottom by start; a later-source current unit can sit below a future unit (D-01)
    - Fixture text that looks like markup (img onerror) appears as characters; querySelector('img') is null (D-06)
    - Static regions render glyphFor middle-dot and newline glyphs via .ws-glyph (UI-SPEC glyph backstop — characters, not pixels)
    - Fallback 1 / 1 landmark present; one current card; no future role (SCAF-03 empty / D-09)
    - complete=true: zero #capture-surface; every unit/gap data-scaffold-role is done or gap, none future or current
    - scrollIntoView spy on the current-card element is called on mount and when unitIndex changes, with block nearest and inline nearest; behavior instant when matchMedia reduce matches, else smooth (D-13)
    - CaptureSurface is called with the current unit slice, never the full multi-unit file
    - COPY.landmark uses spaces around slash; KIND_LABEL.other is the empty string
  </behavior>
  <action>
    Create FileScaffold per D-01 through D-06 and D-13. COPY and KIND_LABEL copied verbatim from 09-UI-SPEC.md (do not paraphrase). Landmark assembly rules from that spec's table. Do not duplicate RepoBrowser fallback/planned status copy on the chrome.

    Render coverFile segments in array order (already source-sorted). Current unit card wraps CaptureSurface with key={loadToken} and text={sliceUnit(text, unit.start, unit.end)}. When complete is true, pass coverFile unitIndex equal to units.length so every unit is done, and do not mount CaptureSurface.

    Static done/future/gap: React text and glyph spans only. Treat blob text as data, never as markup (D-06). Future className text-muted; user-select text; not a textarea. Padding var(--space-md) on every pre and the current card. Chrome max-height 70vh overflow auto; landmark outside the scroller. Current card uses surface+border tokens, not accent (07-UI-SPEC reserved list).

    Copy prefersReducedMotion from CaptureSurface as a local helper (do not edit CaptureSurface). useLayoutEffect on unitIndex + mount: current-card scrollIntoView block nearest, inline nearest, behavior instant vs smooth as UI-SPEC. Default block start is forbidden.

    Do not subscribe the file chrome to rAF / useCharLogTick. Do not call persist from FileScaffold. Do not grow CaptureSurface props. Zero live GitHub, zero WASM in tests. Spy scrollIntoView; do not assert scrollTop.

    Tests: happy-dom ui project, createRoot/act/IS_REACT_ACT_ENVIRONMENT analog of CaptureSurface.test.tsx. FilePlan fixtures, not planUnits(realTree).
  </action>
  <verify>
    <automated>pnpm exec vitest run --project ui src/ui/FileScaffold.test.tsx && python3 -c "import pathlib; t=pathlib.Path('src/ui/FileScaffold.tsx').read_text(); assert 'innerHTML' not in t; assert 'dangerouslySetInnerHTML' not in t"</automated>
  </verify>
  <acceptance_criteria>
    - FileScaffold.test.tsx exits 0
    - FileScaffold.tsx has no innerHTML token and no dangerouslySetInnerHTML token
    - FileScaffold.tsx contains COPY.landmark, KIND_LABEL, coverFile, sliceUnit, data-scaffold-current, aria-label File
    - CaptureSurface.tsx is unmodified
  </acceptance_criteria>
  <done>Full file is visible in source order; only the current slice is typeable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: App startScaffold, last-wins, paste exit</name>
  <files>src/ui/App.tsx, src/ui/App.test.tsx</files>
  <read_first>
    - .planning/phases/09-scaffolded-trainer/09-PATTERNS.md (App.tsx, App.test.tsx)
    - .planning/phases/09-scaffolded-trainer/09-UI-SPEC.md (App COPY empty-state and Restart labels)
    - src/ui/App.tsx
    - src/ui/App.test.tsx (idle onPlanned test to invert; persist helpers; tab-switch test)
    - src/ui/FileScaffold.tsx
  </read_first>
  <behavior>
    - onPlanned of a fallback 1-unit FilePlan mounts #capture-surface and a 1 / 1 landmark; empty-state GitHub sentence is gone (D-07, D-09, invert Pitfall 10)
    - CaptureSurface value/text for that fallback may equal the file (one unit); for a two-unit fixture, #capture-surface value is the first curriculum slice and is not the full file (D-02, SCAF-01)
    - handleLoad after a scaffold unmounts FileScaffold (no data-scaffold-current) and mounts whole-file CaptureSurface (D-10, SCAF-05)
    - Switching Paste | GitHub tabs does not clear an in-progress scaffold (existing tab test stays green; Phase 7 D-04)
    - A second onPlanned replaces the first: landmark returns to 1 / M of the new plan; prior unit snapshots are discarded (D-10 last-wins, SCAF-01 concurrency)
    - Empty-state heading remains No exercise loaded; body is the UI-SPEC GitHub sentence verbatim
    - Hide-not-unmount: trainer display none on History, never the hidden attribute (D-11)
  </behavior>
  <action>
    Replace RepoBrowser onPlanned={setFilePlan} with startScaffold (D-07, D-08). startScaffold sets exercise to plan.exercise (full file), curriculum to plan.units, unitIndex 0, empty snapshots ref, file-level startedAt Date.now(), resetCapture then loadToken++, clears metrics and saveFailed. Keep filePlan state for data-file-plan if the attribute still exists.

    Do not pass plan.exercise into the paste/upload load callback — that callback is whole-file typing (SCAF-05).

    handleLoad also last-wins: clear curriculum, snapshots, unitIndex, filePlan, then existing resetCapture / setExercise / remount / buildSession path so CaptureSurface text is exercise.text with no FileScaffold.

    App COPY const verbatim from 09-UI-SPEC.md emptyHeading, emptyBody, restartExercise, restartUnit. Render FileScaffold when curriculum is non-null; else when exercise is set, mount CaptureSurface on exercise.text as today.

    Invert the Phase 8 test that holds onPlanned FilePlan in memory without starting the trainer (Pitfall 10). Keep paste Load exercise tests green. Skip the 250ms live buildSession interval when curriculum is non-null so persist cannot depend on a current-unit-only sessionRef.

    Do not edit capture.ts, CaptureSurface.tsx, RepoBrowser.tsx, or HistoryView.tsx.
  </action>
  <verify>
    <automated>pnpm exec vitest run --project ui src/ui/App.test.tsx src/ui/FileScaffold.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - App tests covering onPlanned start, multi-unit slice ≠ full file, handleLoad exits scaffold, tab switch does not reset, last-wins second onPlanned — exit 0
    - App.tsx contains startScaffold and onPlanned={startScaffold} or equivalent
    - App.tsx empty-state body matches the UI-SPEC GitHub sentence
    - capture.ts and CaptureSurface.tsx still unmodified
  </acceptance_criteria>
  <done>Click-to-type starts unit 0; paste/upload still whole-file; last-wins holds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Advance, persist, unit restart, History github row</name>
  <files>src/ui/App.tsx, src/ui/App.test.tsx</files>
  <read_first>
    - .planning/phases/09-scaffolded-trainer/09-RESEARCH.md (Pattern 3, Pattern 4, Pitfalls 1, 2, 3, 6, 11)
    - .planning/phases/09-scaffolded-trainer/09-UI-SPEC.md (Restart copy, post-complete chrome)
    - src/ui/App.tsx
    - src/ui/App.test.tsx (loadAndCompleteExercise, listNewestFirst, History nav)
    - src/ui/CaptureSurface.test.tsx (trustedKeyEvent Escape)
    - src/ui/HistoryView.tsx (github ternary — read only, D-21)
    - src/metrics/metrics.ts (computeSessionMetrics target)
    - src/metrics/resolve-metrics.ts (do not change; document trap only)
    - .planning/phases/08-parse-dependency-units/08-UAT.md (test 2 skipped — now checkable)
  </read_first>
  <behavior>
    - Two-unit GitHub fixture typed to completion: exactly one listNewestFirst row; exercise.text is the full file; sourceType github; sourceRef is owner/repo:path; charLog contains commits from both slices (D-14, D-15, SCAF-03, D-21)
    - Results panel is visible after last unit; Restart button is absent; FileScaffold has no #capture-surface (all done) (D-18)
    - Completing unit 0 of a two-unit plan does not write a History row and does not show ResultsView; landmark becomes 2 / 2 (D-12, SCAF-02)
    - After unit 0 is done and unit 1 is showing, trusted Escape does not put unit 0's slice back into #capture-surface; unitIndex stays 1; History still has zero rows (D-16, SCAF-04)
    - Restart unit button (label Restart unit) same as Escape: remounts current slice, keeps snapshots (D-16)
    - Paste path still persists one row via live buildSession and shows Restart exercise (SCAF-05 regression)
    - After scaffold persist, Escape/Restart must not write a second History row (Pitfall 11 option a)
    - History view for that github row shows the sourceRef and does not show Pasted snippet (D-21, 08-UAT test 2)
  </behavior>
  <action>
    Intercept CaptureSurface onComplete while curriculum is active (D-12, D-15). Same turn, synchronous: snapshot getEvents/getCharLog/getMarkers, then resetCapture before React commits the next surface. If more units remain, increment unitIndex and loadToken. Do not call persist until the last unit (SCAF-02 vs SCAF-03). Fallback 1-unit: first complete is last — still FileScaffold, still one Session.

    Last unit: flattenSnapshots including the last snapshot, assembleSessionFromLogs(exercise full file, flat logs, file-level startedAt), computeSessionMetrics(joinUnitSlices(exercise.text, curriculum), charLog, markers, completedAt) — never pass source-order exercise.text as the metrics target (RESEARCH Pitfall 2 / SCAF-03 encoding). setMetrics then fire-and-forget saveSession as today. Set complete so FileScaffold unmounts CaptureSurface. Hide Restart. Do not push another snapshot on a later complete; do not arm a second saveSession (D-18).

    Narrow handleRestart when curriculum is active and not complete: resetCapture, loadToken++, keep unitIndex, snapshots, startedAt (D-16, D-17). Paste path keeps today's handler (new startedAt). Escape already calls onRestartRequested from CaptureSurface — share this handler. COPY restartUnit vs restartExercise vs hidden per UI-SPEC.

    Do not change HistoryView.tsx (D-21). Do not change resolve-metrics.ts or Dexie (metricsSnapshot is what History shows). Do not modify capture.ts.

    Tests reuse trusted beforeinput + nextFrame from App.test.tsx and trusted Escape from CaptureSurface.test.tsx. Two-unit fixture must be leaves-first so typedTarget ≠ file text — assert the persisted row's exercise.text is still the source file. Zero api.github.com. Zero Language.load.
  </action>
  <verify>
    <automated>pnpm exec vitest run --project ui src/ui/App.test.tsx src/ui/FileScaffold.test.tsx && pnpm exec vitest run --project unit src/scaffold/slice.test.ts src/scaffold/cover.test.ts src/scaffold/flatten.test.ts src/session.test.ts && pnpm exec tsc --noEmit -p tsconfig.json && test -z "$(git diff -- src/capture/capture.ts src/ui/CaptureSurface.tsx src/ui/HistoryView.tsx src/ui/RepoBrowser.tsx)" && python3 -c "from pathlib import Path; t=Path('src/ui/FileScaffold.tsx').read_text()+Path('src/ui/App.tsx').read_text(); assert 'innerHTML' not in t; assert 'dangerouslySetInnerHTML' not in t"</automated>
  </verify>
  <acceptance_criteria>
    - All listed ui and unit tests exit 0; tsc --noEmit exits 0
    - capture.ts, CaptureSurface.tsx, HistoryView.tsx, RepoBrowser.tsx have no diff
    - App.tsx FileScaffold/App.tsx have no innerHTML token and no dangerouslySetInnerHTML token
    - App.tsx uses joinUnitSlices (or equivalent curriculum-order join) as computeSessionMetrics first argument on the scaffold persist path
    - App.tsx uses flattenSnapshots and assembleSessionFromLogs on last unit
    - Restart unit label appears during scaffold; Restart is not rendered after last-unit persist
  </acceptance_criteria>
  <done>Unit advance, unit restart, one History row for the whole GitHub file, paste path unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub blob text → FileScaffold static regions and CaptureSurface slice | Untrusted corpus crosses into the DOM |
| CaptureSurface onComplete → saveSession | Scaffold must persist once per file, last unit only |
| FilePlan → App curriculum | Must not enter the paste/upload whole-file load callback |
| Static region clicks → typing | Clicks on done/future must not type |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-05 | Tampering | FileScaffold static regions | high | mitigate | React text children and glyph spans only; fixture that would execute if interpreted as HTML must remain characters; no highlighter |
| T-09-06 | Tampering | CaptureSurface text | high | mitigate | text is sliceUnit of the current unit only when units.length is greater than 1; native textarea, no markup sink |
| T-09-07 | Tampering | App persist path | high | mitigate | persist only on last unit; after persist do not arm a second saveSession (Pitfall 11); one Session per file |
| T-09-08 | Denial of service | FileScaffold full-file paint | medium | mitigate | 100 KB ingest cap already; static regions must not subscribe to rAF; useCharLogTick stays inside CaptureSurface |
| T-09-09 | Spoofing | Future/done clicks | low | accept | Not a textarea; clicks do not type; user-select allowed; single-user local SPA |
| T-09-10 | Information disclosure | FileScaffold / App | low | accept | No fetch from these files; github/client remains the only fetch module |
| T-09-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages |
</threat_model>

<verification>
- pnpm exec vitest run --project ui src/ui/FileScaffold.test.tsx src/ui/App.test.tsx
- pnpm exec vitest run --project unit src/scaffold/slice.test.ts src/scaffold/cover.test.ts src/scaffold/flatten.test.ts src/session.test.ts
- pnpm exec tsc --noEmit -p tsconfig.json
- git diff empty for capture.ts, CaptureSurface.tsx, HistoryView.tsx, RepoBrowser.tsx
- FileScaffold.tsx and App.tsx contain no innerHTML token and no dangerouslySetInnerHTML token
<human-check>
1. pnpm dev in Chromium: import a small public repo, click a .ts file — typing starts on unit 0 immediately; full file visible; future units dimmed; landmark N / M.
2. Type the current unit to completion — instant advance, no Next button; Escape restarts only that unit.
3. Finish the last unit — ResultsView appears, Restart is gone, History shows one row with owner/repo:path and not Pasted snippet (08-UAT test 2).
4. Paste tab Load exercise after a scaffold — FileScaffold gone, whole-file trainer, Restart exercise.
5. Glyph columns: done/future · and ↵ visually line up with the overlay (layout-engine; unit tests cannot assert pixels).
</human-check>
</verification>

<success_criteria>
- SCAF-01..05 observable in App tests
- Metrics for multi-unit github use curriculum-order join
- One History row, full Exercise.text, github sourceRef
- CaptureSurface props and capture.ts unchanged
- UI-SPEC COPY verbatim; Restart hidden after last-unit persist
</success_criteria>

<output>
Create `.planning/phases/09-scaffolded-trainer/09-02-SUMMARY.md` when done
</output>

---
phase: 07-github-url-repo-tree
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - src/ui/RepoBrowser.tsx
  - src/ui/RepoBrowser.test.tsx
  - src/ui/App.tsx
  - src/ui/App.test.tsx
  - src/index.css
autonomous: true
requirements: [REPO-01, REPO-02, REPO-03, REPO-04]

must_haves:
  truths:
    - "Trainer shows a Paste | GitHub tablist; Paste is selected on first paint; one panel visible at a time (D-01, D-03)"
    - "Corpus shell (switch + both panels) uses display none on History and Analytics and stays mounted (D-02)"
    - "Switching tabs does not clear paste text, language, GitHub field, caption, tree, or status (D-04)"
    - "User pastes a public GitHub URL or owner/repo, submits Import or Enter, and sees the default-branch filesystem tree (REPO-01, D-11)"
    - "Folders expand and collapse via native details; every returned path is visible including non-TS/JS (REPO-02, D-12)"
    - "Non-loadable file click shows blocked copy in the status region and does not load an exercise (REPO-03, D-05)"
    - "TS/JS file click shows the distinct not-yet copy, does not fetch a blob, and does not load an exercise (D-05, D-06, success criterion 5)"
    - "404, rate-limit, and truncated each have their locked UI-SPEC strings; truncated still renders arrived entries (REPO-04, D-14, D-15)"
    - "Invalid URL, 404, rate-limit, other HTTP, unreachable use role alert and destructive color; blocked, not-yet, truncated, empty-repo use role status and text-muted (D-14)"
    - "First successful import opens only top-level details; nested folders omit the open attribute (D-16)"
    - "Empty GitHub panel is the labeled URL field + Import — no extra empty-state paragraph (UI empty E2)"
    - "While Importing… the submit button is disabled and the URL field stays enabled; no spinner (UI loading E2)"
    - "Named error copy writes the status region; previous tree stays; aria-invalid only for invalid-URL copy (UI error E2)"
    - "URL field is full width; long values scroll inside the native input (UI long-text E2)"
    - "Caption min-height 1.4em is a floor, not a clip; wraps with overflow-wrap anywhere (UI overflow/long-text E3)"
    - "Empty-repo success omits the tree ul, keeps caption, status is the empty-repo string (UI empty E4, zero-one-many E4)"
    - "Previous tree and caption stay until a new success; busy is Importing… on the form, not a tree skeleton (UI loading E4)"
    - "On error the previous tree stays browseable (UI error E4)"
    - "Populated tree is nested ul, top-level folders open, every returned path visible (UI populated E4)"
    - "truncated true still renders arrived entries plus the truncated notice (UI partial E4, D-15)"
    - "Tree wrapper max-height 40vh overflow auto; names wrap; horizontal scroll only for an unbreakable token (UI overflow E4)"
    - "One or many files share the same row layout — no plural copy (UI zero-one-many E4)"
    - "File and folder names wrap with overflow-wrap anywhere; no ellipsis (UI long-text E5, REPO-02)"
    - "Status min-height 48px so two-sentence notices do not reflow; extra wrap may grow the slot (UI overflow E6, D-14)"
    - "Status copy wraps in the column; no ellipsis (UI long-text E6)"
    - "Last-wins Import token: start clears status immediately; success replaces tree/caption; error keeps previous tree (D-08, REPO-03 concurrency)"
    - "Folder summary toggles only and never writes the status region (D-12)"
    - "No selection highlight or pending-file API (D-07)"
    - "Rate-limit {time} is toLocaleTimeString(undefined, { hour: 'numeric', minute: '2-digit' }) from resetEpochS * 1000; missing reset uses the unknown-reset string (REPO-04 precision)"
    - "Header nav stays exactly Trainer, History, Analytics"
  prohibitions:
    - statement: "MUST NOT start a whole-file typing session from Import or any tree click"
      status: unverified
      verification: flagged
    - statement: "MUST NOT distinguish private-vs-missing in user-facing copy — one 404 string"
      status: unverified
      verification: flagged
    - statement: "MUST NOT persist last URL or last tab to localStorage this phase"
      status: unverified
      verification: flagged
    - statement: "MUST NOT reuse the blocked-file sentence for TS/JS clicks"
      status: unverified
      verification: flagged
    - statement: "MUST NOT use Phase 8, parser, WASM, or tree-sitter jargon in notices"
      status: unverified
      verification: flagged
    - statement: "MUST NOT echo GitHub JSON message, client IP, or a stack trace as error copy"
      status: unverified
      verification: flagged
  artifacts:
    - path: "src/ui/RepoBrowser.tsx"
      provides: "Form, caption, disclosure tree, single status region; COPY const verbatim from 07-UI-SPEC"
      exports: ["RepoBrowser"]
    - path: "src/ui/RepoBrowser.test.tsx"
      provides: "happy-dom; mocked fetchRepoTree; blocked vs not-yet; no exercise-load callback"
    - path: "src/ui/App.tsx"
      provides: "Trainer-only corpus shell + Paste|GitHub tablist; CorpusInput unchanged inside Paste"
    - path: "src/index.css"
      provides: ".repo-tree, .repo-status, .repo-caption, [role=tab][aria-selected] — zero new color tokens"
  key_links:
    - from: "src/ui/RepoBrowser.tsx"
      to: "src/github/client.ts"
      via: "onImport calls parseGithubRef then fetchRepoTree"
      pattern: "fetchRepoTree"
    - from: "src/ui/App.tsx"
      to: "src/ui/RepoBrowser.tsx"
      via: "GitHub tabpanel mounts RepoBrowser with no exercise-load prop"
      pattern: "RepoBrowser"
    - from: "src/ui/RepoBrowser.tsx"
      to: "src/github/tree.ts"
      via: "foldTree(result.entries) and isLoadablePath for muted names + click branch"
      pattern: "foldTree|isLoadablePath"
---

<objective>
Ship the Trainer-only Paste | GitHub switch and a browse-only RepoBrowser:
Import a public repo, expand/collapse every returned path, show locked
blocked / not-yet / 404 / rate-limit / truncated copy, and never start a
typing session from the tree (D-01..D-16, REPO-01..04).

Purpose: this is the user-visible phase goal. 07-01/07-02 are unreachable
without this chrome.

Output: RepoBrowser, App corpus shell, tree/tab CSS, updated App tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-github-url-repo-tree/07-CONTEXT.md
@.planning/phases/07-github-url-repo-tree/07-RESEARCH.md
@.planning/phases/07-github-url-repo-tree/07-UI-SPEC.md
@.planning/phases/07-github-url-repo-tree/07-PATTERNS.md
@src/ui/CorpusInput.tsx
@src/ui/CorpusInput.test.tsx
@src/ui/App.tsx
@src/ui/App.test.tsx
@src/index.css
@src/github/url.ts
@src/github/client.ts
@src/github/tree.ts
@src/github/errors.ts
</context>

## Artifacts this phase produces

Full phase inventory (07-01 types/errors/url/tree, 07-02 fetchRepoTree) plus this plan:

- `RepoBrowser` component — `src/ui/RepoBrowser.tsx` (no exercise-load prop)
- `COPY` const keys: tab labels live in App; RepoBrowser COPY holds urlLabel, urlPlaceholder, importIdle `Import`, importBusy `Importing…`, invalidUrl, notFound, rateLimited, rateLimitedUnknown, otherHttp, unreachable, truncated, blocked, notYet, emptyRepo — strings verbatim from 07-UI-SPEC.md Copywriting Contract
- Caption interpolates `{owner}/{repo}@{defaultBranch}` with no Loaded-from prefix
- CSS: `.repo-tree`, `.repo-tree ul`, `.repo-tree ul ul`, `.repo-tree button`, `.repo-tree summary`, `.repo-caption`, `.repo-status`, `[role='tab'][aria-selected='true']`, `[role='tab'][aria-selected='false']`
- Ids: `github-url`, `corpus-tab-paste`, `corpus-tab-github`, `corpus-panel-paste`, `corpus-panel-github`
- Tree wrapper `aria-label="Repository files"`
- Form `aria-busy` while in flight

<canon_referral>
- XSS via file/dir names is canon — T-07-06; React text children only; not minted.
- Path traversal is canon — T-07-03; not minted.
- Credential headers are canon — T-07-04; not minted.
</canon_referral>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RepoBrowser form, tree, status, and CSS (D-05..D-16, D-11)</name>
  <files>src/ui/RepoBrowser.tsx, src/ui/RepoBrowser.test.tsx, src/index.css</files>
  <read_first>
    - .planning/phases/07-github-url-repo-tree/07-PATTERNS.md (RepoBrowser.tsx, RepoBrowser.test.tsx, src/index.css)
    - src/ui/CorpusInput.tsx (COPY, last-wins token, busy primary, reserved status row, label+control+wrapped primary)
    - src/ui/CorpusInput.test.tsx (createRoot + act + IS_REACT_ACT_ENVIRONMENT + controlled setter)
    - .planning/phases/07-github-url-repo-tree/07-UI-SPEC.md (Copywriting Contract, Interaction Contract, spacing exceptions)
    - src/github/url.ts
    - src/github/client.ts
    - src/github/tree.ts
    - src/github/errors.ts
    - src/index.css (nav button[aria-current], .preview, .save-failed-dismiss, .history-list)
  </read_first>
  <behavior>
    - Invalid URL (gist / gitlab / whitespace) shows COPY.invalidUrl on a role=alert region, aria-invalid on #github-url, and fetchRepoTree is not called
    - Successful fixture with src/App.tsx and README.md renders caption o/r@main, a details for src (open), a muted README.md button, a full-color App.tsx button
    - Click README.md → status role=status text This file can't be split yet. No exercise loaded.
    - Click App.tsx → status role=status text TypeScript/JavaScript files open as scaffolded exercises in the next step. Browsing only for now.
    - Click .mjs fixture → blocked string, not the not-yet string
    - Truncated fixture still lists returned names and shows the truncated string
    - EmptyRepoError or tree [] → caption set, no .repo-tree ul, empty-repo string, role=status
    - RepoNotFoundError → 404 string, role=alert, previous tree kept if one existed
    - RateLimitedError with resetEpochS → rate-limit string including toLocaleTimeString hour numeric minute 2-digit
    - TypeError from fetchRepoTree → Couldn't reach GitHub. Check the connection and try again.
    - Import start clears status; second overlapping Import last-wins
    - Folder details click does not change status text
    - Enter in #github-url submits the form
  </behavior>
  <action>
    Add CSS in src/index.css only, reusing existing tokens (no new colors).
    Selected/unselected [role='tab'][aria-selected] copies nav button[aria-current]
    fill/weight/muted — not accent. .repo-tree copies .preview max-height 40vh
    overflow auto. Outer ul list-style none margin 0 padding 0 display grid gap 0;
    nested ul padding-inline-start var(--space-md). .repo-tree button and
    summary: min-height 32px, transparent, no border, text-align left, width 100%,
    overflow-wrap anywhere (same exemption class as .save-failed-dismiss).
    .repo-caption min-height 1.4em; .repo-status min-height 48px. Tabs and Import
    stay on the 44px floor.

    Create RepoBrowser with no exercise-load prop. COPY const verbatim from
    07-UI-SPEC.md tables (do not paraphrase; do not invent a getting-started
    paragraph). Form onSubmit preventDefault → onImport (D-11). URL input
    id=github-url type=text (not url), spellCheck false, autocomplete off,
    class control, width 100%, visible label htmlFor. Import type=submit
    class=primary wrapped in a single-child div, disabled while busy, label
    Importing… while busy. URL field stays enabled. aria-busy on the form.
    No fetch on paste or change.

    Last-wins useRef token like CorpusInput handleFileChange: increment;
    setBusy true; clear status immediately; keep previous tree/caption until
    success. parseGithubRef then fetchRepoTree. instanceof map to COPY.
    TypeError → unreachable copy (do not echo the browser error message).
    Unknown errors rethrow. EmptyRepoError and tree [] are successes:
    replace tree with empty listing, set caption, empty-repo status.
    truncated true → render foldTree(entries) plus truncated status.
    Error paths do not replace tree or caption.

    Caption: separate muted .repo-caption above the tree,
    `{owner}/{repo}@{defaultBranch}`, empty until success.

    Tree: foldTree → nested ul/li/details/summary per 07-PATTERNS.md Dir
    helper. open={depth === 0 ? true : undefined} only (D-16). Never write
    open={false}.     Files are button type=button with {node.name} as the
    accessible name. className text-muted when !isLoadablePath (D-13). Folders
    never muted and use full --color-text. No icons. React text children only — no HTML-string injection, no
    markdown. No role=tree. Folder summary: native toggle, do not set
    status. File click replaces status (D-08); no dismiss control; no
    aria-current on the row (D-07). Loadable click → not-yet string.
    Other files including commit leaves → blocked string. Do not call
    fetchRepoTree for blobs. Do not invoke App's exercise loader.

    One .repo-status p under the tree. role alert + --color-destructive for
    invalid URL, 404, rate-limit, other HTTP, unreachable. role status +
    text-muted for blocked, not-yet, truncated, empty-repo, and idle empty.
    aria-invalid on the URL field only while invalid-URL copy is showing;
    clear it on the next Import start.

    Tests: happy-dom, createRoot/act analog from CorpusInput.test.tsx.
    Controlled input setter uses HTMLInputElement.prototype. vi.mock
    ../github/client fetchRepoTree — zero live network. Do not pass an
    exercise-load callback into RepoBrowser.
  </action>
  <verify>
    <automated>pnpm exec vitest run --project ui src/ui/RepoBrowser.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - RepoBrowser.test.tsx exits 0
    - COPY.blocked and COPY.notYet are different strings and both appear in RepoBrowser.tsx
    - RepoBrowser.tsx has no onLoad prop and no handleLoad identifier
    - RepoBrowser.tsx contains Importing… and the 404 string No public GitHub repository matches that URL.
    - RepoBrowser.tsx does not contain Phase 8 or tree-sitter
    - .repo-tree and .repo-status exist in src/index.css
    - File names are rendered as JSX text children
  </acceptance_criteria>
  <done>Browse-only GitHub panel: Import, tree, locked notices, no exercise start.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Trainer-only Paste | GitHub shell (D-01..D-04)</name>
  <files>src/ui/App.tsx, src/ui/App.test.tsx</files>
  <read_first>
    - .planning/phases/07-github-url-repo-tree/07-PATTERNS.md (App.tsx and App.test.tsx)
    - src/ui/App.tsx (view union, three-item header, CorpusInput line, hide-not-unmount trainer subtree)
    - src/ui/App.test.tsx (loadAndCompleteExercise via #corpus-paste; nav labels; Analytics stay-mounted block)
    - src/ui/RepoBrowser.tsx
    - .planning/phases/07-github-url-repo-tree/07-UI-SPEC.md (Tabs and persistence, Accessibility extras)
    - .planning/phases/07-github-url-repo-tree/07-RESEARCH.md (Pitfall 7)
  </read_first>
  <behavior>
    - First paint: Paste tab aria-selected true, GitHub tab aria-selected false, #corpus-paste in the Paste tabpanel
    - Click GitHub tab: Paste panel display none, GitHub panel display grid, #github-url present
    - Click Paste tab again: paste textarea value is still whatever the user typed
    - Header buttons textContent still ['Trainer', 'History', 'Analytics']
    - History and Analytics: corpus shell display none; #corpus-paste and #github-url still in the document
    - loadAndCompleteExercise still works because Paste is the default tab
    - RepoBrowser is rendered without an exercise-load prop
    - localStorage is not read or written for corpus tab
  </behavior>
  <action>
    In App.tsx keep the view union and three header buttons unchanged.
    Wrap CorpusInput and RepoBrowser in a corpus shell:
    style display view === 'trainer' ? grid : none, gap var(--space-md).
    Never use the hidden attribute. Never unmount the shell.

    Inside the shell: role=tablist aria-label=Corpus source, two buttons
    role=tab, ids corpus-tab-paste and corpus-tab-github, aria-controls
    corpus-panel-paste / corpus-panel-github, min-height 44px, padding
    matching header nav. Labels Paste and GitHub. useState corpus tab
    'paste' on load (D-03). Do not persist the tab.

    Each tabpanel: id + role=tabpanel + display grid when selected else
    none (D-04). Paste panel: existing CorpusInput onLoad={handleLoad}
    unchanged — do not teach CorpusInput about GitHub. GitHub panel:
    RepoBrowser with no extra props. handleLoad body stays untouched.

    Update App.test.tsx: keep loadAndCompleteExercise querying #corpus-paste.
    Keep nav label assertion. Extend the Analytics stay-mounted test so
    #corpus-paste is still present and the corpus shell wrapper display is
    none. Add the same for History. Add a test that GitHub tab reveals
    #github-url and that switching back keeps paste text. Add that
    document.cookie / localStorage does not gain a corpus-tab key (assert
    localStorage.getItem for any tab key is null after switching).
  </action>
  <verify>
    <automated>pnpm exec vitest run --project ui src/ui/App.test.tsx src/ui/RepoBrowser.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - App.test.tsx and RepoBrowser.test.tsx exit 0
    - App.tsx contains role="tablist" and aria-label="Corpus source"
    - App.tsx still has exactly the three nav labels Trainer History Analytics
    - CorpusInput still receives onLoad={handleLoad}
    - RepoBrowser usage in App.tsx has no onLoad=
    - App.tsx does not call localStorage for the corpus tab
  </acceptance_criteria>
  <done>Paste remains the default corpus path; GitHub is an opt-in Trainer-only tab; History/Analytics hide the shell without unmounting.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub path names → DOM | Untrusted blob/tree names render in the tree |
| User URL field → parseGithubRef | Invalid hosts must not reach fetchRepoTree |
| RepoBrowser → App exercise state | This phase must not cross into handleLoad |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-06 | Tampering | RepoBrowser tree labels | high | mitigate | React text children / textContent only; no HTML-string injection; no README markdown |
| T-07-10 | Tampering | file click → trainer | high | mitigate | RepoBrowser has no exercise-load prop; clicks only write the status region |
| T-07-11 | Information disclosure | localStorage | medium | mitigate | Do not persist last URL or last tab (D-03) |
| T-07-14 | Information disclosure | status copy | medium | mitigate | Locked COPY strings only; do not interpolate GitHub JSON or IP |
| T-07-12 | Denial of service | Import | medium | mitigate | No fetch-on-paste; last-wins token; button disabled while busy |
| T-07-13 | Elevation of privilege | CSP / COEP | high | mitigate | Do not strip COEP in vite.config.ts; listing origin already allowlisted in 07-01 |
| T-07-SC | Tampering | npm installs | low | accept | No packages; no tree libraries |
</threat_model>

<verification>
- pnpm exec vitest run --project ui src/ui/App.test.tsx src/ui/RepoBrowser.test.tsx
- pnpm exec vitest run --project unit src/github/url.test.ts src/github/tree.test.ts src/github/client.test.ts
- pnpm exec tsc --noEmit -p tsconfig.json
- Header still three items; CorpusInput COPY unchanged
- vite.config.ts still sends COOP same-origin and COEP require-corp on server and preview (do not edit unless a live check forces credentialless)
  <human-check>
    1. `pnpm dev` and open Chromium. Trainer shows Paste selected. Switch to GitHub: labeled field + Import, no extra intro paragraph.
    2. Paste a small public repo (`owner/repo`) and press Import. Caption `owner/repo@default_branch` appears; top-level folders are open; nested folders are closed. Expand/collapse works. A non-TS/JS file shows the blocked sentence; a `.ts`/`.tsx`/`.js`/`.jsx` file shows the not-yet sentence. Trainer still shows “No exercise loaded” — no session starts.
    3. Paste a gist or GitLab URL and Import: alert copy “Paste a GitHub URL or owner/repo.” Network tab shows no api.github.com request.
    4. Switch to History then Analytics: no URL field or tree visible. Return to Trainer: GitHub tree and Paste text still there.
    5. `curl -sI` against `pnpm preview` still includes Cross-Origin-Opener-Policy: same-origin and Cross-Origin-Embedder-Policy: require-corp. In the running app, `crossOriginIsolated === true`. If GitHub is blocked, switch COEP to credentialless only — never remove isolation.
  </human-check>
</verification>

<success_criteria>
- All five ROADMAP success criteria are true in the UI
- D-01 through D-16 are implemented
- Paste/upload path and SourceType union are unchanged
</success_criteria>

<output>
Create `.planning/phases/07-github-url-repo-tree/07-03-SUMMARY.md` when done
</output>

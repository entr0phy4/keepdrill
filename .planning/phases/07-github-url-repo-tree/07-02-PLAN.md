---
phase: 07-github-url-repo-tree
plan: 02
type: tdd
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/github/client.ts
  - src/github/client.test.ts
autonomous: true
requirements: [REPO-01, REPO-04]

must_haves:
  truths:
    - "fetchRepoTree issues GET https://api.github.com/repos/{owner}/{repo} then GET .../git/trees/{encodeURIComponent(default_branch)}?recursive=1 (REPO-01)"
    - "Every fetch uses mode cors, credentials omit, and headers exactly { Accept: application/vnd.github+json } (carried-forward lock)"
    - "HTTP 404 maps to RepoNotFoundError (public missing and private share one class — D-14 / Pitfall 4)"
    - "HTTP 429, or HTTP 403 with x-ratelimit-remaining header equal to the character 0, maps to RateLimitedError with remaining and optional resetEpochS (REPO-04 boundary)"
    - "HTTP 403 with remaining not equal to 0 maps to GithubHttpError(403), not RateLimitedError (REPO-04 boundary)"
    - "HTTP 409 on the tree GET maps to EmptyRepoError with owner, repo, and defaultBranch copied from the already-successful GET /repos payload (API default_branch); fetchRepoTree does not return RepoTreeResult for 409"
    - "HTTP 200 with truncated true returns RepoTreeResult.truncated true and the returned tree[] — it does not throw (D-15, REPO-04)"
    - "A second fetchRepoTree for the same owner/repo after success performs no additional fetch (in-memory cache by owner/repo/sha plus defaultBranch index; D-cache / Pitfall 2)"
    - "Concurrent fetchRepoTree for the same owner/repo share one in-flight Promise (REPO-02 concurrency / Pitfall 2)"
    - "Rate-limit resetEpochS is Number(x-ratelimit-reset) seconds; missing header leaves resetEpochS undefined — UI clock is 07-03 (REPO-04 precision)"
    - "src/github/client.ts is the only production fetch( in src/ (platform seam)"
  prohibitions:
    - statement: "MUST NOT distinguish private-vs-missing in mapped errors — both are RepoNotFoundError"
      status: unverified
      verification: flagged
    - statement: "MUST NOT echo GitHub JSON message or client IP into thrown Error.message used as UI copy"
      status: unverified
      verification: flagged
  artifacts:
    - path: "src/github/client.ts"
      provides: "fetchRepoTree platform seam + in-flight/cache Maps"
      exports: ["fetchRepoTree", "resetGithubCache"]
    - path: "src/github/client.test.ts"
      provides: "vi.stubGlobal fetch fixtures; zero live api.github.com"
  key_links:
    - from: "src/github/client.ts"
      to: "src/github/errors.ts"
      via: "status mapping throws named classes"
      pattern: "RepoNotFoundError|RateLimitedError|EmptyRepoError|GithubHttpError"
    - from: "src/github/client.ts"
      to: "https://api.github.com"
      via: "githubGet concatenates origin + encoded path"
      pattern: "https://api.github.com"
---

<objective>
Implement the dirty GitHub seam: fetchRepoTree resolves the default branch,
loads one recursive tree, maps 404 / rate-limit / 409 / other HTTP / truncated
200, and caches so StrictMode or a second Import does not double-spend the
60 req/h budget (REPO-01, REPO-04, D-11 network happens only when called).

Purpose: TDD on HTTP mapping and cache — the two ways this phase burns the
user or lies about errors.

Output: `src/github/client.ts` + `client.test.ts` with a stubbed fetch.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-github-url-repo-tree/07-CONTEXT.md
@.planning/phases/07-github-url-repo-tree/07-RESEARCH.md
@.planning/phases/07-github-url-repo-tree/07-PATTERNS.md
@src/github/types.ts
@src/github/errors.ts
@src/github/tree.ts
@src/persistence/db.ts
@src/ingestion/upload.ts
@src/ingestion/upload.test.ts
</context>

## Artifacts this phase produces

Same inventory as 07-01; this plan adds:

- `fetchRepoTree(ref: RepoRef): Promise<RepoTreeResult>`
- `resetGithubCache(): void` — test-only reset of in-flight and result Maps
- private `githubGet(path: string): Promise<Response>` inside client.ts (the only fetch)

<feature>
  <name>fetchRepoTree</name>
  <files>src/github/client.ts, src/github/client.test.ts</files>
  <behavior>
    Two GETs on a cold import: repo then recursive tree. Cache hit: zero new
    GETs. 404 → RepoNotFoundError. 429 or 403+remaining 0 → RateLimitedError.
    Other 403 → GithubHttpError. Tree GET 409 → EmptyRepoError({ owner,
    repo, defaultBranch }) after GET /repos learned default_branch; do not
    return RepoTreeResult. truncated true stays on the result. Headers
    Accept-only. URLs always https://api.github.com/. Cases: see Task 1
    behavior list.
  </behavior>
  <implementation>
    Mirror persistence/db.ts as the sole dirty import. githubGet from
    07-PATTERNS.md / 07-RESEARCH.md Pattern 1. encodeURIComponent owner, repo,
    and default_branch. Always send recursive=1 (omit the query to stay
    non-recursive — this phase always sends it). In-flight Map keyed by
    owner/repo; success cache keyed by owner/repo@sha plus
    owner/repo:defaultBranch → sha. Do not wrap TypeError; UI maps
    unreachable copy. Export resetGithubCache for tests.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — stubbed fetch contract for fetchRepoTree</name>
  <files>src/github/client.test.ts</files>
  <read_first>
    - .planning/phases/07-github-url-repo-tree/07-PATTERNS.md (src/github/client.ts and client.test.ts)
    - src/ingestion/upload.test.ts (spy I/O, assert never ran on early reject)
    - src/github/types.ts
    - src/github/errors.ts
    - .planning/phases/07-github-url-repo-tree/07-RESEARCH.md (Fetch + error mapping, Vitest zero live GitHub)
  </read_first>
  <behavior>
    - Cold fetchRepoTree({ owner: 'o', repo: 'r' }) calls fetch twice; both URLs start with https://api.github.com/; second URL includes /git/trees/ and recursive=1; headers equal { Accept: 'application/vnd.github+json' }
    - After success, a second fetchRepoTree({ owner: 'o', repo: 'r' }) does not call fetch again (2 total, not 4)
    - Two overlapping cold calls share one in-flight pair (still 2 fetch invocations)
    - 404 → RepoNotFoundError
    - 403 with x-ratelimit-remaining '0' → RateLimitedError remaining 0
    - 429 → RateLimitedError
    - 403 with remaining '12' → GithubHttpError status 403
    - Tree GET 409 after repo 200 { default_branch: 'main' } for { owner: 'o', repo: 'r' } → EmptyRepoError with owner 'o', repo 'r', defaultBranch 'main'; no RepoTreeResult returned
    - 200 truncated true → result.truncated === true and entries length matches fixture tree[]
    - Branch name feature/foo is encoded in the tree URL
  </behavior>
  <action>
    Create src/github/client.test.ts in the unit project. beforeEach
    vi.stubGlobal('fetch', vi.fn()); afterEach vi.unstubAllGlobals() and
    resetGithubCache(). Helper jsonRes(body, status, headers) builds a
    Response-like object with .ok, .status, .headers.get, .json.

    Write the behavior cases so they fail until client.ts exists. Assert
    fetch.mock.calls length, URL prefix, search params, and headers object
    equality. Never call the real network. Import error classes from
    ./errors. Do not import RepoBrowser.
  </action>
  <verify>
    <automated>pnpm exec vitest run --project unit src/github/client.test.ts; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - client.test.ts exists and imports fetchRepoTree from ./client
    - First run of the file is RED (implementation missing or incomplete)
    - No test performs an un-stubbed network call
  </acceptance_criteria>
  <done>Failing contract tests exist for mapping, cache, and headers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — client.ts seam, mapping, cache (carried-forward fetch lock)</name>
  <files>src/github/client.ts, src/github/client.test.ts</files>
  <read_first>
    - .planning/phases/07-github-url-repo-tree/07-PATTERNS.md (client.ts githubGet block, cache keys, Pattern 1)
    - src/persistence/db.ts (sole dirty import file-lead)
    - src/ingestion/upload.ts (throw before/during I/O, never swallow)
    - src/github/types.ts
    - src/github/errors.ts
    - src/github/tree.ts (foldTree is optional here — client may return flat entries; UI folds)
    - .planning/phases/07-github-url-repo-tree/07-RESEARCH.md (readGithub mapping)
  </read_first>
  <behavior>
    - Same cases as Task 1 now pass
    - fetchRepoTree return shape is RepoTreeResult with owner, repo, defaultBranch, sha, entries, truncated
    - Tree GET 409 throws EmptyRepoError whose defaultBranch equals the GET /repos default_branch (no RepoTreeResult)
    - resetGithubCache empties both Maps so tests stay isolated
  </behavior>
  <action>
    Create src/github/client.ts as the only module in src/ that calls fetch
    (file-lead: platform seam, like db.ts). Export fetchRepoTree and
    resetGithubCache.

    githubGet(path) calls fetch('https://api.github.com' + path) with method
    GET, mode cors, credentials omit, headers { Accept: 'application/vnd.github+json' }
    only. Do not set auth, API-version, or User-Agent headers.

    encodeURIComponent owner, repo, and default_branch (slashes in refs).
    Tree path always includes ?recursive=1. Follow default redirect behavior
    for renamed repos (301).

    Map statuses per 07-RESEARCH.md readGithub: 404 → RepoNotFoundError;
    429 or (403 and remaining header === '0') → RateLimitedError; other
    non-OK → GithubHttpError(status). Truncated true is success. Do not
    throw on truncated. Do not wrap TypeError.

    HTTP 409 on the tree GET is EmptyRepoError, thrown only after GET
    /repos already returned default_branch: pass { owner: ref.owner,
    repo: ref.repo, defaultBranch } using that API string. Do not invent
    defaultBranch. Do not return RepoTreeResult for 409 — 07-03 treats
    instanceof EmptyRepoError as the empty-success path and captions from
    those three fields.

    Cache: in-flight Promise Map keyed `${owner}/${repo}` while running;
    after success store by `${owner}/${repo}@${sha}` and
    `${owner}/${repo}:${defaultBranch}` → sha so a later Import skips both
    GETs. resetGithubCache clears both.

    Return flat GitHub tree[] as RepoTreeResult.entries (UI calls foldTree).
    Do not add sourceType. Do not fetch blobs.

    GREEN: pnpm exec vitest run --project unit src/github/client.test.ts
    exits 0. Then confirm the only production fetch( in src/ lives here:
    rg -n 'fetch\\(' src --glob '*.ts' --glob '*.tsx' --glob '!*test*'
  </action>
  <verify>
    <automated>pnpm exec vitest run --project unit src/github/client.test.ts src/github/url.test.ts src/github/tree.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - client tests exit 0
    - fetchRepoTree is exported from client.ts
    - A cache-hit test expects fetch to have been called twice total for two sequential imports of the same ref
    - The 409 test asserts EmptyRepoError.owner, .repo, and .defaultBranch from the repo payload
    - Production src/ (excluding *.test.ts and *.test.tsx) has fetch( only in src/github/client.ts
  </acceptance_criteria>
  <done>Default-branch + recursive tree fetch is tested, cached, and typed-error mapped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client.ts → api.github.com | Unauthenticated CORS GET; response JSON and headers are untrusted |
| RepoRef → URL path | Owner/repo must be encoded; never interpolate a user URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-02 | Information disclosure | githubGet | high | mitigate | Construct https://api.github.com + encoded path only; never fetch the user-typed string |
| T-07-04 | Information disclosure | request headers | high | mitigate | Accept header only; no auth header; REPO-06 deferred |
| T-07-07 | Denial of service | cache / in-flight | medium | mitigate | Coalesce in-flight by owner/repo; cache by sha + defaultBranch index; Import is click/Enter only (D-11) |
| T-07-08 | Information disclosure | 404 vs private | medium | mitigate | Single RepoNotFoundError; do not probe further; do not parse GitHub message for UI |
| T-07-09 | Information disclosure | RateLimitedError | low | accept | remaining and resetEpochS are not secrets; do not put client IP from JSON into Error.message |
| T-07-01 | Spoofing | 301 follow | low | accept | fetch follows redirects only on api.github.com URLs we constructed |
| T-07-SC | Tampering | npm installs | low | accept | No packages added |
</threat_model>

<verification>
- pnpm exec vitest run --project unit src/github/client.test.ts
- Production fetch( only in client.ts
</verification>

<success_criteria>
- REPO-01 network path exists as fetchRepoTree
- REPO-04 status mapping is tested
- Cache prevents a double Import from issuing four GETs
</success_criteria>

<output>
Create `.planning/phases/07-github-url-repo-tree/07-02-SUMMARY.md` when done
</output>

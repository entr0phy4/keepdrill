---
phase: 03-session-metrics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/metrics/metrics.ts
  - src/metrics/metrics.test.ts
  - src/ui/ResultsView.tsx
  - src/ui/CaptureSurface.tsx
  - src/ui/CaptureSurface.test.tsx
  - src/ui/App.tsx
autonomous: true
requirements: [METR-01, METR-02, METR-04]
user_setup: []

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "On completion, the user sees net WPM computed as correctAttempts / 5 / activeMinutes, where activeMinutes comes from computeActiveElapsedMs(charLog, markers, completedAt) — never a wall-clock or Date.now()-based duration (D-01, METR-01)."
    - "On completion, the user sees accuracy computed as correctAttempts / (correctAttempts + incorrectAttempts), where both counts come from replaying every insert-branch charLog attempt against the target text — including attempts later overwritten by a backspace-and-retype — not from TrainerState.perCharStatus's collapsed history (D-02, METR-02)."
    - "src/metrics/metrics.ts is a pure module: zero DOM access, zero non-`import type` runtime imports besides a direct call to computeActiveElapsedMs, re-runnable over any {target, charLog, markers, now} tuple with no side effects (METR-04)."
    - "src/metrics/metrics.ts exposes a schemaVersion field (METRICS_SCHEMA_VERSION = 1) on every MetricsResult it returns (METR-04's versioned-schema requirement)."
    - "The moment TrainerState.completedAt transitions from null to non-null (surfaced via CaptureSurface's new onComplete callback, D-07), the results panel renders automatically, in the same render pass — no button, no separate 'view results' action, no additional delay beyond the current frame (D-05)."
    - "The trainer surface (CaptureSurface) remains mounted and visually unchanged in its completed state (per-character coloring, any still-incorrect positions, the caret) both above and after the results panel appears — it never hides, dims, or replaces the trainer surface."
    - "The existing single Restart button renders after the results panel in DOM order once results are visible; its click / Enter-Space-when-focused / Escape-while-capture-surface-focused behaviors are all unchanged (D-06) — no new confirmation step is added because results are now showing."
    - "Clicking Restart from the results view discards the just-computed metrics and unmounts the results panel, returning to the pre-typing trainer state exactly as Phase 2's handleRestart already does."
    - "The results panel container carries role=\"status\" aria-live=\"polite\", so its appearance and the stat values it contains are announced to assistive technology the instant it renders."
    - { statement: "If computeActiveElapsedMs ever returns 0 at completion, the WPM stat renders '0 wpm' — never 'Infinity wpm' or 'NaN wpm'.", verification: backstop }
    - "METR-01's WPM formula counts correct characters using JS code-point iteration (Array.from(rec.data ?? '')), matching Phase 2's own state.ts convention — not UTF-16 code units or grapheme clusters."
    - "An exercise whose target text is empty never reaches the results panel: computeTrainerState only sets completedAt inside its charLog fold, so a zero-length target with an empty charLog never transitions completedAt to non-null (existing Phase 2 behavior, unmodified by this phase) — the WPM/accuracy 'empty input' case is therefore unreachable via the UI, not a case metrics.ts needs to special-case beyond its own zero-attempt/zero-elapsed guards."
    - "METR-02's accuracy denominator boundary: zero total attempts returns 1 (100%) per computeAccuracy's guard — a pure-function safety default, since the UI can never actually reach the results panel with zero attempts (completedAt requires cursor >= target.length, which requires at least one insert-branch attempt for any non-empty target)."
    - "Every number returned by computeSessionMetrics (wpm, accuracy) is unrounded; Math.round() is applied only inside ResultsView.tsx at the moment of formatting a stat value for display, never inside metrics.ts (Pitfall 5)."
    - "computeSessionMetrics has no shared mutable module state and is safe to call repeatedly: CaptureSurface's onComplete fires at most once per distinct completedAt value (a ref-guarded useEffect, D-07), so App.tsx's handleComplete is never invoked twice for the same completion, and computeSessionMetrics itself is a pure function with no reentrancy hazard (METR-04's re-runnable requirement)."
  artifacts:
    - path: "src/metrics/metrics.ts"
      provides: "computeSessionMetrics/computeWpm/computeAccuracy pure functions + MetricsResult type + METRICS_SCHEMA_VERSION constant (METR-01/02/04)"
      contains: "computeSessionMetrics"
    - path: "src/metrics/metrics.test.ts"
      provides: "golden-case tests locking D-01/D-02 formulas and Pitfalls 1/2/3/6"
      contains: "computeSessionMetrics"
    - path: "src/ui/ResultsView.tsx"
      provides: "auto-revealed results panel showing WPM + accuracy stats"
      contains: "ResultsView"
    - path: "src/ui/CaptureSurface.tsx"
      provides: "onComplete callback prop fired once per completedAt transition (D-07)"
      contains: "onComplete"
    - path: "src/ui/App.tsx"
      provides: "handleComplete wiring + metrics state + ResultsView mount gate + metrics reset on load/restart"
      contains: "handleComplete"
  key_links:
    - from: "src/ui/CaptureSurface.tsx"
      to: "src/ui/App.tsx"
      via: "onComplete callback fired once per completedAt transition (D-07)"
      pattern: "onComplete\\?\\.\\(completedAt\\)"
    - from: "src/ui/App.tsx"
      to: "src/metrics/metrics.ts"
      via: "handleComplete calls computeSessionMetrics(text, session.charLog, session.markers, completedAt)"
      pattern: "computeSessionMetrics\\("
    - from: "src/ui/App.tsx"
      to: "src/ui/ResultsView.tsx"
      via: "conditional render gated on metrics !== null"
      pattern: "metrics !== null"
    - from: "src/metrics/metrics.ts"
      to: "src/trainer/active-time.ts"
      via: "computeSessionMetrics calls computeActiveElapsedMs for the WPM/accuracy time basis (D-01)"
      pattern: "computeActiveElapsedMs\\("
  prohibitions:
    - "Never render WPM/accuracy with performance-threshold color-coding (no good/bad green/red banding on these numbers) — explicit locked UI-SPEC rule against gamification framing."
    - "Never add a celebratory/gamified framing (confetti, streak counters, exclamation-point copy, pass/fail bands) to the results panel — explicitly out of scope per REQUIREMENTS.md's Out of Scope table."
    - "Never persist computed metrics anywhere (no IndexedDB, no localStorage, no network call) — PERS-01/02 is explicitly v2; metrics are computed live and discarded on restart (D-06)."
---

<objective>
Build the pure metrics core (`computeSessionMetrics`, D-01/D-02) and wire it, end-to-end,
into a new auto-revealed results panel: `CaptureSurface` gains a completion callback (D-07),
`App.tsx` computes a fresh `MetricsResult` the instant the exercise completes, and
`ResultsView` shows net WPM and accuracy.

Purpose: this is the tracer for Phase 3 — the thinnest path that proves the whole
architecture (completion signal -> pure fold over the capture log -> auto-revealed display)
before the slowest-keys feature and full visual polish expand around it in 03-02.

Output: `src/metrics/metrics.ts` (new pure module), `src/ui/ResultsView.tsx` (new component),
`CaptureSurface.tsx` and `App.tsx` updated to detect completion and display results.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-session-metrics/03-CONTEXT.md
@.planning/phases/03-session-metrics/03-RESEARCH.md
@.planning/phases/03-session-metrics/03-UI-SPEC.md
@.planning/phases/03-session-metrics/03-PATTERNS.md

<interfaces>
<!-- Current on-disk shape (read this session) — extend, don't re-derive. -->

src/capture/types.ts (Phase 1, unchanged by this plan):
  export interface CommittedChar { seq: number; inputType: string; data: string | null; tMs: number }
  export type MarkerKind = 'blur' | 'focus' | 'hidden' | 'visible'
  export interface CaptureMarker { seq: number; kind: MarkerKind; tMs: number }
  export interface Session { exercise: Exercise; events: readonly KeystrokeEvent[]; charLog: readonly CommittedChar[]; markers: readonly CaptureMarker[]; timingResolutionUs: number; crossOriginIsolated: boolean; startedAt: number /* Date.now(), display-only — never use as `now` */ }

src/trainer/state.ts (Phase 2, unchanged by this plan):
  export interface TrainerState { perCharStatus: PerCharStatus[]; cursor: number; correctedCount: number; uncorrectedCount: number; completedAt: number | null }
  export function computeTrainerState(target: string, charLog: readonly CommittedChar[]): TrainerState
  export function glyphFor(char: string): string  // ' ' -> '·', '\n' -> '↵', else unchanged

src/trainer/active-time.ts (Phase 2, unchanged by this plan):
  export function computeActiveElapsedMs(charLog: readonly CommittedChar[], markers: readonly CaptureMarker[], now: number): number
  // guard-and-return-default convention: charLog.length === 0 -> 0; never throws.

src/session.ts (Phase 1, unchanged by this plan):
  export function buildSession(exercise: Exercise, startedAt: number): Session
  // CR-01: a LIVE snapshot — call fresh at completion time, never read a cached sessionRef.

src/capture/capture.ts (Phase 1, unchanged by this plan):
  export function getCharLog(): readonly CommittedChar[]
  export function getMarkers(): readonly CaptureMarker[]
  export function resetCapture(): void

src/ui/CaptureSurface.tsx (Phase 2, CURRENT props before this plan):
  export function CaptureSurface({ text, onRestartRequested }: { text: string; onRestartRequested?: () => void })
  // computeTrainerState(text, getCharLog()) is called once per render; currently destructures
  // only { perCharStatus, cursor } — this plan additionally destructures `completedAt`.

src/ui/App.tsx (Phase 2, CURRENT shape before this plan):
  const [exercise, setExercise] = useState<Exercise | null>(null)
  const loadRef = useRef<{ exercise: Exercise; startedAt: number } | null>(null)
  const handleLoad = (loaded: Exercise) => { resetCapture(); setExercise(loaded); ...; buildSession(...) }
  const handleRestart = () => { resetCapture(); setLoadToken(t => t+1); ...; buildSession(...) }
  // <CaptureSurface key={loadToken} text={exercise.text} onRestartRequested={handleRestart} />
  // <button ...>Restart exercise</button>

src/ui/Banners.tsx (Phase 1, unchanged — the role="status" prop-driven display convention this
  plan's ResultsView mirrors):
  export function Banners({ crossOriginIsolated, timingResolutionUs }: BannersProps)
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — completion signal to WPM + accuracy, auto-revealed</name>
  <precondition>src/trainer/state.ts exports computeTrainerState returning { completedAt }, and src/trainer/active-time.ts exports computeActiveElapsedMs(charLog, markers, now) (Phase 2, complete)</precondition>
  <reversibility rating="costly">D-01 fixes WPM's time basis to computeActiveElapsedMs's active-elapsed clock domain; every later displayed WPM number is defined relative to this basis, so switching to wall-clock time later would change the meaning of every historical number, not just the code path.</reversibility>
  <files>src/metrics/metrics.ts, src/metrics/metrics.test.ts, src/ui/CaptureSurface.tsx, src/ui/App.tsx, src/ui/ResultsView.tsx, src/ui/CaptureSurface.test.tsx</files>
  <read_first>
    - src/trainer/state.ts (computeTrainerState's delete/insert reducer shape to mirror; the `completedAt` field)
    - src/trainer/active-time.ts (computeActiveElapsedMs's signature + guard-and-return-default convention)
    - src/capture/types.ts (CommittedChar/CaptureMarker/Session shapes; Session.startedAt is Date.now()-based and display-only — never use it as `now`)
    - src/session.ts (buildSession's CR-01 "fresh snapshot, not a cached one" comment)
    - src/ui/CaptureSurface.tsx (current props { text, onRestartRequested }; the computeTrainerState call site destructuring { perCharStatus, cursor }; the existing one-effect-per-concern style; the existing onRestartRequested prop pattern to mirror for onComplete)
    - src/ui/App.tsx (handleLoad/handleRestart/loadRef pattern; the exercise===null conditional render block; the SESSION_REFRESH_MS 250ms interval — explicitly NOT the mechanism for completion detection, per RESEARCH.md Pitfall 6)
    - src/ui/Banners.tsx (role="status" prop-driven display component convention to mirror for ResultsView)
    - src/ui/CaptureSurface.test.tsx (trustedInputEvent/beforeInput/keyDown helpers; the RestartHarness precedent; the nextFrame() rAF-tick waiter)
  </read_first>
  <action>
    Create `src/metrics/metrics.ts` as a pure module (zero DOM access, zero non-`import type`
    runtime imports besides a direct call to `computeActiveElapsedMs`). Open with a header
    comment stating purity, citing D-01/D-02, naming `metrics.test.ts` as the locking test
    file, and listing three "do NOT" items: do not read `TrainerState.perCharStatus` for
    accuracy (D-02 — it collapses corrected-over history into one final state per position);
    do not pass `Session.startedAt` or a fresh timestamp read as `now` (Pitfall 1 — wrong
    clock domain vs. charLog/markers' event.timeStamp-based tMs values); do not round any
    number inside this module (Pitfall 5 — round only at the display layer).

    Export `METRICS_SCHEMA_VERSION = 1` and `interface MetricsResult { schemaVersion: number; wpm: number; accuracy: number }`.

    Write a private `replayAttempts(target: string, charLog: readonly CommittedChar[])`
    function mirroring computeTrainerState's delete/insert branching exactly: a delete-type
    record (`rec.inputType.startsWith('delete')`) moves `cursor` back one position (floored
    at 0) and is never itself counted as an attempt; an insert-branch record iterates
    `rec.data ?? ''` per JS code point (`for...of`, not UTF-16 units), comparing each code
    point against `target[cursor]` and incrementing `correctAttempts` or `incorrectAttempts`,
    then advancing `cursor`. Return `{ correctAttempts, incorrectAttempts }`.

    Export `computeWpm(correctChars: number, elapsedMs: number): number` — guard
    `elapsedMs <= 0` to return `0` (Pitfall 2), else `correctChars / 5 / (elapsedMs / 60000)`.

    Export `computeAccuracy(correctAttempts: number, totalAttempts: number): number` — guard
    `totalAttempts === 0` to return `1`, else `correctAttempts / totalAttempts`.

    Export `computeSessionMetrics(target: string, charLog: readonly CommittedChar[], markers: readonly CaptureMarker[], now: number): MetricsResult`
    that computes `elapsedMs = computeActiveElapsedMs(charLog, markers, now)` (D-01), calls
    `replayAttempts(target, charLog)`, then returns
    `{ schemaVersion: METRICS_SCHEMA_VERSION, wpm: computeWpm(correctAttempts, elapsedMs), accuracy: computeAccuracy(correctAttempts, correctAttempts + incorrectAttempts) }`.

    Create `src/ui/ResultsView.tsx` exporting `interface ResultsViewProps { metrics: MetricsResult }`
    and `function ResultsView({ metrics }: ResultsViewProps)`, returning a
    `<section className="results-panel" role="status" aria-live="polite">` containing an
    `<h2>Results</h2>` and a `<div className="results-stats">` with two
    `<div className="results-stat">` blocks — one showing `{Math.round(metrics.wpm)} wpm`
    with a `wpm` label span, one showing `{Math.round(metrics.accuracy * 100)}%` with an
    `accuracy` label span. Render every value as a plain JSX text child, matching
    `CaptureSurface.tsx`'s existing `{targetChar}` convention (the character corpus is
    untrusted per Phase 1's boundary — RESEARCH.md's V5 ASVS finding).

    Modify `src/ui/CaptureSurface.tsx`: add `onComplete?: (completedAt: number) => void` to
    the destructured props type, alongside the existing `onRestartRequested`; destructure
    `completedAt` from the existing `computeTrainerState(text, getCharLog())` call result
    (currently only destructures `{ perCharStatus, cursor }`); add
    `const firedCompletedAtRef = useRef<number | null>(null)` near the other refs; add a new
    `useEffect(() => { if (completedAt !== null && firedCompletedAtRef.current !== completedAt) { firedCompletedAtRef.current = completedAt; onComplete?.(completedAt) } }, [completedAt, onComplete])`,
    placed after the existing visibilitychange effect, mirroring this component's
    one-effect-per-concern style.

    Modify `src/ui/App.tsx`: import `computeSessionMetrics` and the `MetricsResult` type from
    `'../metrics/metrics'`, and `ResultsView` from `'./ResultsView'`. Add
    `const [metrics, setMetrics] = useState<MetricsResult | null>(null)`. Add
    `handleComplete(completedAt: number)`: read `loadRef.current`, return early if `null`,
    call `buildSession(current.exercise, current.startedAt)` fresh (per session.ts's CR-01
    comment — never read the cached `sessionRef.current`), then call
    `computeSessionMetrics(current.exercise.text, session.charLog, session.markers, completedAt)`,
    and `setMetrics(result)`. Add `setMetrics(null)` inside both `handleLoad` and
    `handleRestart` (so a fresh load or a restart never leaves a stale results panel showing
    the previous session's numbers — D-06's "no stale metrics survive" rule extended to a
    fresh load for the identical reason). Pass `onComplete={handleComplete}` to the
    `<CaptureSurface>` element, and insert `{metrics !== null && <ResultsView metrics={metrics} />}`
    between the `<CaptureSurface>` element and the "Restart exercise" button.

    Extend `src/ui/CaptureSurface.test.tsx`: add a `beforeInputAt(el, init, tMs)` helper that
    builds on the existing `beforeInput`/`trustedInputEvent` helpers, additionally calling
    `Object.defineProperty(evt, 'timeStamp', { value: tMs, configurable: true })` before
    dispatch (mirroring `capture.test.ts`'s existing timeStamp-override convention) so
    keystroke timing is deterministic in tests. Add a `MetricsHarness({ text }: { text: string })`
    component (mirroring the existing `RestartHarness` precedent in this file) rendering
    `<CaptureSurface text={text} onComplete={handleComplete} />` plus
    `{metrics !== null && <ResultsView metrics={metrics} />}`, where `handleComplete` calls
    `computeSessionMetrics(text, getCharLog(), getMarkers(), completedAt)` and stores the
    result in a local `useState`. Add a new `describe` block with two tests:
    (1) render `<MetricsHarness text="ab" />`, dispatch
    `beforeInputAt(textarea, { inputType: 'insertText', data: 'a' }, 0)`, wait a frame,
    dispatch `beforeInputAt(textarea, { inputType: 'insertText', data: 'b' }, 600)`, wait a
    frame, then assert `container.querySelector('.results-panel')!.textContent` contains
    `'40 wpm'` (elapsedMs=600ms -> 0.01 active minutes -> 2 correct chars / 5 / 0.01 = 40)
    and contains `'100%'`; (2) reusing the same scenario, add a call counter inside the
    harness's `handleComplete` and assert it was invoked exactly once even after one further
    post-completion animation-frame tick, proving D-07's fire-once-per-`completedAt` guard.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/ui/CaptureSurface.test.tsx && pnpm exec tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function computeSessionMetrics" src/metrics/metrics.ts` equals 1
    - `grep -c "export function ResultsView" src/ui/ResultsView.tsx` equals 1
    - `grep -v '^\s*//' src/ui/ResultsView.tsx | grep -c 'dangerouslySetInnerHTML'` equals 0 (never inject raw markup — plain JSX text children only)
    - `grep -c "onComplete" src/ui/CaptureSurface.tsx` is greater than or equal to 2 (prop declaration + effect call site)
    - `pnpm exec vitest run src/ui/CaptureSurface.test.tsx` passes, including the new tracer end-to-end describe block
  </acceptance_criteria>
  <done>Typing "ab" to completion in the harness reveals a `.results-panel` showing "40 wpm" and "100%", `onComplete` fires exactly once per completion, and `pnpm exec tsc --noEmit -p tsconfig.json` reports zero errors.</done>
</task>

<task type="auto">
  <name>Task 2: Golden-case hardening — D-01/D-02 formulas + Pitfalls 1/2/3/6</name>
  <files>src/metrics/metrics.test.ts, src/metrics/metrics.ts</files>
  <read_first>
    - src/metrics/metrics.ts (Task 1's computeSessionMetrics/computeWpm/computeAccuracy signatures and header comment)
    - src/trainer/active-time.test.ts (Case/it.each convention + the char()/marker() fixture builders to reuse verbatim)
    - src/trainer/state.test.ts (Case/it.each convention; the char(seq, inputType, data, tMs) fixture builder; case 7's composed-commit fixture shape)
  </read_first>
  <action>
    Extend `src/metrics/metrics.test.ts` with a `Case`/`it.each` golden-case table (matching
    `state.test.ts`'s convention) exercising `computeSessionMetrics` directly, covering at
    least these six cases:
    1. Zero-elapsed-time guard (Pitfall 2): target `"a"`, charLog `[{ seq:0, inputType:'insertText', data:'a', tMs:50 }]`, `now=50` -> `wpm` is exactly `0` (not `Infinity`/`NaN`), `accuracy` is exactly `1`.
    2. D-02's corrected-position denominator + Pitfall 3 (delete records excluded from the
       denominator): target `"a"`, charLog `[{seq:0,inputType:'insertText',data:'x',tMs:0}, {seq:1,inputType:'deleteContentBackward',data:null,tMs:10}, {seq:2,inputType:'insertText',data:'a',tMs:20}]`,
       `now=20` -> `accuracy` is exactly `0.5` (1 correct + 1 incorrect = 2 total attempts; the
       delete record itself contributes no third attempt).
    3. Clock-domain fidelity (Pitfall 1 / RESEARCH Pattern 4): target `"a"`, charLog
       `[{seq:0,inputType:'insertText',data:'a',tMs:0}]`, `now=500` -> `wpm` is exactly `24`
       (elapsedMs=500 matches active-time.test.ts's own case 2 numbers; 1 correct char / 5 /
       (500/60000) = 24), proving `now` is used literally with no internal wall-clock
       substitution.
    4. Active-time/marker integration: reuse a local `marker(kind, tMs)` fixture builder
       matching `active-time.test.ts`'s exact shape; target `"a"`, charLog
       `[{seq:0,inputType:'insertText',data:'a',tMs:0}]`, markers
       `[marker('blur', 100), marker('focus', 200)]`, `now=500` -> `wpm` is exactly `30`
       (elapsedMs=400 matches active-time.test.ts's own case 3 numbers; 1/5/(400/60000)=30).
    5. Multi-codepoint (IME) commit encoding: target `"ab"`, charLog
       `[{seq:0,inputType:'insertFromComposition',data:'ab',tMs:10}]`, `now=10` -> `accuracy`
       is exactly `1` (both code points scored correctly in one record), matching
       `state.test.ts` case 7's composed-commit fixture shape.
    6. Zero-attempt safety default: target `"a"`, charLog `[]`, `now=0` -> `accuracy` is
       exactly `1` and `wpm` is exactly `0` (both guards trigger together on an empty log).

    Finalize the WPM and accuracy formula documentation in `metrics.ts`'s header comment (if
    not already complete from Task 1) with the exact expressions:
    `wpm = correctAttempts / 5 / (elapsedMs / 60000)` and
    `accuracy = correctAttempts / (correctAttempts + incorrectAttempts)`.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/metrics/metrics.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec vitest run src/metrics/metrics.test.ts` passes with at least 6 golden cases covering Pitfalls 1/2/3/6 and D-02
    - `grep -c "wpm = correctAttempts" src/metrics/metrics.ts` is greater than or equal to 1 (formula documented in the header comment)
  </acceptance_criteria>
  <done>metrics.test.ts's golden-case table covers Pitfalls 1/2/3/6 and D-02's corrected-denominator behavior; all cases pass; the header comment documents both formulas verbatim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Pasted/uploaded corpus text -> DOM | Exercise content (Phase 1) is untrusted user/third-party input; this phase renders characters derived from it (the target text, via ResultsView's future slowest-key chip in 03-02) into the DOM |
| Completion signal -> metrics computation | `completedAt` is derived purely from an internal state fold (computeTrainerState), never a raw event the caller could forge |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering / Information Disclosure | src/ui/ResultsView.tsx (any corpus-derived character it renders) | high | mitigate | Render every user-derived character strictly as a plain JSX text child (`{value}`); never inject raw markup into the DOM — matches CaptureSurface.tsx's existing `{targetChar}` convention (RESEARCH.md's V5 ASVS finding). Verified by a filtered negative grep in Task 1's acceptance criteria. |
| T-03-02 | Denial of Service | src/metrics/metrics.ts `computeWpm` | low | mitigate | Guard `elapsedMs <= 0` -> return `0` (Pitfall 2) so a degenerate active-time never renders `Infinity`/`NaN` to the results panel. |
| T-03-03 | Tampering | src/ui/CaptureSurface.tsx `onComplete` bridge | low | accept | `completedAt` is derived purely from internal state (computeTrainerState's fold over charLog), never a raw DOM event dispatch — this phase adds no new keydown handling, so no new `isTrusted` gate is needed. |

No `T-03-SC` row — this phase installs no npm/pip/cargo packages (03-RESEARCH.md's own Package Legitimacy Audit: not applicable).
</threat_model>

<verification>
- `pnpm exec vitest run` passes for the whole suite (no regressions in Phase 1/2 tests).
- `pnpm exec tsc --noEmit -p tsconfig.json` reports zero errors.
- Manually confirmable: loading "ab", typing it to completion, immediately shows a Results panel with WPM + accuracy, and Restart still works from that view.
</verification>

<success_criteria>
- Typing an exercise to completion automatically reveals net WPM and accuracy, computed per D-01/D-02's exact formulas, with no stale data surviving a restart or a fresh load.
- `src/metrics/metrics.ts` is a pure, versioned, golden-tested module satisfying METR-04's re-runnable/no-I/O requirement for the WPM and accuracy formulas (slowest-5 completes in 03-02).
</success_criteria>

<output>
Create `.planning/phases/03-session-metrics/03-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces (consolidated — spans 03-01 and 03-02)

- `src/metrics/metrics.ts`: `METRICS_SCHEMA_VERSION`, `MetricsResult` (interface), `SlowestKeyEntry`
  (interface, added in 03-02), `computeWpm`, `computeAccuracy`, `computeSessionMetrics` (all
  exported, established in 03-01); private `replayAttempts` (established in 03-01, extended in
  03-02 to also track per-character latency samples), private `median`/`slowestFive` (added in
  03-02).
- `src/ui/ResultsView.tsx`: `ResultsViewProps` (interface), `ResultsView` (component) — WPM +
  accuracy stats established in 03-01; the slowest-keys list / "not enough data" fallback added
  in 03-02.
- `src/ui/CaptureSurface.tsx`: new `onComplete?: (completedAt: number) => void` prop and internal
  `firedCompletedAtRef` — established in 03-01, unchanged by 03-02.
- `src/ui/App.tsx`: new `metrics` state, new `handleComplete` function, the `ResultsView` mount
  site, and `setMetrics(null)` resets in `handleLoad`/`handleRestart` — established in 03-01,
  unchanged by 03-02.
- `src/index.css`: new `.results-panel`, `.results-stats`, `.results-stat`, `.results-slowest`,
  `.results-slowest-list`, `.results-slowest-row`, `.key-chip` classes + `results-fade-in`
  keyframes — added in 03-02.

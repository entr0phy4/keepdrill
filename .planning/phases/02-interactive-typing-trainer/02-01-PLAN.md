---
phase: 02-interactive-typing-trainer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/trainer/state.ts
  - src/trainer/state.test.ts
  - src/ui/CaptureSurface.tsx
  - src/ui/CaptureSurface.test.tsx
  - src/ui/App.tsx
  - src/index.css
  - vite.config.ts
autonomous: true
requirements: [TYPE-01, TYPE-02, TYPE-03, TYPE-04]
user_setup: []

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Before the first keystroke, every target character renders as pending (muted text, no background/underline) and the custom caret sits before index 0 (TYPE-01)."
    - "While typing, each committed character immediately re-renders as correct or incorrect per the per-status rule, and the custom caret advances to the new cursor position (TYPE-01)."
    - "Advancing past a mistyped character is never blocked (free-correction, TYPE-02): the mistyped character keeps its incorrect styling (red text + tint + 2px underline) after the cursor has moved further ahead, until backspace returns to it."
    - "Backspace (TYPE-03) reopens exactly the previous position to pending (muted, no background) — it does not retain a 'previously wrong' ghost mark once re-typed."
    - "After backspacing and retyping a position correctly, it renders with plain correct styling — no 'this was corrected' visual badge (that distinction is Phase 3 accuracy data, not a Phase 2 visual state)."
    - "On completion (cursor reaches text.length), any remaining incorrect positions keep full incorrect styling — completion is never conflated with all-correct; nothing auto-clears or grays out uncorrected errors (D-04)."
    - "The invisible <textarea>'s native caret is never visible at any point (caret-color: transparent at all times) — the custom caret is the only cursor indicator a user ever sees."
    - "Space renders as a muted middle dot (·, U+00B7) and newline renders as a muted down-arrow (↵, U+21B5) followed by the real line break — glyphs overlay the correct/incorrect palette rather than replacing it (TYPE-04, D-06 amended: no tab glyph, since Exercise.text can never contain a literal tab)."
    - { statement: "A long unbroken line (no whitespace) wraps via overflow-wrap: anywhere in both the rendered layer and the invisible textarea identically, without ever requiring horizontal scroll.", verification: backstop }
    - "Exercise.text is never empty: CorpusInput.tsx blocks whitespace-only submission (value.trim() === '' guard) and normalize() always appends exactly one trailing newline for non-empty input, so target.length is always >= 1 — computeTrainerState never needs an empty-target code path."
    - "Per-character comparison operates on JS code points (via for...of over CommittedChar.data), not UTF-16 code units or grapheme clusters, matching Phase 1 normalize.ts's 'do not Unicode-normalize' policy (D-09, Phase 1) — a decomposed character is compared/typed position-by-position, not folded."
  artifacts:
    - path: "src/trainer/state.ts"
      provides: "computeTrainerState pure reducer (D-03/D-04/D-05), TrainerState/PerCharStatus types, glyphFor whitespace-glyph mapping"
      contains: "computeTrainerState"
    - path: "src/trainer/state.test.ts"
      provides: "golden-case suite locking free-correction, backspace, IME-composed-commit, and completion behavior"
      contains: "computeTrainerState"
    - path: "src/ui/CaptureSurface.tsx"
      provides: "transparent-textarea-over-rendered-layer overlay (D-01/D-02), per-character coloring, custom caret, caret resync (D-10)"
      contains: "trainer-caret"
    - path: "src/ui/CaptureSurface.test.tsx"
      provides: "React-DOM + happy-dom end-to-end render test dispatching trusted beforeinput events"
      contains: "createRoot"
  key_links:
    - from: "src/ui/CaptureSurface.tsx"
      to: "src/trainer/state.ts"
      via: "computeTrainerState(text, getCharLog()) recomputed on every charLog-length tick"
      pattern: "computeTrainerState\\("
    - from: "src/ui/App.tsx"
      to: "src/ui/CaptureSurface.tsx"
      via: "text={exercise.text} prop replaces the old separate exercise preview block"
      pattern: "text=\\{exercise\\.text\\}"
---

<objective>
Build the pure trainer-state reducer (`computeTrainerState`, D-03/D-04/D-05) and wire it,
end-to-end, into a transparent-textarea-over-rendered-layer overlay (D-01/D-02) that shows
live per-character correctness coloring, a custom caret, and whitespace glyphs (D-06 amended).

Purpose: this is the tracer for the whole phase — the thinnest path that proves the
architecture (pure-core fold + DOM-coupled overlay render, wired through Phase 1's existing
`capture.ts` without touching its hot path) before Restart/Tab/active-time expand around it.

Output: `src/trainer/state.ts` (new pure module), a rebuilt `CaptureSurface.tsx` that is the
sole renderer of the loaded exercise, and `App.tsx` updated to feed it `exercise.text` instead
of a separate read-only preview block.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-interactive-typing-trainer/02-CONTEXT.md
@.planning/phases/02-interactive-typing-trainer/02-RESEARCH.md
@.planning/phases/02-interactive-typing-trainer/02-UI-SPEC.md
@.planning/phases/02-interactive-typing-trainer/02-PATTERNS.md
@.planning/phases/01-corpus-input-keystroke-capture/01-03-SUMMARY.md

<interfaces>
<!-- Current on-disk shape (read this session) — extend, don't re-derive. -->

src/capture/types.ts (Phase 1, unchanged):
  export interface CommittedChar { seq: number; inputType: string; data: string | null; tMs: number }
  export type MarkerKind = 'blur' | 'focus' | 'hidden' | 'visible';
  export interface CaptureMarker { seq: number; kind: MarkerKind; tMs: number }

src/capture/capture.ts (Phase 1, unchanged by this plan):
  export function getCharLog(): readonly CommittedChar[];
  export function getMarkers(): readonly CaptureMarker[];
  export function resetCapture(): void;
  export function attachCapture(target: HTMLElement): void;
  export function detachCapture(): void;

src/capture/use-capture.ts (Phase 1, unchanged by this plan):
  export function useCapture(target: RefObject<HTMLElement|null>, onPasteBlocked?: () => void): { count: number }
  // useSyncExternalStore + setInterval(250ms) subscribe — a proven pattern this plan mirrors
  // with a per-frame (requestAnimationFrame) subscribe instead, for the trainer's own re-render.

src/ingestion/types.ts (Phase 1, unchanged):
  export interface Exercise { text: string; language: string; sourceType: 'paste'|'upload'; sourceRef?: string }

Contract this plan ADDS (src/trainer/state.ts):
  export type PerCharStatus = 'correct' | 'incorrect' | 'pending';
  export interface TrainerState {
    perCharStatus: PerCharStatus[]; cursor: number;
    correctedCount: number; uncorrectedCount: number; completedAt: number | null;
  }
  export function computeTrainerState(target: string, charLog: readonly CommittedChar[]): TrainerState;
  export function glyphFor(char: string): string;  // added in Task 2

Contract this plan CHANGES (src/ui/CaptureSurface.tsx):
  export function CaptureSurface(props: { text: string }): JSX.Element  // was: no props
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — trainer reducer + transparent overlay, one happy path end-to-end</name>
  <files>src/trainer/state.ts, src/trainer/state.test.ts, src/ui/CaptureSurface.tsx, src/ui/CaptureSurface.test.tsx, src/ui/App.tsx, src/index.css, vite.config.ts</files>
  <read_first>src/ingestion/normalize.ts, src/ingestion/normalize.test.ts, src/capture/types.ts, src/capture/capture.ts, src/capture/capture.test.ts, src/capture/use-capture.ts, src/ui/CaptureSurface.tsx, src/ui/App.tsx, src/index.css, vite.config.ts, .planning/phases/02-interactive-typing-trainer/02-CONTEXT.md, .planning/phases/02-interactive-typing-trainer/02-UI-SPEC.md</read_first>
  <reversibility rating="costly">D-01's transparent-textarea-over-rendered-layer pattern is the seam Phase 3's results panel and any later visual work build on (per CONTEXT.md D-01's own rationale) — not gated (already locked by the user), but flagged for the record.</reversibility>
  <behavior>
    - computeTrainerState('ab', []) -> { perCharStatus: ['pending','pending'], cursor: 0, correctedCount: 0, uncorrectedCount: 0, completedAt: null }
    - computeTrainerState('ab', [{seq:0,inputType:'insertText',data:'a',tMs:10}]) -> perCharStatus[0]==='correct', cursor===1
    - a commit of the wrong character at cursor 0 (target 'a', data 'x') -> perCharStatus[0]==='incorrect', cursor===1 (free-correction: cursor still advances, D-04)
    - one delete-type record (inputType starting with 'delete') after the wrong commit -> cursor back to 0, perCharStatus[0] reset to 'pending' (D-05)
    - retyping the correct character after that backspace -> perCharStatus[0]==='correct', correctedCount===1 (was ever wrong AND now correct)
    - cursor reaching target.length -> completedAt equals the tMs of the record that closed it
  </behavior>
  <action>
    Create `src/trainer/state.ts` as a PURE module — only `import type { CommittedChar } from '../capture/types'`, zero runtime imports, zero DOM access — exporting `PerCharStatus`, `TrainerState`, and `computeTrainerState(target, charLog)` per the `<interfaces>` shape above. Implement D-03/D-04/D-05 exactly per 02-RESEARCH.md Architecture Patterns Pattern 3: iterate `charLog` once; for any `inputType` starting with `'delete'`, decrement `cursor` by exactly one (guarded at 0) and reset that position's `perCharStatus` entry to `'pending'` without touching whether it was ever wrong (D-11's documented one-position-back limitation — do not attempt to infer a multi-character delete length from `data`, which is always `null` for delete* records); otherwise iterate `rec.data ?? ''` per JS code point via `for...of` (correct for IME multi-char commits), comparing each code point to `target[cursor]`, marking `perCharStatus[cursor]` `'correct'` or `'incorrect'` (recording `wasEverWrong[cursor] = true` on the first mismatch) and incrementing `cursor`, stopping the inner loop once `cursor >= target.length`; set `completedAt` to the current record's `tMs` the first time `cursor >= target.length` is reached, and never overwrite it afterward. After the loop, tally `correctedCount` (positions now `'correct'` that were ever wrong) and `uncorrectedCount` (positions currently `'incorrect'`). Add a top-of-file header comment following `normalize.ts`'s convention: purity statement, "D-03/D-04/D-05/D-11", the test file that locks it, and an explicit "do NOT" list (do not read `textarea.selectionStart` here; do not infer multi-char delete length from `data`).

    Add four new CSS custom properties to `src/index.css`'s `:root` block and its `@media (prefers-color-scheme: dark)` block, per 02-UI-SPEC.md's Color contract: `--color-correct` (`#1c1c1a` light / `#e6edf3` dark — aliases `--color-text`), `--color-pending` (`#5c5c57` light / `#8b949e` dark — aliases `--color-text-muted`), `--color-incorrect` (`#c0362c` light / `#f85149` dark — aliases `--color-destructive`), `--color-incorrect-bg` (`rgba(192,54,44,0.14)` light / `rgba(248,81,73,0.18)` dark). Add a shared rule (e.g. `.trainer-stack > *`) carrying, identically on both stacked children (D-02): `font-family: var(--font-mono)`, `font-size: var(--text-body-size)`, `line-height: var(--text-body-line)`, `letter-spacing: normal`, `padding: var(--space-md)`, `border: 1px solid transparent`, `white-space: pre-wrap`, `word-break: normal`, `overflow-wrap: anywhere`, `tab-size: 4`, `margin: 0`, `box-sizing: border-box`. `.trainer-stack` itself gets `position: relative; display: grid;` and both children get `grid-area: 1 / 1`. `.trainer-rendered-layer` additionally gets `pointer-events: none` and text color from the per-status rule below. `.trainer-textarea` additionally gets `color: transparent`, `-webkit-text-fill-color: transparent`, `background: transparent`, `caret-color: transparent`, `resize: none`, `overflow: hidden` — but keeps the existing `textarea` selector's border/focus-ring styling (do not remove those). Per-status rule (as CSS attribute selectors under `.trainer-rendered-layer [data-status="..."]`): `pending` -> `color: var(--color-pending)`; `correct` -> `color: var(--color-correct)`; `incorrect` -> `color: var(--color-incorrect); background: var(--color-incorrect-bg); text-decoration: underline; text-decoration-thickness: 2px; text-underline-offset: 2px`. `.trainer-caret` gets `border-left: 2px solid var(--color-accent); display: inline-block;` (an in-flow, zero-width marker — do NOT position it via `getBoundingClientRect()` or pixel math).

    Modify `src/ui/CaptureSurface.tsx`: add a required prop `text: string` to `CaptureSurface({ text }: { text: string })`. Wrap the existing `<textarea>` and a new `<div className="trainer-rendered-layer">` inside a new `<div className="trainer-stack">`. Give the textarea an added `className="trainer-textarea"` (keep its existing `id`/`ref`/`aria-describedby`/etc.). Inside `.trainer-rendered-layer`, render one entry per target character: `<span key={i} data-status={perCharStatus[i]}>{text[i]}</span>`, plus one empty `<span className="trainer-caret" aria-hidden="true" />` inserted in the map immediately before the span at index `cursor` (real DOM element in normal document flow, not pixel-positioned — matches 02-UI-SPEC.md's Interaction Contract). Derive `perCharStatus`/`cursor` each render from `computeTrainerState(text, getCharLog())`. Add a new local re-render trigger (do NOT reuse `useCapture`'s 250ms-throttled `count` — too slow for "live" feedback): a small `useSyncExternalStore`-based hook defined inline in this file (e.g. `useCharLogTick()`) whose `subscribe(onChange)` schedules `onChange` via `requestAnimationFrame`, re-scheduling the next frame from inside the callback (same throttled-subscribe shape as `use-capture.ts`'s `subscribe`/`THROTTLE_MS`, just per-frame instead of per-250ms) and whose `getSnapshot()` returns `getCharLog().length` (React bails out via `useSyncExternalStore` when the snapshot is unchanged — nothing about this touches `capture.ts`'s hot path, D-13). Add a `useLayoutEffect` keyed on `cursor` that sets `ref.current.selectionStart = ref.current.selectionEnd = cursor` whenever either currently differs from `cursor` (D-10) — this is what keeps the invisible native caret from drifting away from the visible logical caret if the user presses an arrow key or clicks.

    Modify `src/ui/App.tsx`: pass `text={exercise.text}` into `<CaptureSurface key={loadToken} text={exercise.text} />`. Remove the now-redundant block `<section><h2>Exercise</h2><pre className="preview">{exercise.text}</pre></section>` (the current lines rendering it) — `CaptureSurface`'s new rendered layer is the sole display of `exercise.text` going forward; showing the same text twice (once read-only, once as the overlay) would be duplicated, confusing UX and is the direct mechanical consequence of implementing D-01. The `.preview` CSS rule in `index.css` may be left in place (harmless, possibly reused by Phase 3) — do not delete it as part of this task.

    Add a new project entry to `vite.config.ts`'s `test.projects` array, alongside the existing `unit`/`dom` entries (leave those two unchanged): `{ test: { name: 'ui', environment: 'happy-dom', include: ['src/ui/**/*.test.tsx'] } }`.

    Create `src/ui/CaptureSurface.test.tsx`: render `<CaptureSurface text="ab" />` via `createRoot` (from `react-dom/client`) into a `document.createElement('div')` appended to `document.body`, wrapping render/dispatch calls in React's `act` (imported from `react`) so effects flush before assertions. Reuse the exact trusted-event-construction helpers already established in `src/capture/capture.test.ts` (`trustedInputEvent`/`beforeInput` — force `isTrusted: true` via `Object.defineProperty`, matching the existing convention) to dispatch a `beforeinput` with `{ inputType: 'insertText', data: 'a' }` on the rendered `<textarea>`, then assert the DOM shows `[data-status="correct"]` containing `"a"` at the first character position and `.trainer-caret` immediately precedes the second character's span. Call `resetCapture()` in a `beforeEach` (mirroring `capture.test.ts`'s setup) so each test starts from an empty charLog.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit -- state && pnpm vitest run --project ui</automated>
  </verify>
  <acceptance_criteria>
    - `src/trainer/state.ts` has zero non-type imports (only `import type { CommittedChar } from '../capture/types'`)
    - `computeTrainerState('a', [])` returns `{ perCharStatus: ['pending'], cursor: 0, correctedCount: 0, uncorrectedCount: 0, completedAt: null }`
    - a delete-type record with cursor at 1 moves cursor to 0 and sets `perCharStatus[0]` back to `'pending'`
    - `grep -rn "dangerouslySetInnerHTML\|\.innerHTML" src/ui src/trainer` returns no matches
    - rendering `<CaptureSurface text="ab" />` and dispatching a trusted `beforeinput` (`insertText`, data `'a'`) results in a DOM node with `[data-status="correct"]` whose text content is `"a"`
    - `vite.config.ts` `test.projects` includes an entry with `name: 'ui'`, `environment: 'happy-dom'`, `include: ['src/ui/**/*.test.tsx']`, and the existing `unit`/`dom` entries are unchanged
    - `src/ui/App.tsx` no longer contains the string `<h2>Exercise</h2>`
  </acceptance_criteria>
  <done>The single happy path — load "ab", type "a" correctly — works end-to-end and is committed: the reducer computes it, the overlay renders it, App.tsx feeds it real exercise text, and an automated test proves the full path without manual verification.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Whitespace glyphs + extended golden coverage</name>
  <files>src/trainer/state.ts, src/trainer/state.test.ts, src/ui/CaptureSurface.tsx, src/ui/CaptureSurface.test.tsx, src/index.css</files>
  <read_first>src/trainer/state.ts, src/trainer/state.test.ts, src/ui/CaptureSurface.tsx, src/ui/CaptureSurface.test.tsx, src/index.css, .planning/phases/02-interactive-typing-trainer/02-UI-SPEC.md, .planning/phases/02-interactive-typing-trainer/02-RESEARCH.md</read_first>
  <behavior>
    - glyphFor(' ') === '·' (U+00B7), glyphFor('\n') === '↵' (U+21B5), glyphFor('a') === 'a'
    - an IME-composed 2-codepoint commit (inputType 'insertFromComposition', data 'ab') against target 'ab' scores both positions in one record
    - a second delete-type record when cursor is already 0 is a no-op (cursor stays 0, no throw, no negative index)
    - once completedAt is set, a further insert record past target.length does not overwrite it
  </behavior>
  <action>
    Add `export function glyphFor(char: string): string` to `src/trainer/state.ts` (space -> `'·'`, `'\n'` -> `'↵'`, all else -> the character unchanged), with a one-line comment noting this is intentionally reachable-but-inert for tab: `Exercise.text` can never contain `'\t'` because Phase 1's `normalize.ts` expands every tab to spaces before an `Exercise` exists (D-06 amended) — do NOT add a tab->glyph mapping here, and do NOT add any `capture.ts` export for synthetic Tab-character insertion (02-RESEARCH.md's `recordSyntheticChar` design and 02-PATTERNS.md's pre-amendment pattern both predate CONTEXT.md's amendment of D-06/D-07 — the amended, locked CONTEXT.md decision is authoritative; Tab handling itself is Plan 02-02's Task 2, not this task).

    Extend `src/trainer/state.test.ts`'s golden-case table (`Case`/`it.each`, matching `normalize.test.ts`'s convention) with the four behaviors listed above, plus a fifth case for `glyphFor`'s three mappings.

    In `CaptureSurface.tsx`, change each character span's rendered content: when `text[i]` is `' '` or `'\n'`, render `<span className="ws-glyph">{glyphFor(text[i])}</span>` as the span's child instead of the bare character; for every other character, keep rendering the bare character directly (no extra wrapper node) to avoid unnecessary DOM nodes. The outer `[data-status]` span keeps its Task-1 correct/incorrect/pending coloring, incorrect background, and underline regardless of whether it wraps a glyph or plain text. In `src/index.css`, add `.ws-glyph { opacity: 0.55; }` — this dims only the glyph's own text, never the parent span's background/underline decorations (02-UI-SPEC.md: "the incorrect background tint and underline are not dimmed — only the glyph character's own opacity is reduced"). The real `'\n'` character stays in the DOM text content (inside the glyph span or immediately after it) so `white-space: pre-wrap` (already set in Task 1) produces the actual line break — do not add an explicit `<br>`.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit -- state && pnpm vitest run --project ui</automated>
  </verify>
  <acceptance_criteria>
    - `glyphFor(' ')` returns exactly `'·'`; `glyphFor('\n')` returns exactly `'↵'`
    - `grep -n "'\\\\t'" src/trainer/state.ts` finds no tab-to-glyph mapping (confirms D-06 amendment honored — no tab glyph)
    - `grep -n "recordSyntheticChar" src/capture/capture.ts src/ui/CaptureSurface.tsx src/trainer/state.ts` returns no matches (confirms no pre-amendment Tab-insertion machinery was added)
    - a rendered `[data-status="incorrect"]` span wrapping a space still shows the incorrect background/underline styling; only its nested `.ws-glyph` child has `opacity: 0.55`
    - `src/trainer/state.test.ts`'s `it.each` case table has at least 5 more named cases than before this task
  </acceptance_criteria>
  <done>Whitespace renders as visible, muted glyphs overlaying (not replacing) the correct/incorrect palette; the extended golden suite locks IME multi-codepoint commits, idempotent backspace-at-zero, and monotonic completion.</done>
</task>

</tasks>

<planner_assumptions>
Flagged, unresolved edge-probe rows (spec-less fallback protocol — never a silent drop).
Rows below are the 7 of 11 total probe rows assigned to this plan (TYPE-01/02/03/04); the
remaining 4 (TYPE-05/06) are in 02-02-PLAN.md.

- **TYPE-01/empty:** "What is the result for empty, single-element, or null input?" —
  RESOLVED, not a genuine gap: `Exercise.text` is guaranteed non-empty by construction
  (`CorpusInput.tsx`'s `value.trim() === ''` guard blocks whitespace-only paste submission;
  `normalize()` always appends exactly one trailing `'\n'` for non-empty input, per Phase 1's
  own golden case 13). `computeTrainerState(target, [])` with `target.length >= 1` and an
  empty `charLog` correctly returns the all-pending initial state — there is no empty-target
  path to build.
- **TYPE-01/encoding:** "Whose definition of length/equality applies?" — RESOLVED:
  code-point comparison via `for...of`, not UTF-16 units or grapheme clusters, per this
  plan's `computeTrainerState` design — inherits Phase 1 normalize.ts's explicit "do NOT
  Unicode-normalize (NFC/NFD)" policy rather than introducing a new one.
  This applies at the character level, but for actual keystroke correctness scoring it
  is the reducer's for...of loop that carries this definition forward.
- **TYPE-02/unclassified:** the probe could not classify a specific edge shape for
  free-correction. ASSUMPTION (flagged, narrower interpretation applied): scoped strictly
  to single-character-at-a-time correctness — no batch/multi-char paste-driven correctness
  ambiguity, since paste into the capture surface is already blocked (Phase 1 CAPT-04); IME
  multi-codepoint commits are the only multi-character case and are handled explicitly
  (Task 2's behavior cases).
- **TYPE-03/empty:** backspace with no prior keystroke — RESOLVED: the delete-branch is
  guarded (`if (cursor > 0)`), so pressing backspace before typing anything is a no-op —
  no negative cursor, no throw.
- **TYPE-03/encoding:** delete-branch code-point semantics — RESOLVED: every delete-type
  record decrements `cursor` by exactly one target-character position (not one UTF-16 unit,
  not one grapheme) — a known, documented v1 limitation for multi-code-point composed
  sequences (D-11), not something this plan attempts to fix.
- **TYPE-04/empty:** whitespace glyph on a minimal exercise — RESOLVED: since `Exercise.text`
  always ends in exactly one `'\n'` (see TYPE-01/empty above), the smallest possible exercise
  is the single character `'\n'`, which renders as one `↵`-glyphed span; no separate "empty"
  rendering path is needed.
- **TYPE-04/encoding:** glyph substitution for non-ASCII whitespace-like characters —
  RESOLVED: `glyphFor` operates per single JS code point; NBSP (U+00A0) and other
  space-like Unicode characters are NOT substituted with `·` (they render as themselves),
  matching normalize.ts's own precedent of treating NBSP as distinct from ASCII space —
  a scope boundary, not a bug.
</planner_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Corpus text (Phase 1, potentially adversarial pasted/uploaded content) -> rendered per-character span layer | Every character of `exercise.text`, including any HTML-looking substrings, is rendered by this plan's new overlay |
| Native `<textarea>` selection/caret <-> logical `cursor` (trainer/state.ts) | Arrow-key/click-driven native selection changes must never let a keystroke land at an unexpected logical position |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering / Elevation of Privilege (XSS) | src/ui/CaptureSurface.tsx per-character span rendering | high | mitigate | Every character and glyph renders exclusively as a React text child (`{text[i]}` / `{glyphFor(text[i])}`); no `dangerouslySetInnerHTML`, no manual `innerHTML`/`textContent`-as-HTML anywhere in `src/ui` or `src/trainer` — verified by grep (acceptance criteria) at the end of the task, continuing the exact invariant Phase 1 established. |
| T-02-02 | Information Disclosure | src/trainer/state.ts, src/ui/CaptureSurface.tsx | low | accept | No network egress introduced — pure in-memory computation only, continuing Phase 1's no-`fetch`/XHR/WebSocket invariant. |
| T-02-03 | Denial of Service (resource exhaustion) | src/ui/CaptureSurface.tsx per-character span rendering | low | accept | Up to ~100KB corpus (Phase 1's upload cap) rendered as one `<span>` per character (worst case ~100k DOM nodes, per 02-RESEARCH.md Assumption A5) — accepted for v1 per that risk assessment; span-batching by contiguous same-status runs is the documented future mitigation if a real perf problem is measured, not built preemptively. |
| T-02-04 | Tampering (native selection drift desyncing the logical cursor from the visible caret) | src/ui/CaptureSurface.tsx caret-resync effect | medium | mitigate | A `useLayoutEffect` forces `textarea.selectionStart`/`selectionEnd` back to `cursor` (D-10) whenever they differ, so no arrow-key/click drift can let a subsequent keystroke commit at an unexpected index. |
</threat_model>

<verification>
- [ ] `pnpm vitest run --project unit` green (includes `src/trainer/state.test.ts`)
- [ ] `pnpm vitest run --project ui` green (`src/ui/CaptureSurface.test.tsx`)
- [ ] `pnpm vitest run --project dom` still green (Phase 1's `capture.test.ts` unaffected)
- [ ] `pnpm exec tsc --noEmit -p tsconfig.json` passes under strict + `noUncheckedIndexedAccess`
- [ ] `grep -rn "dangerouslySetInnerHTML\|\.innerHTML" src/` returns no matches
- [ ] `App.tsx` no longer renders a separate read-only exercise preview block
</verification>

<success_criteria>
- Both tasks complete; all verification checks pass
- TYPE-01 (live per-char coloring + caret), TYPE-02 (free-correction), TYPE-03 (backspace), TYPE-04 (whitespace glyphs) are all satisfied end-to-end and provable by the automated `ui`/`unit` test projects, not manual-only
- `src/trainer/state.ts` remains a zero-runtime-import pure module
- No tab-glyph or synthetic-Tab-insertion machinery was added (D-06/D-07 amendment honored)
</success_criteria>

<output>
Create `.planning/phases/02-interactive-typing-trainer/02-01-SUMMARY.md` when done.
</output>

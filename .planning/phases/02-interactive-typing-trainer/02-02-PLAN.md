---
phase: 02-interactive-typing-trainer
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/ui/App.tsx
  - src/ui/CaptureSurface.tsx
  - src/ui/CaptureSurface.test.tsx
  - src/index.css
  - src/trainer/active-time.ts
  - src/trainer/active-time.test.ts
autonomous: true
requirements: [TYPE-04, TYPE-05, TYPE-06]
user_setup: []

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Pressing Tab produces zero visual change: no character is inserted, the cursor does not move, and focus remains in the capture textarea (TYPE-04 support, D-07 amended)."
    - "The Restart control is always visible once the trainer has mounted, uses the primary-button treatment (accent fill, 44px min hit target), and fires immediately on Enter/Space/click with no confirmation dialog (TYPE-05, D-08)."
    - "Clicking Restart resets the trainer to its initial all-pending state, moves the custom caret back to index 0, keeps the same loaded exercise content, and refocuses the capture surface (TYPE-05, D-08)."
    - "computeActiveElapsedMs is exported, pure, and unit-tested with synthetic marker sequences; Phase 2 does not display a running timer — Phase 3 consumes the value directly (TYPE-06, D-09)."
    - { statement: "The custom caret blinks on a ~1s cycle (~530ms visible / ~530ms hidden) while the window is focused and visible, renders solid under prefers-reduced-motion: reduce, and renders solid (not hidden) while the window is blurred or the tab is hidden.", verification: backstop }
  artifacts:
    - path: "src/trainer/active-time.ts"
      provides: "computeActiveElapsedMs pure toggle-state-machine over CaptureMarker[] (D-09)"
      contains: "computeActiveElapsedMs"
    - path: "src/trainer/active-time.test.ts"
      provides: "golden-case suite locking D-09 behavior (no keystroke, no blur, full pair, unmatched hidden, overlapping pair, out-of-order marker)"
      contains: "computeActiveElapsedMs"
  key_links:
    - from: "src/ui/App.tsx"
      to: "src/capture/capture.ts"
      via: "handleRestart calls resetCapture() before bumping loadToken, keeping the same exercise"
      pattern: "resetCapture\\(\\)"
    - from: "src/ui/CaptureSurface.tsx"
      to: "src/index.css"
      via: "trainer-caret blink animation gated by a focus/visibility data attribute + prefers-reduced-motion"
      pattern: "trainer-caret"
---

<objective>
Add the Restart control (D-08), the Tab keydown no-op + caret-blink motion (D-07 amended,
UI-SPEC backstop), and the pure `computeActiveElapsedMs` session-timing computation (D-09) —
the three remaining expansion slices around Plan 02-01's tracer.

Purpose: closes out TYPE-05 (restart) and TYPE-06 (active-time data + Tab-safe focus) without
touching the reducer or overlay-rendering core Plan 02-01 already proved end-to-end.

Output: a working Restart button, a Tab key that safely no-ops instead of stealing focus, a
focus/visibility-aware caret blink, and `src/trainer/active-time.ts` ready for Phase 3.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-interactive-typing-trainer/02-CONTEXT.md
@.planning/phases/02-interactive-typing-trainer/02-RESEARCH.md
@.planning/phases/02-interactive-typing-trainer/02-UI-SPEC.md
@.planning/phases/02-interactive-typing-trainer/02-01-SUMMARY.md

<interfaces>
<!-- Shape produced by 02-01-PLAN.md — extend, don't re-derive. -->

src/trainer/state.ts (02-01 shape):
  export type PerCharStatus = 'correct' | 'incorrect' | 'pending';
  export interface TrainerState { perCharStatus: PerCharStatus[]; cursor: number; correctedCount: number; uncorrectedCount: number; completedAt: number | null }
  export function computeTrainerState(target: string, charLog: readonly CommittedChar[]): TrainerState;
  export function glyphFor(char: string): string;

src/ui/CaptureSurface.tsx (02-01 shape):
  export function CaptureSurface(props: { text: string }): JSX.Element
  // renders .trainer-stack > (.trainer-textarea, .trainer-rendered-layer)
  // .trainer-rendered-layer contains one <span data-status> per char + one <span className="trainer-caret">
  // a useLayoutEffect resyncs textarea.selectionStart/selectionEnd to `cursor` (D-10)
  // a local useCharLogTick() (useSyncExternalStore + requestAnimationFrame) drives re-render

src/ui/App.tsx (02-01 shape):
  <CaptureSurface key={loadToken} text={exercise.text} />
  // handleLoad(loaded): resetCapture(); setExercise(loaded); setLoadToken(t => t+1); ...

src/capture/types.ts (Phase 1, unchanged):
  export type MarkerKind = 'blur' | 'focus' | 'hidden' | 'visible';
  export interface CaptureMarker { seq: number; kind: MarkerKind; tMs: number }

Contract this plan ADDS (src/trainer/active-time.ts):
  export function computeActiveElapsedMs(charLog: readonly CommittedChar[], markers: readonly CaptureMarker[], now: number): number;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restart control (D-08)</name>
  <files>src/ui/App.tsx, src/ui/CaptureSurface.test.tsx, src/index.css</files>
  <read_first>src/ui/App.tsx, src/ui/CaptureSurface.tsx, .planning/phases/02-interactive-typing-trainer/02-UI-SPEC.md</read_first>
  <action>
    Add `handleRestart` to `src/ui/App.tsx`: calls `resetCapture()`, then `setLoadToken((t) => t + 1)` — do NOT call `setExercise` (the exercise content is unchanged, per D-08's "keeping the loaded content and resetting all session state"). Re-derive `loadRef.current` and `sessionRef.current` for the SAME `exercise` with a fresh `startedAt = Date.now()`, mirroring `handleLoad`'s shape but without loading new content. Render a `<button type="button" className="primary" onClick={handleRestart}>Restart exercise</button>` inside the existing `exercise !== null` branch, placed after `<CaptureSurface key={loadToken} text={exercise.text} />`, with `--space-md` (16px) of gap between the trainer surface and the button (02-UI-SPEC.md Spacing contract) — wrap both in a `style={{ display: 'grid', gap: 'var(--space-md)' }}` container if the existing `<main>`'s `--space-lg` gap is too loose for this specific pairing. No confirmation dialog: the button fires `handleRestart` directly on click (native `<button>` already handles Enter/Space activation) — do not add `window.confirm` or a dialog element, per 02-UI-SPEC.md's Copywriting Contract ("no confirmation dialog... restarting only discards the current in-progress attempt, never saved data").
  </action>
  <verify>
    <automated>pnpm vitest run --project ui</automated>
  </verify>
  <acceptance_criteria>
    - `handleRestart` in `App.tsx` calls `resetCapture()` before `setLoadToken`
    - `handleRestart` does not call `setExercise`
    - the Restart button's visible label is exactly `"Restart exercise"`
    - `grep -n "window.confirm" src/ui/App.tsx` returns no matches
    - a rendered test: after dispatching one committed character on `CaptureSurface`'s textarea then clicking the Restart button, the DOM shows all character spans back at `[data-status="pending"]` and `.trainer-caret` immediately precedes the first character span
  </acceptance_criteria>
  <done>Restart resets the trainer to its initial state (same exercise, fresh session) with no confirmation step, matching D-08 and the UI-SPEC's Restart contract.</done>
</task>

<task type="auto">
  <name>Task 2: Tab keydown no-op + caret-blink motion</name>
  <files>src/ui/CaptureSurface.tsx, src/ui/CaptureSurface.test.tsx, src/index.css</files>
  <read_first>src/ui/CaptureSurface.tsx, .planning/phases/02-interactive-typing-trainer/02-CONTEXT.md, .planning/phases/02-interactive-typing-trainer/02-UI-SPEC.md</read_first>
  <reversibility rating="reversible">D-07 amended is isolated to one keydown branch with no capture-pipeline change (per CONTEXT.md D-07's own rationale) — no checkpoint needed, already locked by the user.</reversibility>
  <action>
    Add an `onKeyDown` handler to the `<textarea>` in `CaptureSurface.tsx`: `if (e.key === 'Tab') e.preventDefault()` and nothing else — no character insertion, no new `capture.ts` export, no cursor movement (D-07 amended, superseding 02-RESEARCH.md's pre-amendment `recordSyntheticChar`/`setRangeText` design and 02-PATTERNS.md's matching pre-amendment pattern — CONTEXT.md's amended, locked decision is authoritative; do not build the `recordSyntheticChar` capture.ts export those documents describe).

    Track focus/visibility state locally in `CaptureSurface` with `const [isActive, setIsActive] = useState(true)`: add `onFocus={() => setIsActive(true)}` and `onBlur={() => setIsActive(false)}` to the `<textarea>`, and a `useEffect` that adds a `document.addEventListener('visibilitychange', ...)` listener setting `isActive` to `!document.hidden` (removed on cleanup). Render `.trainer-caret` with `data-active={isActive}`.

    In `src/index.css`, add `@keyframes trainer-caret-blink { 0%, 49% { opacity: 1; } 50%, 100% { opacity: 0; } }` (a ~530ms/~530ms cycle at `animation-duration: 1060ms`... use `animation: trainer-caret-blink 1060ms step-end infinite;` on `.trainer-caret`) nested inside `@media (prefers-reduced-motion: no-preference)` only — so `prefers-reduced-motion: reduce` never animates it (solid at all times, satisfying that half of the backstop truth without extra code). Add `.trainer-caret[data-active="false"] { animation: none; opacity: 1; }` so the caret renders solid (not hidden) while blurred/hidden, matching D-09's "timing is paused" passive cue.
  </action>
  <verify>
    <automated>pnpm vitest run --project ui</automated>
  </verify>
  <acceptance_criteria>
    - dispatching a trusted `keydown` with `key: 'Tab'` on the focused capture textarea leaves `document.activeElement` unchanged and `getCharLog().length` unchanged afterward
    - `grep -n "recordSyntheticChar" src/capture/capture.ts src/ui/CaptureSurface.tsx` returns no matches
    - `src/index.css`'s `trainer-caret-blink` `@keyframes` rule (or its consuming `animation` declaration) is nested under `@media (prefers-reduced-motion: no-preference)`
    - simulating a `blur` event on the textarea results in `.trainer-caret` carrying `data-active="false"`
  </acceptance_criteria>
  <done>Tab is fully absorbed with zero visual or capture-log change; the custom caret blinks only while focused+visible and under motion-preference, and renders solid otherwise.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: computeActiveElapsedMs (D-09)</name>
  <files>src/trainer/active-time.ts, src/trainer/active-time.test.ts</files>
  <read_first>src/capture/types.ts, src/ingestion/normalize.ts, .planning/phases/02-interactive-typing-trainer/02-RESEARCH.md</read_first>
  <behavior>
    - computeActiveElapsedMs([], [], 1000) === 0 (no keystroke yet)
    - charLog=[{tMs:0,...}], markers=[], now=500 -> 500 (no blur/hidden at all)
    - one full blur(t=100)/focus(t=200) pair inside a session starting at t=0 -> elapsed(now=500) === 400
    - a 'hidden' marker with no matching 'visible' (session ended while backgrounded) -> the open interval counts up to `now`
    - overlapping blur(t=100) + hidden(t=100), then a single focus(t=200) -> only one 100ms interval subtracted, not double-subtracted
    - an out-of-order/duplicate 'focus' marker while already active is a no-op
  </behavior>
  <action>
    Create `src/trainer/active-time.ts` as a PURE module (only `import type { CommittedChar, CaptureMarker } from '../capture/types'`, zero DOM access) exporting `computeActiveElapsedMs(charLog, markers, now): number` implementing the toggle-state-machine from 02-RESEARCH.md Architecture Patterns Pattern 4 verbatim: `t0 = charLog[0]?.tMs`, return `0` immediately if `charLog[0]` is `undefined`; track a single `inactiveSince: number | null`; for each marker with `tMs >= t0`, a `'blur'`/`'hidden'` kind sets `inactiveSince = tMs` only if currently `null`, and a `'focus'`/`'visible'` kind closes it (`totalInactiveMs += tMs - inactiveSince; inactiveSince = null`) only if currently non-null; after the loop, if `inactiveSince` is still non-null, add `Math.max(0, now - inactiveSince)` to `totalInactiveMs`; return `Math.max(0, now - t0 - totalInactiveMs)`. Add the same pure-module header-comment convention as `state.ts` (purity statement, "D-09", the test file that locks it).

    Create `src/trainer/active-time.test.ts` with a `Case`/`it.each` golden-case table (matching `normalize.test.ts`'s convention) covering the six behaviors listed above.
  </action>
  <verify>
    <automated>pnpm vitest run --project unit -- active-time</automated>
  </verify>
  <acceptance_criteria>
    - `src/trainer/active-time.ts` has zero non-type imports
    - `computeActiveElapsedMs([], [], 1000)` returns exactly `0`
    - a synthetic overlapping blur+hidden pair followed by one focus marker produces the same elapsed result as a single blur followed by one focus marker (proves the no-op-while-already-inactive guard)
    - `src/trainer/active-time.test.ts` has at least 6 named cases
  </acceptance_criteria>
  <done>computeActiveElapsedMs is exported, pure, and golden-tested against every marker-ordering edge case 02-RESEARCH.md calls out; ready for Phase 3 to consume.</done>
</task>

</tasks>

<planner_assumptions>
Flagged, unresolved edge-probe rows (spec-less fallback protocol — never a silent drop).
Rows below are the 4 of 11 total probe rows assigned to this plan (TYPE-05/06); the
remaining 7 (TYPE-01/02/03/04) are in 02-01-PLAN.md.

- **TYPE-05/adjacency:** "when two things are exactly equal or just touch, do they merge,
  collide, or separate?" — interpreted as: does clicking Restart while a restart is already
  in flight cause a double-reset? RESOLVED: `handleRestart` is a synchronous state update
  (`resetCapture()` + `setLoadToken` increment), not async/debounced — there is no in-flight
  window. A rapid double-click just increments `loadToken` twice, remounting `CaptureSurface`
  twice in a row with no observable difference from once (idempotent).
- **TYPE-05/empty:** Restart with no exercise loaded — RESOLVED: the Restart button only
  renders inside the `exercise !== null` branch, so there is no "restart with nothing loaded"
  case to handle.
- **TYPE-05/ordering:** "when elements compare equal, is output order specified and stable?"
  — interpreted as: do repeated Restarts produce a deterministic reset order across the
  capture buffers? RESOLVED: `resetCapture()` (Phase 1, unchanged) clears `buffer`, `charLog`,
  `markers`, `downCodes`, `composing`, `lastValue`, `lastRealKeydownTMs`, and `seq`
  synchronously and unconditionally in a fixed order every time — repeated restarts are
  fully deterministic; Phase 2 introduces no new ordering concern.
- **TYPE-06/unclassified:** the probe could not classify a specific edge for "session timing
  starts on first keystroke / excludes blurred-hidden time." ASSUMPTION (flagged, genuinely
  open): `computeActiveElapsedMs` is verified via synthetic-marker-sequence unit tests
  (Task 3) rather than a live end-to-end blur/focus browser simulation, since D-09 explicitly
  states Phase 2 does not display this value to the user — any behavioral gap in a real
  browser's focus/visibility event timing would only surface once Phase 3 consumes the
  value. Accepted as sufficient verification for this phase's stated scope.
</planner_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Physical Tab keypress -> browser's default focus-traversal behavior | This plan's keydown exception intercepts Tab before the browser can move focus out of the capture textarea |
| Restart control -> capture buffers | A user-triggered reset must fully and deterministically clear all three capture logs, never leaving stale state visible in the next session |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Tampering (focus loss silently breaking capture) | src/ui/CaptureSurface.tsx Tab keydown handler | medium | mitigate | `e.preventDefault()` on `Tab` keydown, no character insertion, no cursor movement, no new `capture.ts` export — the amended D-07 fully closes the isTrusted-bypass surface 02-RESEARCH.md's original design would have introduced, since no synthetic-record path exists at all in this phase. |
| T-02-06 | Repudiation (Restart silently discarding an in-progress session) | src/ui/App.tsx handleRestart | low | accept | No confirmation dialog is an explicit, documented UI-SPEC decision: nothing is persisted in v1 (PERS-01/02 are v2), so Restart only discards an unsaved in-progress attempt, never saved data — treating it as a destructive-with-confirmation action would be over-engineering for a stateless, instantly-repeatable control. |
| T-02-07 | Information Disclosure (focus/visibility timing patterns) | src/trainer/active-time.ts | low | accept | Pure in-memory computation, no egress, not displayed to any external party or the user in this phase — consistent with Phase 1's markers already being in-memory only. |
</threat_model>

<verification>
- [ ] `pnpm vitest run --project ui` green (Restart, Tab no-op, caret-blink data-attribute assertions)
- [ ] `pnpm vitest run --project unit` green (`src/trainer/active-time.test.ts`)
- [ ] `pnpm exec tsc --noEmit -p tsconfig.json` passes under strict + `noUncheckedIndexedAccess`
- [ ] `grep -n "recordSyntheticChar" src/capture/capture.ts` returns no matches (D-07 amendment honored)
- [ ] `grep -n "window.confirm" src/ui/App.tsx` returns no matches
</verification>

<success_criteria>
- All 3 tasks complete; all verification checks pass
- TYPE-05 (restart) and TYPE-06 (active-time data + Tab-safe focus) fully satisfied
- No pre-amendment Tab-insertion machinery (`recordSyntheticChar`, tab glyph) exists anywhere in `src/`
- `src/trainer/active-time.ts` is a zero-runtime-import pure module, ready for Phase 3
</success_criteria>

<output>
Create `.planning/phases/02-interactive-typing-trainer/02-02-SUMMARY.md` when done.
</output>

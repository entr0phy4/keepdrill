---
phase: 06-cross-session-analytics
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - src/index.css
  - src/ui/KeyboardHeatmap.tsx
  - src/ui/DigraphLatencyView.tsx
  - src/ui/LanguageProfileView.tsx
  - src/ui/AnalyticsDashboard.tsx
  - src/ui/AnalyticsDashboard.test.tsx
  - src/ui/App.tsx
  - src/ui/App.test.tsx
autonomous: true
requirements: [ANLY-03, ANLY-04, ANLY-05]

must_haves:
  truths:
    # --- Goal-backward (user-visible) ---
    - "The user can open a third header item labeled Analytics and see a stacked dashboard of slowest digraphs, then the keyboard heatmap, then the per-language profile (phase goal, D-01, D-02, ANLY-03/04/05)"
    - "Zero persisted sessions shows the page-level empty copy and does not mount the three sections (D-04)"
    - "When sessions exist, the ranked digraph table shows at most 10 rows of pair + rounded median ms + sample count; sub-threshold pairs are omitted (ANLY-03, D-05, D-07, D-08)"
    - "When sessions exist, a static US-ANSI diagram shows sequential amber intensity plus on-key milliseconds for gated keys, and unused keys stay drawn with surface fill and no number (ANLY-04, D-10..D-14)"
    - "When sessions exist, a language table shows WPM, adj. WPM, accuracy, and session count per tag, with plaintext as a visible ordinary bucket (ANLY-05, D-16..D-20)"
    # --- UI Considerations: Resolved — explicit (lift verbatim as truths) ---
    - "The header nav always renders exactly three options — Trainer, History, Analytics — from first paint. Trainer is active on initial load. Exactly one button has aria-current=\"page\". Switching is a synchronous useState flip — no router, no URL, no spinner (D-01, Pattern 4)."
    - "Switching to Analytics sets the trainer wrapper to display: none and mounts AnalyticsDashboard in the same main-grid slot. The trainer subtree is never unmounted (Phase 4 D-08). CorpusInput remains visible."
    - "useLiveQuery(listNewestFirst) undefined → loading copy; [] → page empty copy and no section mounts; length > 0 → all three sections in digraph → heatmap → language order (D-02/D-04)."
    - "Digraph pairs below DIGRAPH_MIN_SAMPLES = 5 post-filter are omitted, not greyed (D-07). Zero eligible pairs → section empty copy; heatmap and language still render."
    - "Each digraph row shows pair + rounded median ms + sample count n (D-08). Rounding happens only at the display layer."
    - "Heatmap is a static US-ANSI diagram keyed by KeyboardEvent.code, colored by median latency, not frequency (D-10/D-11). Sampled keys show color and the numeric median ms (D-12). Below-gate and unused keys stay drawn, --color-surface, no number (D-13)."
    - "Heatmap color is relative to current gated min/max (D-14), sequential amber tokens, never accent/destructive, never live during capture (D-15)."
    - "Language profile is a table, one row per tag with ≥1 session, columns language + net WPM + adj. WPM + accuracy + n, sorted by n descending (D-16..D-19). plaintext is a visible ordinary bucket (D-20) via .key-chip."
    - "History rows stay inert (Phase 4 D-13). This page adds no per-session drill-down."
    - "Digraph pairs, key labels, and language tags render as React text nodes."
    # --- UI Considerations table: 19 covered rows (engine said 18; all table covered rows lifted, no silent drop) ---
    - "empty A4: Zero sessions → page empty copy from Copywriting; three sections unmounted (D-04)."
    - "empty A5/A6: Sessions exist but zero pairs clear n≥5 → section heading + empty copy; heatmap and language still mount (D-07)."
    - "empty A7/A8: Diagram always mounts when sessions exist. Zero gated keys → all-neutral keys, no numbers, plus ungated caption. Never replace the diagram with a paragraph (D-13)."
    - "empty A9: Not a section-empty: ≥1 session ⇒ ≥1 language row (D-19). plaintext is a normal bucket."
    - "loading A3: undefined → Loading analytics…; never the empty copy. undefined and [] are never conflated."
    - "error A2: No user-facing read-error banner this phase (same as History). Folds do not throw on empty input."
    - "populated A5: 1–10 rows after gate+sort+cap; columns Digraph / Median / Samples; omit sub-threshold pairs."
    - "populated A7: Full US 101 alphanumeric silhouette; sampled keys show sequential amber + on-key ms; unused stay neutral."
    - "populated A9: One row per tag, sorted by session count desc; columns per D-17; adj. WPM always present via resolveMetrics."
    - "partial A5: 1–9 eligible pairs is a valid shorter table; no padding to 10; no only-N-pairs caption."
    - "partial A7: A mix of sampled and unused keys is the expected shape; unused remain drawn."
    - "overflow A5, A9: Tables are width 100% and wrap with the column; long language strings wrap inside the cell, never a page-level horizontal scrollbar."
    - "overflow A7: --column-max unchanged; .keyboard-heatmap may overflow-x auto at narrow viewports."
    - "overflow A1 nav: Header flex row already flex-wrap wrap; three labels wrap as a unit, never clip."
    - "overflow A2 page: Dashboard grows with normal document flow; no independent page-level scroll container; --column-max unchanged."
    - "overflow A8 caption: Figure caption is Body 15/400/1.5 muted and wraps inside --column-max."
    - "zero-one-many A5, A9: 0 digraph rows → section empty (not a 0-row table). 1 language row is a normal table. Many language rows grow the page."
    - "long-text A5 digraph: Pair is two codepoints after glyphFor; chip stays 1–4 visible characters."
    - "long-text A1, A2, A8: Nav label, page heading, and ungated caption are fixed locked copy (Analytics / No keys have 5 samples yet.) — never user-typed."
    # --- UI Considerations backstops (4) ---
    - statement: "at 320px viewport the keyboard diagram never expands #root horizontally; overflow is confined to .keyboard-heatmap (overflow-x: auto) and the US-ANSI silhouette remains intact after a horizontal scroll"
      verification: backstop
    - statement: "an unexpectedly long exercise.language string wraps or breaks inside its table cell and never causes a page-level horizontal scrollbar"
      verification: backstop
    - statement: "every drawn key's label (≤ 5 chars, e.g. Shift) plus optional 1–4 digit ms number stay inside the 32×48 key box without overflowing into a neighbor"
      verification: backstop
    - statement: "subsequent useLiveQuery live updates do not flash the page-level loading line or unmount sections; only table/heatmap contents update in place"
      verification: backstop
  prohibitions:
    - statement: "MUST NOT grey, rank, or caption sub-threshold digraphs as results — omit them and show the section empty copy when zero pairs clear the gate (D-07, transparency)"
      status: resolved
      verification: test
    - statement: "MUST NOT use shaming, guilt, loss-aversion, personal-worst, or you're-slow-at copy on Analytics (values; 03-UI-SPEC no gamified framing)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT paint the heatmap with a green/red score palette or with accent/destructive tokens, and MUST NOT make color the only slowness cue — gated keys always show the on-key millisecond number (D-12, values + WCAG 1.4.1)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT mount or update the heatmap during capture / on the hot path (D-15, FEATURES.md anti-feature)"
      status: resolved
      verification: test
    - statement: "MUST NOT add in-page jump links, a section TOC, a fourth header item, charts, sparklines, language filters, or per-session drill-down (D-01/D-03, ANLY-07/08 deferred, PROJECT.md charts out of scope)"
      status: resolved
      verification: test
    - statement: "MUST NOT hide unused heatmap keys or fill them with the heatmap-lo tint as if they were fast (D-13 transparency)"
      status: resolved
      verification: test
    - statement: "MUST NOT add a network, sync, or export path for analytics (privacy; local IndexedDB only)"
      status: resolved
      verification: test
  artifacts:
    - path: "src/ui/AnalyticsDashboard.tsx"
      provides: "useLiveQuery(listNewestFirst) three-way branch + stacked sections (D-01..D-04)"
      exports: ["AnalyticsDashboard"]
    - path: "src/ui/DigraphLatencyView.tsx"
      provides: "semantic table Slowest digraphs; glyphFor; Math.round ms (ANLY-03, D-05..D-08)"
      exports: ["DigraphLatencyView"]
    - path: "src/ui/KeyboardHeatmap.tsx"
      provides: "static US-ANSI CSS-grid diagram, relative amber lerp, on-key ms (ANLY-04, D-10..D-15)"
      exports: ["KeyboardHeatmap"]
    - path: "src/ui/LanguageProfileView.tsx"
      provides: "semantic table Language profile with key-chip tags (ANLY-05, D-16..D-20)"
      exports: ["LanguageProfileView"]
    - path: "src/ui/App.tsx"
      provides: "MODIFIED — view union trainer|history|analytics; third nav button; hide-not-unmount extends to Analytics"
    - path: "src/index.css"
      provides: "MODIFIED — --heatmap-lo/--heatmap-hi/--heatmap-key-fg + .analytics-table + .keyboard-heatmap/.kb-key"
  key_links:
    - from: "src/ui/App.tsx"
      to: "src/ui/AnalyticsDashboard.tsx"
      via: "{view === 'analytics' && <AnalyticsDashboard />} with trainer wrapper display grid|none"
      pattern: "AnalyticsDashboard"
    - from: "src/ui/AnalyticsDashboard.tsx"
      to: "src/persistence/repository.ts"
      via: "useLiveQuery(listNewestFirst) — querier only, never persistence/db"
      pattern: "useLiveQuery\\(listNewestFirst\\)"
    - from: "src/ui/AnalyticsDashboard.tsx"
      to: "src/analytics/analytics.ts"
      via: "computeDigraphLatency(sessions) and computeLanguageProfile(sessions) only when sessions.length > 0"
      pattern: "computeDigraphLatency\\("
    - from: "src/ui/KeyboardHeatmap.tsx"
      to: "src/index.css"
      via: "--heatmap-lo / --heatmap-hi / --heatmap-key-fg plus .kb-key grid cells"
      pattern: "--heatmap-lo"
---

<objective>
Wire the Analytics sibling view (D-01) as one stacked, scroll-only dashboard
(D-02, D-03): semantic digraph table, US-ANSI heatmap, per-language table. Honor
06-UI-SPEC copy, tokens, tables, and hide-not-unmount. Do **not** copy
06-PATTERNS.md's ResultsView `&lt;ol class="results-slowest-list"&gt;` / HistoryRow
list analog for these two rankings — UI-SPEC locks `&lt;table class="analytics-table"&gt;`.

Purpose: ANLY-03, ANLY-04, ANLY-05 become visible. CorpusInput stays mounted
(match History). Trainer subtree hides via display, never unmounts (Phase 4 D-08).

Output: AnalyticsDashboard + three leaves + App.tsx view union + heatmap CSS tokens.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cross-session-analytics/06-CONTEXT.md
@.planning/phases/06-cross-session-analytics/06-RESEARCH.md
@.planning/phases/06-cross-session-analytics/06-UI-SPEC.md
@.planning/phases/06-cross-session-analytics/06-PATTERNS.md
@src/ui/App.tsx
@src/ui/App.test.tsx
@src/ui/HistoryView.tsx
@src/ui/HistoryView.test.tsx
@src/ui/ResultsView.tsx
@src/index.css
@src/analytics/analytics.ts
@src/analytics/heatmap.ts
@src/trainer/state.ts
</context>

<artifacts_this_phase_produces>
Symbols this plan creates or modifies (complete Phase 6 UI inventory; 06-01/06-02 produced the compute symbols):

- `AnalyticsDashboard` (`src/ui/AnalyticsDashboard.tsx`)
- `DigraphLatencyView` (`src/ui/DigraphLatencyView.tsx`)
- `KeyboardHeatmap` (`src/ui/KeyboardHeatmap.tsx`)
- `LanguageProfileView` (`src/ui/LanguageProfileView.tsx`)
- `digraphGlyph(pair)` (local helper in DigraphLatencyView — glyphFor per codepoint, join with no separator)
- App `view` union `'trainer' | 'history' | 'analytics'`
- CSS tokens `--heatmap-lo`, `--heatmap-hi`, `--heatmap-key-fg` (light + dark, 06-UI-SPEC locked hex)
- CSS classes `.analytics-table`, `.keyboard-heatmap`, `.kb-key`
- Copy: nav `Analytics`; heading `Analytics`; `Loading analytics…`; page empty matching HistoryView; digraph empty `Not enough digraph samples yet. Pairs need at least 5 in-window observations across your history.`; heatmap caption `Median latency in ms. Darker amber is slower, relative to keys with at least 5 samples.`; ungated `No keys have 5 samples yet.`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Heatmap tokens, keyboard grid CSS, KeyboardHeatmap leaf (D-10, D-12, D-13, D-14)</name>
  <files>src/index.css, src/ui/KeyboardHeatmap.tsx</files>
  <read_first>
    - src/index.css (:root tokens lines 6-60; dark override 62-80; .key-chip 382-388; nav aria-current 421-431; --column-max 37 — do NOT widen it)
    - src/ui/ResultsView.tsx (Math.round at display layer only, lines 48-50; no score coloring comment lines 6-8)
    - src/analytics/heatmap.ts (HeatmapCell: medianMs null vs number)
    - .planning/phases/06-cross-session-analytics/06-UI-SPEC.md (Color new tokens + locked hex; Layout keyboard heatmap; Copywriting heatmap rows; Interaction: keys are divs not buttons)
    - .planning/phases/06-cross-session-analytics/06-PATTERNS.md (section KeyboardHeatmap.tsx — copy rounding only; do NOT start from platform/layout.ts)
  </read_first>
  <behavior>
    - KeyboardHeatmap always renders a figure with role="group" and aria-label "US ANSI keyboard, median latency in milliseconds" plus the locked caption about median latency in ms.
    - A cell with medianMs number shows color-mix between --heatmap-lo and --heatmap-hi and a rounded millisecond number with no unit on the key; aria-label is "{label}, {n} milliseconds".
    - A cell with medianMs null uses --color-surface fill, --color-text label, no number; aria-label is "{label}, not enough samples".
    - When every cell has medianMs null, the diagram still mounts and the ungated caption "No keys have 5 samples yet." appears in addition to the diagram.
    - Keys are not buttons (no onClick, no tabindex).
  </behavior>
  <action>
    In `src/index.css` :root (light), add exactly three new custom properties with the
    locked 06-UI-SPEC hex: --heatmap-lo #fef3c7, --heatmap-hi #c97116, --heatmap-key-fg
    #1c1c1a. Inside the existing prefers-color-scheme dark :root, set --heatmap-lo
    #2a1c08, --heatmap-hi #9a5a14, --heatmap-key-fg #e6edf3. Do not add any other new
    tokens. Do not change --column-max.

    Add .analytics-table: width 100%; border-collapse collapse. Header row bottom
    border 1px solid var(--color-border). Cells padding var(--space-sm) block /
    var(--space-md) inline. Numeric columns (th/td that are not the glyph/language
    column) text-align right. Optional 1px hairline between body rows. .analytics-table
    .key-chip background var(--color-surface) (table sits on --color-bg; inverse of
    History-row chips).

    Add .keyboard-heatmap: overflow-x auto (last-resort only); display grid;
    grid-template-columns repeat(15, minmax(32px, 1fr)); gap var(--space-xs). Add
    .kb-key: min-width 32px; min-height 48px; padding var(--space-xs); border 1px
    solid var(--color-border); border-radius 4px; display grid; align-content center;
    justify-items center; gap 0; font uses existing label-size tokens. Do not use
    accent or destructive tokens for key fill.

    Create `src/ui/KeyboardHeatmap.tsx` exporting KeyboardHeatmap({ cells }: {
    cells: HeatmapCell[] }). Wrapper is a section with h3 className "text-label
    text-muted" copy Keyboard heatmap, then a figure role="group" aria-label as
    specified in Copywriting. Always mount the diagram (D-13). Each key is a div.kb-key
    (not a button) with gridColumn / gridRow from cell.col / cell.row and span from
    cell.span. Relative scale (D-14): among cells whose medianMs is non-null, lo =
    min median, hi = max median; t = (hi === lo) ? 0.5 : (medianMs - lo) / (hi - lo).
    Sampled fill: color-mix(in srgb, var(--heatmap-hi) calc(t * 100%), var(--heatmap-lo));
    color var(--heatmap-key-fg). Unused fill: var(--color-surface); color
    var(--color-text). On-key number: Math.round(medianMs) with no ms unit (caption
    carries the unit). Accessible names per Copywriting. Figure caption always the
    locked darker-amber sentence; if zero cells have a non-null medianMs, also render
    the ungated caption. No tooltip-only values. No hover elevation. No motion on
    color. Math.round only in this leaf, never by mutating cells.
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "--heatmap-lo" src/index.css returns at least 2 (light + dark).
    - grep -c "--heatmap-hi: #c97116" src/index.css returns 1.
    - grep -c "grid-template-columns: repeat(15" src/index.css returns 1.
    - grep -c "min-height: 48px" src/index.css returns 1.
    - grep -c "overflow-x: auto" src/index.css returns at least 1 (on .keyboard-heatmap).
    - grep -c "role=\"group\"" src/ui/KeyboardHeatmap.tsx returns 1.
    - KeyboardHeatmap.tsx contains no button elements for keys (keys are div.kb-key).
    - grep -c "Math.round" src/ui/KeyboardHeatmap.tsx returns at least 1.
    - pnpm typecheck exits 0.
  </acceptance_criteria>
  <done>
    Heatmap tokens and 15-column grid exist; KeyboardHeatmap is a static inert diagram
    with relative amber + on-key ms, unused keys neutral, diagram never replaced by
    a paragraph.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Table leaves + AnalyticsDashboard three-way branch (D-02, D-04, D-05, D-07, D-08, D-16..D-20)</name>
  <files>src/ui/DigraphLatencyView.tsx, src/ui/LanguageProfileView.tsx, src/ui/AnalyticsDashboard.tsx, src/ui/AnalyticsDashboard.test.tsx</files>
  <read_first>
    - src/ui/HistoryView.tsx (useLiveQuery(listNewestFirst), undefined vs [] vs length, role=status, h2 always present, lines 1-34)
    - src/ui/HistoryView.test.tsx (waitForLiveQuery, loading-vs-empty, buildInput/saveSession, IS_REACT_ACT_ENVIRONMENT)
    - src/ui/ResultsView.tsx (glyphFor whitespace, Math.round ms — glyph convention only; do not copy the ol markup)
    - src/trainer/state.ts (glyphFor: space → ·, newline → ↵)
    - src/analytics/analytics.ts (computeDigraphLatency, computeLanguageProfile)
    - src/analytics/heatmap.ts (computeKeyboardHeatmap)
    - src/ui/KeyboardHeatmap.tsx (Task 1)
    - .planning/phases/06-cross-session-analytics/06-UI-SPEC.md (Copywriting Contract tables; Layout stacked sections; UI Considerations D-04/D-07)
  </read_first>
  <behavior>
    - AnalyticsDashboard heading is Analytics. While useLiveQuery is undefined, text contains Loading analytics… and does not contain No sessions yet and does not contain Slowest digraphs.
    - Once the query resolves to [], text contains the History-matching empty sentence and the three section titles are absent.
    - Once at least one session is saved, all three section titles mount in order Slowest digraphs, then Keyboard heatmap, then Language profile, even if digraph rows are empty.
    - DigraphLatencyView with [] shows the locked not-enough-samples paragraph and no table element.
    - DigraphLatencyView with rows shows a table with headers Digraph, Median, Samples; each body row has a key-chip glyph, "{Math.round(medianMs)} ms", and the integer sampleCount; at most 10 data rows.
    - LanguageProfileView renders a table with headers Language, WPM, Adj. WPM, Accuracy, Sessions; language is a key-chip of the raw tag; WPM/adj are rounded integers; accuracy is rounded percent with a % suffix; plaintext is not rewritten.
  </behavior>
  <action>
    Create `src/ui/DigraphLatencyView.tsx` exporting DigraphLatencyView({ rows }: {
    rows: DigraphEntry[] }). Section with h3 className "text-label text-muted" copy
    Slowest digraphs. If rows.length === 0, muted p with the locked sentence about not
    enough digraph samples and at least 5 in-window observations — no table. Else
    semantic table className analytics-table with headers Digraph, Median, Samples.
    Local digraphGlyph: Array.from(pair).map each codepoint through glyphFor when it
    is space or newline, else the codepoint itself, join with no separator. Chip
    className key-chip wrapping that glyph. Median cell `{Math.round(entry.medianMs)} ms`.
    Samples cell the integer sampleCount (no n= prefix — UI-SPEC Copywriting). Key
    rows by entry.pair. Rows are inert diagnostic cells — no row click handlers
    and no focusable row attributes. Do not reuse the ResultsView ordered-list
    markup from PATTERNS.md; UI-SPEC locks the semantic table.

    Create `src/ui/LanguageProfileView.tsx` exporting LanguageProfileView({ rows }: {
    rows: LanguageProfileRow[] }). Section h3 Language profile (same label/muted
    classes). Semantic table analytics-table, headers Language, WPM, Adj. WPM,
    Accuracy, Sessions. Language cell: span.key-chip with row.language verbatim
    (D-20). WPM / Adj. WPM: Math.round of wpm / symbolAdjustedWpm, bare integers.
    Accuracy: Math.round(accuracy * 100) + %. Sessions: sessionCount integer. Do not
    special-case plaintext. Do not gate on sessionCount. No card grid.

    Create `src/ui/AnalyticsDashboard.tsx` mirroring HistoryView's read path: import
    useLiveQuery from dexie-react-hooks and listNewestFirst from
    ../persistence/repository (the querier, never persistence/db). Outer section
    role="status" display grid gap var(--space-lg). h2 Analytics always. Three-way
    branch: undefined → p.text-muted Loading analytics… and do not mount leaves;
    length === 0 → p.text-muted with the exact History empty sentence (including the
    same apostrophe encoding HistoryView uses) and do not mount leaves; else a
    wrapper display grid gap var(--space-xl) containing, in this locked order (D-02):
    DigraphLatencyView with computeDigraphLatency(sessions), KeyboardHeatmap with
    computeKeyboardHeatmap(sessions), LanguageProfileView with
    computeLanguageProfile(sessions). No jump links, no TOC, no fourth section. Do
    not import capture modules. Subsequent live-query updates must keep this branch
    on the populated path without flashing the loading paragraph (sections stay
    mounted once sessions exist — UI-SPEC loading backstop).

    Create `src/ui/AnalyticsDashboard.test.tsx` copying HistoryView.test.tsx harness
    (IS_REACT_ACT_ENVIRONMENT, createRoot, db.delete/open, waitForLiveQuery,
    buildInput/saveSession). Cover the three &lt;behavior&gt; dashboard cases plus the
    two table-empty/table-populated cases if easier as separate leaf renders. Assert
    section title order when populated. Assert Loading analytics… is not the empty
    copy.
  </action>
  <verify>
    <automated>pnpm test -- src/ui/AnalyticsDashboard.test.tsx && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "useLiveQuery(listNewestFirst)" src/ui/AnalyticsDashboard.tsx returns 1.
    - grep -c "from '../persistence/db'" src/ui/AnalyticsDashboard.tsx returns 0.
    - grep -c "from 'dexie'" src/ui/AnalyticsDashboard.tsx returns 0.
    - grep -c "Loading analytics" src/ui/AnalyticsDashboard.tsx returns 1.
    - grep -c "analytics-table" src/ui/DigraphLatencyView.tsx returns at least 1.
    - grep -c "analytics-table" src/ui/LanguageProfileView.tsx returns at least 1.
    - grep -c "results-slowest-list" src/ui/DigraphLatencyView.tsx returns 0.
    - grep -c "glyphFor" src/ui/DigraphLatencyView.tsx returns at least 1.
    - grep -c "Adj. WPM" src/ui/LanguageProfileView.tsx returns 1.
    - grep -c "href=\\|onClick" src/ui/DigraphLatencyView.tsx src/ui/LanguageProfileView.tsx returns 0.
    - pnpm test -- src/ui/AnalyticsDashboard.test.tsx && pnpm typecheck exit 0.
  </acceptance_criteria>
  <done>
    Dashboard distinguishes loading / page-empty / populated; tables follow UI-SPEC
    copy and columns; zero eligible digraphs is a section empty, not a missing heatmap
    or language table.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: App view union, third nav button, hide-not-unmount for Analytics (D-01, D-08, D-15)</name>
  <files>src/ui/App.tsx, src/ui/App.test.tsx</files>
  <read_first>
    - src/ui/App.tsx (view state line 59; header nav 150-165; trainer wrapper display toggle 186-200; HistoryView mount 216; CorpusInput 173 — stays outside the view switch)
    - src/ui/App.test.tsx (single-active invariant 113-142; D-08 History round-trip 145-224 — extend, do not rewrite)
    - src/ui/AnalyticsDashboard.tsx (Task 2)
    - src/ui/CaptureSurface.tsx (must remain inside the always-mounted trainer wrapper; no new analytics import here — D-15)
    - .planning/phases/06-cross-session-analytics/06-UI-SPEC.md (Layout header; Interaction view toggle; no-layout-shift)
  </read_first>
  <behavior>
    - On first paint the nav has exactly three buttons Trainer, History, Analytics; Trainer has aria-current="page"; the others do not.
    - Clicking Analytics moves aria-current to Analytics only, mounts a heading Analytics, and sets the loaded-exercise wrapper display to none without replacing the capture textarea node.
    - Clicking Analytics then Trainer restores the same textarea identity, charLog length, per-char status, and caret index as the existing History D-08 test.
    - CorpusInput is present while view is analytics.
    - CaptureSurface.tsx is unchanged (no heatmap import).
  </behavior>
  <action>
    In `src/ui/App.tsx` extend the view union to 'trainer' | 'history' | 'analytics'
    with default 'trainer' (D-01). Import AnalyticsDashboard. Add a third native
    button after History, labelled Analytics, aria-current when view === 'analytics',
    onClick setView('analytics'). Do not color the active button with the accent
    token — existing nav CSS already keys on aria-current. Leave the trainer wrapper
    as display: view === 'trainer' ? 'grid' : 'none' (inline display ternary only;
    never a boolean visibility attribute on the wrapper; never a conditional unmount
    of CaptureSurface). After the HistoryView conditional, add
    {view === 'analytics' && &lt;AnalyticsDashboard /&gt;}. CorpusInput stays where it is
    (visible on Analytics, matching History). Loading an exercise from Analytics
    does not auto-flip view back to trainer. Focus stays on the activated nav button
    (do not steal focus into the dashboard or the capture textarea). View switch is
    instant — no fade.

    Extend `src/ui/App.test.tsx`: update the single-active invariant to three buttons
    and assert clicking Analytics puts aria-current only on Analytics. Keep the
    existing History D-08 test. Add a parallel D-08 case: type the same a / x /
    delete / b sequence, switch to Analytics, assert wrapper display none and an h2
    Analytics, then back to Trainer and assert textarea identity, charLog length,
    per-char statuses, and caret index match the pre-switch snapshot. Assert
    document.querySelector('#corpus-paste') is non-null while Analytics is showing.
  </action>
  <verify>
    <automated>pnpm test -- src/ui/App.test.tsx src/ui/AnalyticsDashboard.test.tsx && pnpm typecheck && pnpm lint && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "trainer' | 'history' | 'analytics'" src/ui/App.tsx returns 1.
    - grep -c "setView('analytics')" src/ui/App.tsx returns 1.
    - grep -c "AnalyticsDashboard" src/ui/App.tsx returns at least 2 (import + mount).
    - grep -c "view === 'trainer' ? 'grid' : 'none'" src/ui/App.tsx returns 1.
    - grep -E -c "hidden=" src/ui/App.tsx returns 0 (the trainer wrapper uses the inline display ternary, not a boolean visibility attribute).
    - grep -c "from '../analytics/" src/ui/CaptureSurface.tsx returns 0.
    - grep -c "from '../ui/KeyboardHeatmap" src/ui/CaptureSurface.tsx returns 0.
    - App.test.tsx asserts three nav buttons and an Analytics D-08 round-trip.
    - pnpm test -- src/ui/App.test.tsx src/ui/AnalyticsDashboard.test.tsx && pnpm typecheck && pnpm lint && pnpm build exit 0.
  </acceptance_criteria>
  <done>
    Analytics is a third hide-not-unmount sibling of Trainer and History; the hot path
    does not import the heatmap; CorpusInput stays visible; all gates green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IndexedDB origin store → AnalyticsDashboard render | Persisted digraph pairs, key labels, and exercise.language re-enter JSX |
| Header nav click → view union | Trusted UI chrome; no URL / router surface |
| CaptureSurface hot path | Must remain free of analytics imports (D-15) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-07 | Tampering | Digraph pair / language tag / key label in JSX | medium | mitigate | Render only as React text children (chip text, table cells, aria-label templates). No innerHTML APIs. glyphFor is a closed two-entry map (space, newline). ASVS V5 |
| T-06-08 | Info Disclosure | Analytics page showing pasted corpus-derived digraphs | low | accept | Same local-only IndexedDB model as History; this plan adds no export, sync, or network |
| T-06-09 | Elevation of Privilege / UI redress | Heatmap keys mistaken for buttons | low | mitigate | Keys are divs, inert, no tabindex; figure role=group; tables have no row click (D-10 / Interaction Contract) |
| T-06-10 | Denial of Service (self) | Full-history re-fold on dashboard mount | low | accept | CONTEXT carried-forward; live updates replace table contents in place (loading backstop) |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages; no Recharts / heatmap.js / react-simple-keyboard / d3-scale |

Canon stored-XSS / prototype-pollution → breadcrumb /gsd-secure-phase + T-06-07. No high-severity findings (block threshold: high).
</threat_model>

<verification>
- pnpm typecheck && pnpm test && pnpm lint && pnpm build — all green.
- Nav has three buttons; D-08 holds for Analytics as well as History.
- Dashboard loading ≠ empty; sections mount only when sessions.length &gt; 0.
- Heatmap tokens exist in both themes; CaptureSurface has no analytics import.
- Manual (end-of-phase human-check, workflow.human_verify_mode: end-of-phase):
  1. Chromium light + dark: open Analytics with empty DB — page empty copy, no keyboard diagram.
  2. Complete several exercises (mix of paste languages including plaintext) until some pairs and keys clear n=5 — confirm top-10 table, amber heatmap with numbers, language rows including plaintext, no green/red score colors.
  3. Mid-exercise switch to Analytics and back — typed text and caret survive.
  4. Narrow the window toward 320px — page does not grow a horizontal scrollbar; heatmap may scroll inside .keyboard-heatmap.
</verification>

<success_criteria>
- Third header item Analytics shows the stacked dashboard (D-01, D-02) (ANLY-03/04/05).
- Empty history is a single page empty state; section empties only apply when sessions exist (D-04, D-07).
- Heatmap is post-hoc, color + number, unused keys drawn (D-12, D-13, D-15).
- Language table includes plaintext as its own bucket and always shows adj. WPM (D-17, D-20).
- All automated gates green; changes committed.
</success_criteria>

<output>
Create `.planning/phases/06-cross-session-analytics/06-03-SUMMARY.md` when done.
</output>

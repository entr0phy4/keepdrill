---
phase: 06-cross-session-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/metrics/latency-stats.ts
  - src/metrics/latency-stats.test.ts
  - src/metrics/metrics.ts
  - src/metrics/metrics.test.ts
  - src/metrics/resolve-metrics.ts
  - src/ui/history-metrics.ts
  - src/ui/history-metrics.test.ts
autonomous: true
requirements: [ANLY-03, ANLY-04, ANLY-05]

must_haves:
  truths:
    # --- Goal-backward (shared extract for ANLY-03 / ANLY-04 / ANLY-05) ---
    - "A single exported filter→gate→median helper is the only place that owns the exclusive (25ms, 1000ms) window, so digraph ranking and the physical-key heatmap cannot drift from slowestFive (D-09, ANLY-03/ANLY-04)"
    - "DIGRAPH_MIN_SAMPLES is a named export equal to 5 so the post-filter gate is a one-line tune (D-06, D-13)"
    - "slowestFive still gates on CHAR_MIN_SAMPLES = 3 and still returns at most 5 SlowestKeyEntry values with no sampleCount field (METR-03 regression; D-09 extract must not change ResultsView)"
    - "resolveMetrics lives in src/metrics/ so analytics never imports from src/ui/; HistoryView keeps compiling via a one-line re-export (ANLY-05, RESEARCH Open Question 2)"
    - "resolveMetrics still returns the cached snapshot when schemaVersion matches METRICS_SCHEMA_VERSION and still recomputes with completedAtTMs as now when it mismatches (Phase 4 D-05 / Phase 5 schema v2)"
    # --- ANLY-03 edge coverage (covered) ---
    - "ANLY-03 boundary: a sample whose gap equals MIN_GAP_MS (25) or MAX_GAP_MS (1000) is discarded; a sample at 26ms or 999ms is kept (one step either side of the exclusive window, same contract as metrics.test.ts case n=8)"
    - "ANLY-03 boundary: gatedMedian(samples, 5) returns null when fewer than 5 samples remain POST-filter (4 in-window → omitted) and a non-null {medianMs, sampleCount} when the 5th in-window sample arrives"
    - "ANLY-03 empty: median([]) returns 0; gatedMedian([], n) returns null for any n ≥ 1; neither throws"
    - "ANLY-03 precision: even-length filtered lists use the arithmetic mean of the two middle values with no rounding inside latency-stats.ts; odd-length lists use the middle value; display-layer rounding is out of this plan"
  backstops: []
  prohibitions:
    - statement: "MUST NOT copy-paste the gap-window numbers into analytics/ or leave a second private MIN_GAP_MS in metrics.ts after the extract — one helper owns the window (transparency of the sample gate, D-09)"
      status: resolved
      verification: test
    - statement: "MUST NOT round medianMs inside latency-stats.ts or metrics.ts — rounding stays display-layer only"
      status: resolved
      verification: test
    - statement: "MUST NOT add a network, sync, export, or telemetry path while extracting these helpers — persistence stays local-only IndexedDB (privacy)"
      status: resolved
      verification: test
  artifacts:
    - path: "src/metrics/latency-stats.ts"
      provides: "MIN_GAP_MS, MAX_GAP_MS, CHAR_MIN_SAMPLES, DIGRAPH_MIN_SAMPLES, median, gatedMedian — pure extract of metrics.ts filter→gate→median (D-06, D-09)"
      exports: ["MIN_GAP_MS", "MAX_GAP_MS", "CHAR_MIN_SAMPLES", "DIGRAPH_MIN_SAMPLES", "median", "gatedMedian"]
    - path: "src/metrics/metrics.ts"
      provides: "MODIFIED — slowestFive calls gatedMedian(samples, CHAR_MIN_SAMPLES); private window/median/MIN_SAMPLES deleted"
    - path: "src/metrics/resolve-metrics.ts"
      provides: "resolveMetrics(s: ResolvableSession) moved out of ui/ so analytics can recompute schema-stale WPM without importing ui/"
      exports: ["resolveMetrics", "ResolvableSession"]
    - path: "src/ui/history-metrics.ts"
      provides: "MODIFIED — re-exports resolveMetrics from metrics/resolve-metrics (no duplicated guard body)"
  key_links:
    - from: "src/metrics/metrics.ts"
      to: "src/metrics/latency-stats.ts"
      via: "slowestFive calls gatedMedian(samples, CHAR_MIN_SAMPLES) and pushes gated.medianMs"
      pattern: "gatedMedian\\("
    - from: "src/ui/history-metrics.ts"
      to: "src/metrics/resolve-metrics.ts"
      via: "re-export resolveMetrics so HistoryView import path is unchanged"
      pattern: "export \\{ resolveMetrics \\} from '../metrics/resolve-metrics'"
---

<objective>
Extract the shared latency filter→gate→median helper out of `slowestFive` and move
`resolveMetrics` into `src/metrics/` so Phase 6's three analytics folds can reuse
METR-03 constants and schema-aware WPM without copy-paste or a ui/ import
(D-06, D-09; ANLY-03, ANLY-04, ANLY-05 foundation).

Purpose: the post-filter sample gate and the exclusive (25ms, 1000ms) window
become a named, tested module. Digraph ranking and the heatmap cannot drift from
single-char `slowestFive`. Language-profile WPM can call `resolveMetrics` from
pure-core. Existing `metrics.test.ts` cases n=7/8/9/10/11 stay green — that is
the extract's regression net.

Output: `src/metrics/latency-stats.ts` + test, `src/metrics/resolve-metrics.ts`,
modified `metrics.ts` / `history-metrics.ts`.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cross-session-analytics/06-CONTEXT.md
@.planning/phases/06-cross-session-analytics/06-RESEARCH.md
@.planning/phases/06-cross-session-analytics/06-PATTERNS.md
@src/metrics/metrics.ts
@src/metrics/metrics.test.ts
@src/ui/history-metrics.ts
@src/ui/history-metrics.test.ts
</context>

<artifacts_this_phase_produces>
Symbols this plan creates or relocates (companion plans 06-02 / 06-03 add the folds and UI):

- `MIN_GAP_MS` (exported const, `src/metrics/latency-stats.ts`, value `25`)
- `MAX_GAP_MS` (exported const, value `1000`)
- `CHAR_MIN_SAMPLES` (exported const, value `3` — existing slowestFive gate)
- `DIGRAPH_MIN_SAMPLES` (exported const, value `5` — D-06 / D-13)
- `median(samples: readonly number[]): number`
- `gatedMedian(samples: readonly number[], minSamples: number): { medianMs: number; sampleCount: number } | null`
- `ResolvableSession` (type, `src/metrics/resolve-metrics.ts` — Pick of StoredSession fields resolveMetrics reads)
- `resolveMetrics(s: ResolvableSession): MetricsResult` (moved from `src/ui/history-metrics.ts`)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract latency-stats.ts and switch slowestFive to gatedMedian (D-06, D-09)</name>
  <files>src/metrics/latency-stats.ts, src/metrics/latency-stats.test.ts, src/metrics/metrics.ts, src/metrics/metrics.test.ts</files>
  <read_first>
    - src/metrics/metrics.ts (file-lead PURE comment lines 1-27; private MIN_GAP_MS / MAX_GAP_MS / MIN_SAMPLES at lines 48-50; median at 108-123; slowestFive at 125-137; replayAttempts stays here)
    - src/metrics/metrics.test.ts (golden cases n=7 gate, n=8 exclusive window, n=9 cap-at-5 — the extract's regression net; Case + it.each convention)
    - src/metrics/symbol-density.ts (PURE header analog for the new file)
    - .planning/phases/06-cross-session-analytics/06-PATTERNS.md (section `src/metrics/latency-stats.ts` and `src/metrics/metrics.ts`)
    - .planning/phases/06-cross-session-analytics/06-CONTEXT.md (D-06, D-09)
  </read_first>
  <behavior>
    - gatedMedian of 4 in-window samples with minSamples=5 returns null.
    - gatedMedian of 5 in-window samples with minSamples=5 returns non-null with sampleCount === 5.
    - gatedMedian discards gaps of exactly 25 and exactly 1000 and keeps 26 and 999.
    - median([]) returns 0; gatedMedian([], 1) returns null.
    - median of an even-length filtered list [100, 200] equals 150 (mean of the two middle values).
    - computeSessionMetrics still matches metrics.test.ts cases n=7 (CHAR_MIN_SAMPLES=3), n=8 (exclusive window), n=9 (cap 5).
  </behavior>
  <action>
    Create `src/metrics/latency-stats.ts` with a PURE file-lead comment in the spirit of
    metrics.ts lines 1-15: zero DOM, zero side effects, never round numbers in this module
    (round only at the display layer). State that this module owns the exclusive gap window
    and the post-filter sample gate so analytics cannot duplicate those numbers (D-09,
    PITFALLS.md Pitfall 3).

    Export four named constants as one-line tunes (per D-06): MIN_GAP_MS = 25, MAX_GAP_MS =
    1000, CHAR_MIN_SAMPLES = 3 (the existing slowestFive gate), DIGRAPH_MIN_SAMPLES = 5
    (D-06 / D-13). Do not bury 5 as a magic number inside gatedMedian.

    Export median(samples) by moving the body currently at metrics.ts lines 112-123
    verbatim, including the empty-list → 0 guard and the noUncheckedIndexedAccess guards
    on every indexed read (no non-null assertions). Even length: arithmetic mean of the
    two middle values. Odd length: the middle value.

    Export gatedMedian(samples, minSamples) returning null when the count of samples that
    are strictly greater than MIN_GAP_MS AND strictly less than MAX_GAP_MS is below
    minSamples; otherwise { medianMs: median(filtered), sampleCount: filtered.length }.
    The exclusive comparisons are load-bearing (metrics.test.ts n=8). sampleCount is new
    versus slowestFive because D-08 needs n on the digraph row; slowestFive may ignore it.

    In `src/metrics/metrics.ts`: import gatedMedian and CHAR_MIN_SAMPLES from
    ./latency-stats. Remove the three file-private window/gate constants and the
    file-private median function (those now live as exports in latency-stats.ts).
    Rewrite slowestFive so each character calls gatedMedian(samples, CHAR_MIN_SAMPLES)
    and, when non-null, pushes { char, medianMs: gated.medianMs } — do NOT add
    sampleCount to SlowestKeyEntry (ResultsView / HistoryRow do not show n for
    per-session slowest-5). Keep replayAttempts, sort-desc, and slice(0, 5) here.
    Update the METR-03 header comment to name CHAR_MIN_SAMPLES and gatedMedian rather
    than the deleted private names.

    Create `src/metrics/latency-stats.test.ts` mirroring metrics.test.ts / symbol-density.test.ts
    describe + it.each golden-table style. Cover every case in &lt;behavior&gt; except the
    computeSessionMetrics cases, which stay in metrics.test.ts. Do not weaken metrics.test.ts
    n=7/8/9.
  </action>
  <verify>
    <automated>pnpm test -- src/metrics/latency-stats.test.ts src/metrics/metrics.test.ts && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - src/metrics/latency-stats.ts exports MIN_GAP_MS, MAX_GAP_MS, CHAR_MIN_SAMPLES, DIGRAPH_MIN_SAMPLES, median, gatedMedian.
    - grep -c "DIGRAPH_MIN_SAMPLES = 5" src/metrics/latency-stats.ts returns 1.
    - grep -v '^#' src/metrics/latency-stats.ts | grep -c "Math.round" returns 0.
    - grep -c "const MIN_GAP_MS" src/metrics/metrics.ts returns 0 (private duplicate gone).
    - grep -c "gatedMedian(" src/metrics/metrics.ts returns at least 1.
    - grep -c "CHAR_MIN_SAMPLES" src/metrics/metrics.ts returns at least 1.
    - SlowestKeyEntry in metrics.ts still has only char and medianMs (no sampleCount field).
    - pnpm test -- src/metrics/latency-stats.test.ts src/metrics/metrics.test.ts && pnpm typecheck exit 0.
  </acceptance_criteria>
  <done>
    latency-stats.ts owns the exclusive window and both sample-gate constants; slowestFive
    is a thin gatedMedian caller; existing METR-03 golden cases stay green; DIGRAPH_MIN_SAMPLES
    is a named export of 5.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move resolveMetrics into src/metrics/ and re-export from history-metrics.ts (ANLY-05)</name>
  <files>src/metrics/resolve-metrics.ts, src/ui/history-metrics.ts, src/ui/history-metrics.test.ts</files>
  <read_first>
    - src/ui/history-metrics.ts (the whole file — implementation to move, including the clock-domain comment that now must never be startedAt)
    - src/ui/history-metrics.test.ts (vi.mock of ../metrics/metrics; identity-vs-recompute cases that must keep passing via the re-export)
    - src/ui/HistoryView.tsx (import { resolveMetrics } from './history-metrics' — must keep compiling with no import-path change)
    - .planning/phases/06-cross-session-analytics/06-PATTERNS.md (sections `src/metrics/resolve-metrics.ts` and `src/ui/history-metrics.ts`)
    - .planning/phases/06-cross-session-analytics/06-RESEARCH.md (Open Question 2; Pitfall 1 / Pitfall 4)
  </read_first>
  <behavior>
    - resolveMetrics returns the exact metricsSnapshot object reference when schemaVersion matches METRICS_SCHEMA_VERSION.
    - resolveMetrics calls computeSessionMetrics with exercise.text, charLog, markers, and completedAtTMs as now when schemaVersion mismatches.
    - HistoryView.tsx still typechecks importing resolveMetrics from ./history-metrics.
  </behavior>
  <action>
    Create `src/metrics/resolve-metrics.ts` with a PURE file-lead comment copied in spirit
    from history-metrics.ts lines 1-12: recompute-if-stale guard; never round; the
    recompute now argument is completedAtTMs (event.timeStamp domain), never the
    wall-clock session timestamp. Export type ResolvableSession as Pick of StoredSession
    for the five fields this function reads: exercise, charLog, markers, completedAtTMs,
    metricsSnapshot. Import StoredSession as type from ../persistence/types and
    computeSessionMetrics plus METRICS_SCHEMA_VERSION from ./metrics.

    Export function resolveMetrics(s: ResolvableSession): MetricsResult with the same
    body as today: if s.metricsSnapshot.schemaVersion === METRICS_SCHEMA_VERSION return
    s.metricsSnapshot; else return computeSessionMetrics(s.exercise.text, s.charLog,
    s.markers, s.completedAtTMs). Widening the parameter from StoredSession to
    ResolvableSession is load-bearing — analytics Session projections have no Dexie id
    (06-PATTERNS.md). StoredSession remains assignable.

    Replace the body of `src/ui/history-metrics.ts` with a one-line re-export of
    resolveMetrics from ../metrics/resolve-metrics. Keep a short file comment stating
    this file exists so HistoryView's import path does not change. Do not duplicate the
    schema-guard body.

    Keep `src/ui/history-metrics.test.ts` green. If the existing vi.mock('../metrics/metrics')
    no longer intercepts computeSessionMetrics after the move, retarget the mock at the
    same metrics module so the two existing cases (identity on match, completedAtTMs as
    now on mismatch) still pass. Do not add new cases in this task.
  </action>
  <verify>
    <automated>pnpm test -- src/ui/history-metrics.test.ts && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - src/metrics/resolve-metrics.ts exports resolveMetrics and ResolvableSession.
    - grep -c "computeSessionMetrics" src/ui/history-metrics.ts returns 0 (guard body is gone).
    - grep -c "from '../metrics/resolve-metrics'" src/ui/history-metrics.ts returns at least 1.
    - grep -c "completedAtTMs" src/metrics/resolve-metrics.ts returns at least 1.
    - HistoryView.tsx still imports resolveMetrics from ./history-metrics (no import-path change).
    - pnpm test -- src/ui/history-metrics.test.ts && pnpm typecheck exit 0.
  </acceptance_criteria>
  <done>
    resolveMetrics lives in metrics/, HistoryView still imports it from history-metrics.ts,
    and the identity-vs-recompute tests pass. Analytics can call it without importing ui/.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| metrics.ts → latency-stats.ts | In-memory numeric samples only; no new external input |
| StoredSession (IndexedDB origin) → resolveMetrics | Previously persisted local rows re-enter the recompute path |
| npm/pip/cargo | Not applicable — zero new packages this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | gatedMedian / median producing NaN from empty or non-numeric samples | low | mitigate | median([]) returns 0; gatedMedian returns null below the gate; inputs are numbers the app itself recorded as tMs deltas |
| T-06-02 | Tampering | resolveMetrics throwing on a malformed metricsSnapshot.schemaVersion | low | mitigate | !== METRICS_SCHEMA_VERSION (including undefined) takes the recompute branch rather than throwing — same as Phase 4/5 |
| T-06-03 | Info Disclosure | resolveMetrics reading exercise.text to recompute | low | accept | Same local-only model as History; this plan does not render or export the text |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages this plan; install surface is empty |

Canon security/compliance (stored XSS, prototype-pollution) is owned by /gsd-secure-phase — not minted as a prohibition. No high-severity findings (block threshold: high).
</threat_model>

<verification>
- pnpm typecheck — 0 errors.
- pnpm test -- src/metrics/latency-stats.test.ts src/metrics/metrics.test.ts src/ui/history-metrics.test.ts — green.
- Private gap-window constants are gone from metrics.ts; DIGRAPH_MIN_SAMPLES = 5 is a named export.
- HistoryView still compiles against the history-metrics re-export.
</verification>

<success_criteria>
- gatedMedian is the single post-filter gate used by slowestFive (CHAR_MIN_SAMPLES=3) and ready for digraphs/heatmap (DIGRAPH_MIN_SAMPLES=5) (D-06, D-09).
- Existing METR-03 golden cases stay green.
- resolveMetrics is importable from metrics/ without touching ui/ (ANLY-05 foundation).
- All automated gates green; changes committed.
</success_criteria>

<output>
Create `.planning/phases/06-cross-session-analytics/06-01-SUMMARY.md` when done.
</output>

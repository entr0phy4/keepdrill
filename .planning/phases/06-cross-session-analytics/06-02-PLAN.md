---
phase: 06-cross-session-analytics
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/analytics/types.ts
  - src/analytics/keyboard-geometry.ts
  - src/analytics/keyboard-geometry.test.ts
  - src/analytics/analytics.ts
  - src/analytics/analytics.test.ts
  - src/analytics/heatmap.ts
  - src/analytics/heatmap.test.ts
autonomous: true
requirements: [ANLY-03, ANLY-04, ANLY-05]

must_haves:
  truths:
    # --- Goal-backward ---
    - "computeDigraphLatency returns at most 10 DigraphEntry rows sorted by medianMs descending after gating at DIGRAPH_MIN_SAMPLES = 5 POST-filter, pooling charLog gaps across all sessions (ANLY-03, D-05, D-06)"
    - "Pairs below the gate are omitted from the returned array, not returned as greyed rows (D-07)"
    - "Each DigraphEntry carries pair + unrounded medianMs + post-filter sampleCount n (D-08 producer; UI rounds later)"
    - "Digraph pairs are two Unicode codepoints from consecutive insert records over charLog, never KeyboardEvent.code (D-09, Anti-Pattern 4)"
    - "computeKeyboardHeatmap returns one HeatmapCell per US_ANSI_KEYS entry keyed by KeystrokeEvent.code, with medianMs null when the key has fewer than 5 post-filter samples (ANLY-04, D-10, D-11, D-13)"
    - "Heatmap IKI is keydown→keydown among sampleable codes only; isRepeat keydowns and non-sampleable modifiers are neither samples nor previous-timestamp anchors (D-11, RESEARCH A1 / Pitfall 2)"
    - "computeLanguageProfile returns one LanguageProfileRow per distinct exercise.language with ≥1 session, unweighted mean of resolveMetrics wpm / symbolAdjustedWpm / accuracy, sorted by sessionCount descending (ANLY-05, D-16..D-19, RESEARCH A2)"
    - "plaintext is a normal map key and is never folded into another language string (D-20)"
    # --- ANLY-03 remaining edges ---
    - "ANLY-03 encoding: pair keys are two Array.from codepoints concatenated; a supplementary-plane character is one element of the pair, never a UTF-16 surrogate split"
    - "ANLY-03 empty: computeDigraphLatency([]) and computeDigraphLatency of sessions whose charLog is empty both return [] and do not throw"
    - "ANLY-03 boundary: a pair with 4 in-window samples is absent from the result; the 5th in-window sample makes it appear; more than 10 eligible pairs are capped at 10 after sort"
    - "ANLY-03 precision: medianMs in DigraphEntry is the unrounded gatedMedian value (even-length mean of two middles); no Math.round in analytics.ts"
    # --- Flagged assumptions (unclassified probe rows — never auto-backstop, never silent-drop) ---
    - "PLANNER ASSUMPTION (ANLY-04 unclassified probe): heatmap latency is keydown-to-keydown inter-key interval among geometry.sampleable codes, not dwell and not keyup-keydown; unused keys stay in the array with medianMs null; relative color scale is display-layer only. RESEARCH A1 is locked in this plan as the contract."
    - "PLANNER ASSUMPTION (ANLY-05 unclassified probe): language aggregation is the unweighted arithmetic mean of per-session resolveMetrics values (not duration-weighted, not a raw-keystroke re-fold); n is sessionCount; no keystroke-level gate applies (D-19, RESEARCH A2)."
  prohibitions:
    - statement: "MUST NOT key the heatmap by CommittedChar.data or any character glyph — physical KeyboardEvent.code only (ANLY-04 product axis, Anti-Pattern 4)"
      status: resolved
      verification: test
    - statement: "MUST NOT color or rank by keystroke frequency rather than median latency (ANLY-04 product differentiator)"
      status: resolved
      verification: test
    - statement: "MUST NOT omit unused or below-gate keys from the heatmap array — US_ANSI_KEYS length is the result length, medianMs null means unsampled (D-13 transparency)"
      status: resolved
      verification: test
    - statement: "MUST NOT fold plaintext or untagged sessions into a real language bucket (ANLY-05, D-20)"
      status: resolved
      verification: test
    - statement: "MUST NOT hide a language tag that has ≥1 persisted session, including n=1 (D-19 transparency)"
      status: resolved
      verification: test
    - statement: "MUST NOT replace net WPM with symbolAdjustedWpm or drop the companion field from LanguageProfileRow (D-17)"
      status: resolved
      verification: test
    - statement: "MUST NOT compute a 3-character (trigraph) ranking in this phase (ANLY-06 deferred)"
      status: resolved
      verification: test
    - statement: "MUST NOT import persistence/db, the IndexedDB wrapper package, or anything from src/ui/ into src/analytics/ (Phase 4 D-03 / layering)"
      status: resolved
      verification: test
  artifacts:
    - path: "src/analytics/types.ts"
      provides: "AnalyticsSession, DigraphEntry, HeatmapCell, LanguageProfileRow"
      exports: ["AnalyticsSession", "DigraphEntry", "HeatmapCell", "LanguageProfileRow"]
    - path: "src/analytics/keyboard-geometry.ts"
      provides: "KeyGeometry, US_ANSI_KEYS, isSampleable — US 101 alphanumeric block, W3C KeyboardEvent.code strings (D-10)"
      exports: ["KeyGeometry", "US_ANSI_KEYS", "isSampleable"]
    - path: "src/analytics/analytics.ts"
      provides: "computeDigraphLatency, computeLanguageProfile — pure folds, zero DOM (ANLY-03, ANLY-05)"
      exports: ["computeDigraphLatency", "computeLanguageProfile"]
    - path: "src/analytics/heatmap.ts"
      provides: "computeKeyboardHeatmap — physical-code IKI fold (ANLY-04)"
      exports: ["computeKeyboardHeatmap"]
  key_links:
    - from: "src/analytics/analytics.ts"
      to: "src/metrics/latency-stats.ts"
      via: "computeDigraphLatency calls gatedMedian(samples, DIGRAPH_MIN_SAMPLES) then sort-desc slice(0, 10)"
      pattern: "gatedMedian\\("
    - from: "src/analytics/heatmap.ts"
      to: "src/analytics/keyboard-geometry.ts"
      via: "computeKeyboardHeatmap maps US_ANSI_KEYS and skips codes where isSampleable is false"
      pattern: "US_ANSI_KEYS"
    - from: "src/analytics/analytics.ts"
      to: "src/metrics/resolve-metrics.ts"
      via: "computeLanguageProfile calls resolveMetrics per session before averaging"
      pattern: "resolveMetrics\\("
---

<objective>
Add the pure `src/analytics/` fold: types, US-ANSI geometry, `computeDigraphLatency`
(ANLY-03, D-05..D-09), `computeKeyboardHeatmap` (ANLY-04, D-10..D-13), and
`computeLanguageProfile` (ANLY-05, D-16..D-20). Zero DOM, zero Dexie, zero rounding.

**Discretion locked in this plan:** language aggregation is the unweighted arithmetic
mean of per-session `resolveMetrics` values (CONTEXT obvious default / RESEARCH A2).
Heatmap latency is keydown→keydown IKI among `sampleable` codes (RESEARCH A1). File
split: digraph + language in `analytics.ts`; heatmap in `heatmap.ts`; geometry in
`keyboard-geometry.ts`. Equal sessionCount rows sort by language string ascending
so tests are deterministic.

Purpose: three independent compute functions over `readonly AnalyticsSession[]` that
plan 06-03 can render without re-deriving gates, axes, or averages.

Output: `src/analytics/{types,keyboard-geometry,analytics,heatmap}.ts` plus golden
unit tests. No UI, no App.tsx, no CSS.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cross-session-analytics/06-CONTEXT.md
@.planning/phases/06-cross-session-analytics/06-RESEARCH.md
@.planning/phases/06-cross-session-analytics/06-PATTERNS.md
@src/metrics/latency-stats.ts
@src/metrics/resolve-metrics.ts
@src/metrics/metrics.ts
@src/persistence/types.ts
@src/capture/types.ts
@src/ingestion/language-map.ts
</context>

<artifacts_this_phase_produces>
Symbols this plan creates:

- `AnalyticsSession` (interface, `src/analytics/types.ts`)
- `DigraphEntry` `{ pair, medianMs, sampleCount }`
- `HeatmapCell` `{ code, row, col, span, label, medianMs: number | null, sampleCount }`
- `LanguageProfileRow` `{ language, wpm, symbolAdjustedWpm, accuracy, sessionCount }`
- `KeyGeometry` `{ code, row, col, span, label, sampleable }`
- `US_ANSI_KEYS: readonly KeyGeometry[]`
- `isSampleable(code: string): boolean`
- `computeDigraphLatency(sessions: readonly AnalyticsSession[]): DigraphEntry[]`
- `computeLanguageProfile(sessions: readonly AnalyticsSession[]): LanguageProfileRow[]`
- `computeKeyboardHeatmap(sessions: readonly AnalyticsSession[]): HeatmapCell[]`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Analytics types + US-ANSI geometry table (D-10, D-13)</name>
  <files>src/analytics/types.ts, src/analytics/keyboard-geometry.ts, src/analytics/keyboard-geometry.test.ts</files>
  <read_first>
    - src/metrics/metrics.ts (SlowestKeyEntry / MetricsResult house style, lines 35-46)
    - src/persistence/types.ts (StoredSession fields analytics must project, lines 14-34)
    - src/capture/types.ts (KeystrokeEvent.code / tMs / isRepeat, CommittedChar)
    - src/ingestion/language-map.ts (pure static-table analog — named constant + tiny helper; do NOT copy platform/layout.ts)
    - .planning/phases/06-cross-session-analytics/06-PATTERNS.md (sections `src/analytics/types.ts` and `src/analytics/keyboard-geometry.ts`)
    - .planning/phases/06-cross-session-analytics/06-UI-SPEC.md (Layout — keyboard heatmap: canonical W3C codes, integer spans)
  </read_first>
  <behavior>
    - AnalyticsSession is a structural projection with completedAtTMs, exercise, events, charLog, markers, metricsSnapshot — no Dexie id required.
    - US_ANSI_KEYS includes Backquote, Digit0–Digit9, Minus, Equal, Backspace, Tab, KeyQ–KeyP, BracketLeft, BracketRight, Backslash, CapsLock, KeyA–KeyL, Semicolon, Quote, Enter, ShiftLeft, KeyZ–KeyM, Comma, Period, Slash, ShiftRight, ControlLeft, MetaLeft, AltLeft, Space, AltRight, MetaRight, ControlRight and no F-row / Arrow / Numpad / Intl codes.
    - isSampleable('KeyA') is true; isSampleable('ShiftLeft') is false; isSampleable('F1') is false.
    - Spans: Backspace 2, Tab 2, CapsLock 2, Enter 2, ShiftLeft 3, ShiftRight 3, Space 7, remaining writing-system keys 1 (UI-SPEC integer spans).
  </behavior>
  <action>
    Create `src/analytics/types.ts` with a file-lead comment naming this as the read
    projection of StoredSession (enough for the three folds, no Dexie id) plus the three
    fold-output types. Import types only: CommittedChar, KeystrokeEvent, CaptureMarker from
    capture/types; Exercise from ingestion/types; MetricsResult from metrics/metrics. Do
    not re-declare those upstream types.

    Export interface AnalyticsSession with completedAtTMs, exercise, events (readonly
    KeystrokeEvent[]), charLog (readonly CommittedChar[]), markers (readonly
    CaptureMarker[]), metricsSnapshot. StoredSession extra fields (id, startedAt,
    schemaVersion) are intentionally absent so golden tests never need Dexie.
    AnalyticsSession must remain assignable from a StoredSession value (extra fields are
    fine on the producer side).

    Export DigraphEntry { pair: string; medianMs: number; sampleCount: number }.
    Export HeatmapCell { code, row, col, span, label, medianMs: number | null,
    sampleCount } — null medianMs means unused / below-gate (D-13).
    Export LanguageProfileRow { language, wpm, symbolAdjustedWpm, accuracy, sessionCount }.

    Create `src/analytics/keyboard-geometry.ts` as a PURE static table analog of
    language-map.ts. Do not import or mirror the runtime layout-mismatch warning module
    under src/platform/ (D-10). Export interface KeyGeometry { code, row (0 = number row
    … 4 = space row), col, span, label (US-ANSI unshifted glyph string), sampleable }.
    Export US_ANSI_KEYS covering the canonical W3C UI Events code list in 06-UI-SPEC /
    06-RESEARCH (alphanumeric block only). sampleable true for letters, digits,
    punctuation, Space, Enter, Backspace, Tab. sampleable false (drawn, never sampled)
    for ShiftLeft/Right, ControlLeft/Right, AltLeft/Right, MetaLeft/Right, CapsLock.
    Integer spans per UI-SPEC: Backspace 2, Tab 2 (1.5 implemented as 2), CapsLock 2,
    Enter 2, ShiftLeft/ShiftRight 3, Space 7, remaining writing-system keys 1. Labels are
    unshifted US glyphs (Digit1 → "1", KeyA → "A", Minus → "-", Space → "Space" or a short
    unshifted glyph of ≤5 characters so the 32×48 key box can hold label + ms). Export
    isSampleable(code) as US_ANSI_KEYS.some(k => k.code === code && k.sampleable).

    Create `src/analytics/keyboard-geometry.test.ts` asserting the &lt;behavior&gt; cases:
    isSampleable for KeyA vs ShiftLeft vs a function-row code string; US_ANSI_KEYS
    contains Digit9 / BracketLeft / Space and does not contain function-row, arrow,
    numpad, or Intl* codes; Backspace/Tab/CapsLock/Enter span 2, both Shifts span 3,
    Space spans 7.
  </action>
  <verify>
    <automated>pnpm test -- src/analytics/keyboard-geometry.test.ts && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - src/analytics/types.ts exports AnalyticsSession, DigraphEntry, HeatmapCell, LanguageProfileRow.
    - grep -E -c "F1|ArrowLeft|NumpadEnter" src/analytics/keyboard-geometry.ts returns 0.
    - grep -c "Digit9" src/analytics/keyboard-geometry.ts returns at least 1.
    - grep -c "sampleable: false" src/analytics/keyboard-geometry.ts returns at least 1 (modifiers present and unsampleable).
    - grep -c "from '../platform/" src/analytics/keyboard-geometry.ts returns 0.
    - pnpm test -- src/analytics/keyboard-geometry.test.ts && pnpm typecheck exit 0.
  </acceptance_criteria>
  <done>
    Types and the US 101 geometry table exist; modifiers are drawn-but-unsampleable;
    F-row / arrows / numpad / Intl codes are absent; analytics does not reuse the
    platform layout-warning module.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: computeDigraphLatency + computeLanguageProfile (D-05..D-09, D-16..D-20)</name>
  <files>src/analytics/analytics.ts, src/analytics/analytics.test.ts</files>
  <read_first>
    - src/metrics/metrics.ts (replayAttempts delete/insert walk lines 78-102; Array.from(rec.data ?? ''); gap attaches only to last codepoint of a multi-codepoint insert)
    - src/metrics/latency-stats.ts (gatedMedian, DIGRAPH_MIN_SAMPLES from plan 06-01)
    - src/metrics/resolve-metrics.ts (ResolvableSession / resolveMetrics from plan 06-01)
    - src/metrics/metrics.test.ts (char() fixture helper; cases n=10 IME, n=11 brace vs bracket, n=12 supplementary-plane)
    - src/ui/history-metrics.test.ts (makeStoredSession fixture shape)
    - .planning/phases/06-cross-session-analytics/06-PATTERNS.md (section `src/analytics/analytics.ts` and `src/analytics/analytics.test.ts`)
    - .planning/phases/06-cross-session-analytics/06-CONTEXT.md (D-05 through D-09, D-16 through D-20)
  </read_first>
  <behavior>
    - Four in-window samples of pair "ab" yield no DigraphEntry; the fifth in-window sample makes "ab" appear with sampleCount 5.
    - Eligible pairs sort by medianMs descending and the result length is at most 10.
    - A delete record between two inserts does not emit a pair spanning the delete (prevInsertChar cleared; prevTMs still updated).
    - A multi-codepoint insert attributes the gap only to its last codepoint (IME rule, metrics.test.ts n=10 generalized); earlier codepoints of that insert still become prevInsertChar for the next record.
    - A supplementary-plane codepoint is one Array.from element in the pair key.
    - Pairs "{x" and "[x" are distinct DigraphEntry.pair values.
    - computeDigraphLatency([]) returns [].
    - Two rust sessions and one plaintext session yield two LanguageProfileRow values; plaintext is present; rows sort by sessionCount descending (rust first).
    - A session whose metricsSnapshot.schemaVersion is not METRICS_SCHEMA_VERSION still contributes a defined symbolAdjustedWpm via resolveMetrics.
    - LanguageProfileRow always includes wpm, symbolAdjustedWpm, accuracy, sessionCount; wpm is the unweighted mean of per-session resolveMetrics().wpm.
  </behavior>
  <action>
    Create `src/analytics/analytics.ts` with a PURE file-lead comment: zero DOM, zero
    IndexedDB imports, no rounding, latency samples live in the tMs domain only.
    Explicitly name Anti-Pattern 4: this file groups by committed codepoint pairs, never
    by physical key code. Import gatedMedian and DIGRAPH_MIN_SAMPLES from
    ../metrics/latency-stats and resolveMetrics from ../metrics/resolve-metrics.

    Export computeDigraphLatency(sessions). Pool samples across all sessions in a
    Map&lt;string, number[]&gt;. Per session walk charLog: on inputType.startsWith('delete'),
    set prevInsertChar = null and prevTMs = rec.tMs and continue (do not emit a pair
    across a delete — RESEARCH Pitfall 7). On insert, Array.from(rec.data ?? '') and for
    each codepoint, if it is the last codepoint of the record AND prevInsertChar and
    prevTMs are non-null, push (rec.tMs - prevTMs) onto the pair prevInsertChar + ch.
    Then set prevInsertChar = ch for every codepoint (including non-last). After the
    codepoint loop, set prevTMs = rec.tMs. After pooling, for each pair call
    gatedMedian(samples, DIGRAPH_MIN_SAMPLES); omit nulls (D-07); push { pair, medianMs,
    sampleCount }; sort by medianMs descending; return slice(0, 10) (D-05).

    Export computeLanguageProfile(sessions). Group by s.exercise.language as the raw
    stored string — plaintext is a normal key, never rewritten, never merged (D-20).
    For each session call resolveMetrics(s) (AnalyticsSession is assignable to
    ResolvableSession) and push wpm, symbolAdjustedWpm, accuracy into that bucket.
    Mean is unweighted arithmetic mean (sum/length) of those per-session values
    (RESEARCH A2 — not duration-weighted, not a raw-keystroke re-fold). sessionCount
    is the bucket length. Sort by sessionCount descending (D-18); break ties by language
    string ascending. No keystroke-level gate and no minimum sessionCount beyond ≥1
    (D-19). Do not add a languageSource field.

    Create `src/analytics/analytics.test.ts` in the existing unit vitest project
    (src/**/*.test.ts). Build AnalyticsSession literals (no Dexie), mirroring
    history-metrics.test.ts makeStoredSession. Cover every case in &lt;behavior&gt; as an
    explicit assertion. Do not import from src/ui/.
  </action>
  <verify>
    <automated>pnpm test -- src/analytics/analytics.test.ts && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - src/analytics/analytics.ts exports computeDigraphLatency and computeLanguageProfile.
    - grep -v '^#' src/analytics/analytics.ts | grep -c "Math.round" returns 0.
    - grep -v '^#' src/analytics/analytics.ts | grep -c "from 'dexie'" returns 0.
    - grep -v '^#' src/analytics/analytics.ts | grep -c "from '../ui/" returns 0.
    - grep -v '^#' src/analytics/analytics.ts | grep -c "from '../persistence/db" returns 0.
    - grep -c "DIGRAPH_MIN_SAMPLES" src/analytics/analytics.ts returns at least 1.
    - grep -c "resolveMetrics" src/analytics/analytics.ts returns at least 1.
    - grep -c "trigraph\\|computeTrigraph" src/analytics/analytics.ts returns 0.
    - pnpm test -- src/analytics/analytics.test.ts && pnpm typecheck exit 0.
  </acceptance_criteria>
  <done>
    Digraph ranking pools charLog pairs, gates at 5 post-filter samples, caps at 10,
    omits sub-threshold pairs, and is Unicode-safe. Language profile averages
    resolveMetrics per raw language tag with plaintext as its own bucket.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: computeKeyboardHeatmap by physical code (D-11, D-13, D-15 producer)</name>
  <files>src/analytics/heatmap.ts, src/analytics/heatmap.test.ts</files>
  <read_first>
    - src/analytics/keyboard-geometry.ts (Task 1 — US_ANSI_KEYS, isSampleable)
    - src/metrics/latency-stats.ts (gatedMedian, DIGRAPH_MIN_SAMPLES)
    - src/capture/types.ts (KeystrokeEvent: type, code, tMs, isRepeat)
    - src/capture/capture.ts (isRepeat recording — heatmap must skip those events)
    - .planning/phases/06-cross-session-analytics/06-PATTERNS.md (section `src/analytics/heatmap.ts` and `src/analytics/heatmap.test.ts`)
    - .planning/phases/06-cross-session-analytics/06-RESEARCH.md (Pitfall 2 modifiers-as-anchor, Pitfall 3 character-keying, Pattern 2)
  </read_first>
  <behavior>
    - computeKeyboardHeatmap always returns an array whose length equals US_ANSI_KEYS.length.
    - The same code Digit9 producing key "9" then key "(" pools on Digit9 (not on the character).
    - isRepeat true keydowns contribute neither a sample nor a prevTMs update.
    - ShiftLeft is not an IKI anchor: Digit1 after ShiftLeft measures from the previous sampleable keydown.
    - A code with fewer than 5 post-filter samples has medianMs === null and still appears in the array.
    - Samples are tMs deltas only; the wall-clock session timestamp is not read.
  </behavior>
  <action>
    Create `src/analytics/heatmap.ts` with a PURE file-lead comment: this fold groups by
    physical KeystrokeEvent.code (ARCHITECTURE.md Anti-Pattern 4 — the inverse of
    slowestFive's do-not-group-by-code rule). Zero DOM, no rounding, relative color
    interpolation lives in the UI leaf not here. Import gatedMedian and
    DIGRAPH_MIN_SAMPLES, plus US_ANSI_KEYS and isSampleable.

    Export computeKeyboardHeatmap(sessions). Per session walk events: skip unless
    type === 'keydown'. Skip isRepeat (neither sample nor anchor). Skip when
    !isSampleable(code) (modifiers drawn but never sampled and never used as the
    previous-timestamp anchor — RESEARCH Pitfall 2). If prevTMs is non-null, push
    (ev.tMs - prevTMs) onto samplesByCode for ev.code. Then set prevTMs = ev.tMs.
    After pooling, return US_ANSI_KEYS.map: gatedMedian(samplesByCode.get(key.code)
    ?? [], DIGRAPH_MIN_SAMPLES); medianMs is gated.medianMs or null; sampleCount is
    gated.sampleCount or 0; copy code/row/col/span/label from geometry. Do not walk
    the committed-character log (events only). Do not import capture hot-path
    modules into this file beyond types.

    Create `src/analytics/heatmap.test.ts` covering every &lt;behavior&gt; case. Fixture
    KeystrokeEvent objects with explicit tMs / code / isRepeat. Assert result length
    equals US_ANSI_KEYS.length. Assert Digit9 pooling. Assert ShiftLeft does not
    collapse Digit1's gap to the Shift-to-key interval.
  </action>
  <verify>
    <automated>pnpm test -- src/analytics/heatmap.test.ts src/analytics/analytics.test.ts && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - src/analytics/heatmap.ts exports computeKeyboardHeatmap.
    - grep -v '^#' src/analytics/heatmap.ts | grep -c "charLog" returns 0.
    - grep -v '^#' src/analytics/heatmap.ts | grep -c "Math.round" returns 0.
    - grep -v '^#' src/analytics/heatmap.ts | grep -c "from 'dexie'" returns 0.
    - grep -c "DIGRAPH_MIN_SAMPLES" src/analytics/heatmap.ts returns at least 1.
    - grep -c "isRepeat" src/analytics/heatmap.ts returns at least 1.
    - pnpm test -- src/analytics/heatmap.test.ts src/analytics/analytics.test.ts && pnpm typecheck exit 0.
  </acceptance_criteria>
  <done>
    Heatmap fold returns the full US-ANSI cell list keyed by physical code, gates at
    5 post-filter samples, ignores repeats and modifiers-as-anchors, and leaves
    below-gate keys at medianMs null.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| StoredSession projection → analytics folds | Persisted charLog / events / exercise.language re-enter a pure numeric/string fold |
| DigraphEntry.pair / HeatmapCell.label / LanguageProfileRow.language → later UI (plan 06-03) | Untrusted persisted strings leave this plan as data, not HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04 | Tampering | computeDigraphLatency pair strings derived from charLog.data | low | mitigate | Pair is two codepoints concatenated; no HTML assembly in this plan; plan 06-03 renders as React text (ASVS V5) |
| T-06-05 | Denial of Service (self) | O(sessions × keystrokes) full-history re-fold | low | accept | CONTEXT carried-forward: acceptable at single-user scale; incremental cache is deferred |
| T-06-06 | Tampering | computeLanguageProfile grouping on exercise.language | low | mitigate | Language is the stored string as-is; no eval; plaintext is a distinct key not merged |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages this plan |

Canon stored-XSS / prototype-pollution → breadcrumb /gsd-secure-phase; not minted here. No high-severity findings.
</threat_model>

<verification>
- pnpm typecheck — 0 errors.
- pnpm test -- src/analytics/analytics.test.ts src/analytics/heatmap.test.ts — green, including gate 4-vs-5, cap 10, delete-reset, Digit9 pooling, ShiftLeft non-anchor, plaintext bucket, stale-schema adj. WPM.
- src/analytics/ has zero IndexedDB-package imports and zero ui/ imports.
</verification>

<success_criteria>
- Ranked digraphs: top 10, n≥5 post-filter, omitted below gate, Unicode-safe pairs (ANLY-03, D-05..D-09).
- Heatmap cells: full US-ANSI list, physical code, medianMs null below gate (ANLY-04, D-10..D-13).
- Language rows: unweighted mean of resolveMetrics, plaintext distinct, sorted by n desc (ANLY-05, D-16..D-20).
- All automated gates green; changes committed.
</success_criteria>

<output>
Create `.planning/phases/06-cross-session-analytics/06-02-SUMMARY.md` when done.
</output>
